Hi and welcome to part six of the supplementary coding-along video series for the Build a Large Language Model from Scratch book. My name is Sebastian Raschka and I'm the author of the book and yeah, these supplementary video series are meant as an additional resource where I guide you a bit through the code in a more casual way. So in part five, what we have done so far is we pre-trained the GPT model. And now we can finally take advantage of that and build an interesting application on top of this model. So let me just show you where we are in this book. So in the beginning, we coded the data sampling, the attension mechanism, and the LLM architecture. And then in part five here, we essentially pre-trained the model. But pre-trained models are only, I would say, the beginning. So with a pre-trained model, they are quite interesting, but they are just the base model or the foundation for more interesting applications. So for example, when you use something like chat GPT, you are probably thinking of a model that is capable of interacting with a user by addressing or answering to certain queries, for example, summarize this text, tell me who was the first president of the U S or other kinds of things. So translating, summarization, writing a paragraph, rephrasing things, answering questions. These are all things that you would expect from a chat bot or a personal assistant. And this is what we are going to do in part seven. First, so in this part, in part six, I want to show you a simpler application of fine tuning. So part six and part seven, they will be both concerned with fine tuning the model. So fine tuning essentially means that we're taking the pre-trained model and train it further or fine tune it for a certain target application where a target application could be classification or developing this personal assistant. In this case, this will be called instruction fine tuning as we will see in part seven. But yeah, actually I want to start with fine tuning this GPT model that we pre-trained or essentially also the model where we loaded the pre-trained weights from OpenAI as a classifier, because I think this is a great introduction to fine-tuning, and it's also, by the way, one of my favorite applications of LLMs or small LLMs, because it really shows what we can do with smaller models, because not every application in reality requires a general chatbot. There are so many applications in this world, especially in the business world, where you just need a prediction. You don't need necessarily something that gives you a text response or that can do multiple things at the same time, like summarizing and translating text. Sometimes you just need a model that predicts something. For example, customer sentiment or I don't know, like stock market trends or something like that. So there's an infinite amount of things you can do. In this chapter, we will focus on a simple application of email spam classification, because you probably all are familiar with emails and spam emails. And traditionally spam, the problem of implementing a spam classifier was done with more traditional models. So even going back to like logistic regression or naive Bayes classifiers and so forth. If I recall correctly, even one of my first tutorials even was a naive Bayes classifier. It just comes to mind while I'm talking here, but maybe I can say probably can, I can just show you and find it if it's not too distracting here, but, um, so one of my earliest book posts I think was on naive base, um, here it's 2014, um, so traditional methods for developing a spam classifier and these are methods that are very fast. Um, so they're very popular in cases where you have a large customer base. So if you are running a classifier on your computer, I mean, you, you don't really, um, the only limitation is the hardware you have in a small LLM, like the one we developed before it is fine to run on a local computer, but if you are, for example, a big company like, you know, Google, and you have millions of customers who use your email application, you have to be also concerned with how can we reduce cost or how can we make sure that this scales to millions of users. And simple models are actually quite nice for that. And even if you are not concerned with cost savings or efficiency, simpler models like naive base or logistic regression are still great for establishing a performance baseline, because they are usually simpler to implement. And so when I develop a new application, I usually start with the simplest model, and then I look, oh, I get so and so percent accuracy. And then I try to use more complex models or more expensive models and see how much are these models improving over my baseline. So it's actually always a good idea to also start simple. But in this book, because it's a book focused on large language models, we will be actually focusing on adapting the large language model from the previous chapter, the pre trained model, and fine tune it as a classifier on a spam classification problem and spam classification, because it's easy and it's familiar, but you can also do this for any type of classification task, which is quite interesting. And these models are, you will see actually quite good for classification tasks. So yeah, I would say let's get started. So here we will be first focusing on preparing the data set. And so also maybe I should show you one more figure. So where or what we are going to do, where we are here. So this chapter itself, there will be several stages. So the first stage will be concerned with the data set preparation. And we will start with that. And later we will talk about how we set up the model and then essentially how to fine tune and evaluate the model. So one step at a time, we are now focusing on the data set preparation part here. And so for that, I will just show you the code here and I will just briefly talk about it. Let's see if this URL works. So I'm using a data set from UCI. So UCI is, I think it doesn't work like that, UCI is repository, a machine learning repository that has been around for many, many years where you can find public data sets. data sets, they're just, you know, easy off the shelf data sets. And I was trying to find a data set that is relatively small. Let me see if this works. Because I have the zip file. I have the zip file link directly. So maybe I should just search for this. Because I don't want to hear in the browser down at the zip file, I just want to show you the data set. Yeah, this one. So we are going to work with this data set, which is very simple, just a small email, sorry, not even email, like SMS, text message, labeled messages data set that consists of spam and non-spam. And our goal is to classify spam messages, oh, sorry, spam messages. And this would also apply, of course, to email and other problems. I do, in fact, have bonus material where I'm applying this to movie review data set. So in this, um, yeah, GitHub repository for this book, where I keep my bonus material, I do have, I will talk more about this later. I do have also 50,000, um, yeah, the data set of 50,000 movie reviews as another example, but I want to keep this relatively small. This data set, it only has the 7,000 examples, so it's faster to train. And I think it's sufficient to illustrate how you can adapt LLM for classification tasks. And so what you're seeing here is just boilerplate code to download the dataset. You could technically also just do that manually. So this is just to automate it. So it will download this zip file from the spam collection website here. So compared to clicking download, it's doing this in Python and unzipping the file and then adding the making sure the file path is correct. So the file path will be SMS spam collection TSV. So this is a tap-separated value file. It's like a CSV file. But instead of comma separated values, it will be tap-separated values. I will show you how this will look like. So here we have this folder. And this is a small data set. So I can just open it here. In general, I don't recommend just opening files like that, because when they are large that will or can crash your compute memory. If you have like millions of rows, but yeah, as we can see, we have ham or spam where ham means non spam. And then on the right hand side, we do have the, yeah, the text message. And that's how the dataset is structured. Um, yeah. So next, so since we have loaded this now, so next it's, yeah, uh, not a bad idea to log this in Python to also make sure it's formatted correctly. And by the way, let me also show you before I import this pandas library, let me show you the packages that I'm using in this book or in this chapter right now. So we will be using matplotlib later for visualization. The NumPy array library, of course, that underlies matplotlib and pandas. Pandas is a data frame library. It's on top of NumPy adding a data frame format. Nowadays, there's also another library called Polars, which is faster. It's a Python library that also implements the code in Rust underlying the Python implementation, which is similar to what tick token is doing, but I'm using pandas here because we just really not only need very, very basic things and it's a very simple data set. So it's going to be done in half a second. So it doesn't matter whether it's done in half a second or 0.1 second, you know, it's, it's instant anyway. me. And pandas is I think still more popular. So yeah, that's why I'm using pandas. We still need TensorFlow, because we're going to load the pre trained weights from OpenAI that we talked about in the previous chapter and that we used in previous chapters. And then yeah, pytorch for the GPT model and the fine tuning. Okay, so just for reference, the software versions I'm using. But like I mentioned before, you don't have to have exactly the same versions. It's just here as So yeah, it's a reference in case something goes wrong, you know what I used. And you know, if you want to exactly replicate things, if things break. So we are first importing pandas. It's like an array library for, you know, nicely loading the data data set. So I will use the read_csv function. So the read_csv function is usually used for loading, see for loading CSV files, like the name implies, but you can also change the behavior to load tap-separated files. So tap separated, you couldn't really see it here because it's, it looks nicely formatted. But maybe I can let me see if I can do it like this. The first five lines of data set. It's called SMS spam collection and then there. So yeah, so you can see this is just like a bash command and you can see there's like a tab instead of a comma separating this column and this column so here we see there are two columns but when we are looking at edit in text format that's only one line and the column is this tab essentially and so we're saying this tab character is separating our columns and we don't have a header so header means that we don't have like a column label. It's really just, you know, text. So the first line really starts with a data sample. And so we can give it our own column label. So we can call it label and text where label is ham or spam and text is the message we want to classify. And then I can printed here and I can see there are five thousand oops five thousand five hundred seventy two rows and two columns looks good to me and we can also count how many of those are span spam and not spam so what I can do is I can do label and then value counts and so this will be listing how many non spam and spam messages we have. And you can see this is like an imbalanced data problem. So there are many more non spam messages than spam messages. And this will make it a bit trickier here to evaluate the model and the performance because we can't just use accuracy then we I mean there are measures like balanced accuracy, precision recall, like ROC curve and so forth. And so this would be a different type of beast. So for that, maybe just, let me see if I can recommend something, but there's a library called imbalanced-learn, for example, that is specialized in working with imbalanced data. And if you are interested in, let me see, if you're interested in model evaluation and machine learning in general, I do have a paper or let me see, yeah, here on archive. That's not the right link. Let's just search for it. So this one, it's like a many page article. It's probably, yeah, 50 pages almost where I discuss more about model evaluation and the different metrics and so forth. So if you're interested in that, I highly recommend reading this in detail. For this chapter, I will keep things simple. So I don't wanna spend five hours talking about model evaluation and everything, because then otherwise this will be way too long and it won't be about just LLMs anymore. It will be a general machine learning and deep learning lecture. So what I will do is I will balance this data set, which means that we will make sure that we have the same number of non-spam and spam messages. So I will reduce the number of non spam messages to 747 so that we have a balanced data set and it will make just talking about it a bit easier. So it's making the problem a bit simpler here because I want to focus more on the LLM part than all the other evaluation parts. Evaluation is a very important topic. It's I would say just a topic for a different book or different course and so forth. So I will probably in the future one day talk about it separately. But for now, let's make this problem simpler so we can focus on the LLM fine tuning. So what I'm going to do is I'm defining this function here, create balanced data set, which will make sure that non spam and spend messages have the same number of samples. And so what I'm doing is I'm first counting how many spam messages we have. I can just show you here. So we have 747 spam messages. And then what I will do is I will sample the same number of non spam messages. So here the sample function will take a random sample of 747 because that's what we have here. And we set a random seed so that you, when you execute the code, you get exactly the same sample. So to ensure that we all work with the same subset. So when I just execute this, I should probably, because I'm improvising here, should probably assign this to a variable. So yeah, you can see we have 747 non-spam messages, ham. And then what I'm doing is I'm concatenating them into a new data set. there might be more efficient ways to do this, but it's such a small data set that we, you know, we just keep this one and this one and concatenate this into a new data set, which is then our balanced data set. So you can see after creating this balanced data set and looking at the new label counts, we can see we have 747 non spam and 747 spam messages. Now there's still One thing that I would say kind of bothers me about this, so and I go here, balanced. So one thing is that the label is text. And so to make it easier to work with, I usually recommend converting this into a label, an integer label, starting at zero. So you can think of this integer label then as token IDs. So essentially what's interesting is here, everything we are doing is we just want to classify whether something is non-spam or ham or spam. So we have two classes, ham and spam. And so before we wanted our LLM to deal with all kinds of words in the vocabulary and output all these words. Now we can make the problem a bit simpler. So instead of having these token IDs going from zero to 50,257 in the output layer, we can have in the output layer, only two token IDs zero and one, we have to be a bit careful, because our LM will still work with the same input vocabulary, because it still has to process the input text, but the output can be simpler. Now, we will get back to that later. So I don't want to say too much about this yet, we will get back to that here in this part, modifying the model for fine tuning. But here, what I'm doing is I'm essentially assigning these labels zero and one to the label column. So what I'm doing here is using the map method. And so, so first of all, we have the label column, we only focus on the label column. And map is mapping the values using a dictionary. So our dictionary, or mapping dictionary, let's call that map dictionary. So this would be this one here, where we say everything that is ham should become zero, and everything that is spam should become one, like I'm showing you here. And this will execute this automatically for the whole data set. So let's execute this. And then I can double check it. So you can see now we have the tail and head, by the way, are the first five and the last five entries can also maybe just to make sure. Let's use this one. Everything looks correct. So we can see we have now labeled zero and one instead of ham and spam. Yeah. And so that is, we are almost done with the section. That is how we first process the data set to make it, you know, make it nice for further processing. And then the next one I will introduce is splitting the data set. So this will split the data set into subsets. So we will have a training set, a validation set and a test set. So the training set is for training the model. The validation set is for tuning. So some people also call that a tuning data set where when we are training the model, we look at the validation set to kind of estimate how, how well is our model generalizing to data that it doesn't see during training, because we only look during training and on the model overfits, we'll get something like a hundred percent, but then in reality, when the model sees new messages that it hasn't seen during training, we have no idea how well it would perform. So that is why we have the validation set. So we can have an estimate during training how well the model performs. And then we can also meddle with the model. We can fine tune it. We can optimize it and use the validation set to see whether our tweaking or changing of the hyperparameters makes an improvement or not. And then once we are done with everything, we use the final test set. Cause when we use the validation set multiple times, it kind of like gets biased. I talked more about this in my article here, the model evaluation article. So we at the end want to have an unbiased or like an independent test set that we haven't used before during the training procedure. And so how we do this is we take a portion, so train and so maybe going through this sequentially. First we shuffle the data frame. So this is just one way to shuffle a data frame in pandas. So we say sample and we use fraction one. the fraction is how much of the data set and one means the whole data set. And for example, point five would mean half the data set. So what we are doing is we are sampling, it's kind of weird to write it like this. But what's happening is we are sampling from this data set, considering all the rows. And this will essentially shuffle it, because we are just drawing the rows in a random order. And then we calculate the split indices. So we say that, for example, train fraction could be 70%. So it could be, for example, 0.7. And we could say 0.3 or something like that. So that will mean 70% or let me do two here. So 70% of the data, this is the position where we have 70% of the data. So then we extract it. And then from this position considering another 20%. So this is where the validation ends. So maybe in simpler terms, if we have 100 examples, the train end would be at 70. And then the validation end would be 70 plus the 20. So this would be at point at 90 index 90. So if We have hundred 70% of our training, then 20% for validation. So the position is here 90 to, um, sorry, 70 to 90 will be used for validation. Let me write this like this. Um, one to 70 for training, 70 to 94 validation. And then the remainder, we have the test set, the remainder. This will be then 90 to 100. So the remaining 10% will be used for testing. So that's just how we subdivide the data set. And so let's do this and apply our random split function with 70% training data. Here I'm using 10% for validation. So it's basically more like this. And then 80 to 100 for testing. And why these ratios? This is again a hyperparameter, usually 70-80% is a good number for training. If you have a large data set, you know, you can also make the training set smaller. But yeah, it really depends on the application. In this case, 70% is reasonable, 10% for validation. I would say the test set is a bit more important than the validation set because that gives you the final estimate. So I usually make the test set a bit, a bit bigger than the validation set. Some people do it differently. They would make multiple validation sets, for example, and rotate them. So it really depends on how much you work on the, on the hyper parameter tuning. So the more tuning you do, usually the more validation data you want during the development to make sure you don't overuse a certain validation set. But yeah, in our case, it's a very, you know, small problem. So it should be a fine ratio and yeah, just let's double check how they look. So here, by the way, I'm also saving them as CSV files so that later we don't have to go through this whole procedure here again, if we want to come back to this and just load the data set from these files and personally, I also like to do that when I, for example, write papers and so forth on when someone wants to reproduce my results, I can share these files with them. So for example, yeah, you can share the procedure, but let's say someone makes the typo or forgets the random seats somewhere, you would get different results. So in that case, I like to always, you know, save this for record keeping. And so that I know exactly what is used for training, validation and testing. So we have 1,045 data points for training, validation DF, we have 194 for validation. And then the test, we have 300 for testing. Okay, so this is it for this part on the data set processing. And in the next section, we will talk about setting up our data loaders. And then later, of course, setting up the model itself. Yeah, after downloading and preparing the data set in the previous section, we are now ready to create the PyTorch data loaders we will be using for fine-tuning the model later. So for that, I want to show you first the figure before we implement the code, because I wouldn't say it's super tricky, but there is one important aspect to consider here. And so the text messages, they have different lengths. So for example, here are three example text messages. They are not exactly from the data set. I chose shorter ones here because in the data set, they would be too long to fit into this figure. But if we have this message, this is the first text message. And then here, this is another text message. And then this is the third text message, which is very long. You can see they have all different lengths if we convert them into token IDs. So each has a different number of token IDs. And when we create mini batches or batches for training in the data lotter, these are tensors. And tensors have to have the same number of elements in each row. For example, if we have 10 training examples, 10 rows in that batch, each of them has to have the same number of token IDs. So how do we accomplish that? How do we make the number of entries or token IDs the same for each training example? So what we can do is we can pad the sequences the longest sequence in the batch. And what we are doing here is we are using a token ID that is usually not used for many other things. For example, here the token ID 50,256, which is the end of text token, and we are appending it here. So first we have to figure out what is the longest sequence. So if we only had a data set consisting of three training examples, we would check the the lengths here and see, Oh, this is the longest training example. And so we will find out one, two, three, four, five, six, seven, eight, nine, 10, 11, 11 tokens. And so we have to make sure that every entry has 11 tokens now. So here, in this case, because we only have five tokens, it means that we have to add six padding tokens. So here we can see one, two, three, four, five, six, we are adding the 50,000 to our 56 padding token six times. And here in this case, we are adding it five times. So we make each sequence 11 tokens long to match the longest sequence here. And there are multiple ways we can do that. We can do this for each batch. So one batch might have the maximum token length of 11. One other batch might have the maximum token length of 13, another one 15, or what we can do is we can check the longest training example in the data set, and then pad all the examples to the longest training example in the data set. And that is what we are doing here in this chapter. In the next chapter in chapter seven instruction fine tuning, I will show you how we do this on a batch basis. For simplicity. Now, we are finding the longest sequence in the whole data set, um, and then pet all the other ones. Okay. So let's, um, see how this looks like in code. So, and by the way, um, yeah, we will be using the tiktoken tokenizer again. And just to show you the 50,000, 20, 256, that's the end of text token. So how we are going to implement this is we are going to use the data set class, again, the data set class from PyTorch and yeah, just copy pasted it. So it doesn't take forever when I type this. So I will walk through this more slowly. This is a bit different from the data set that we used for pre-training. So in pre-training, by the way, we didn't have that issue with different lengths because we determined the length, right? We set the context length, and then we had these sliding windows and we made sure that each window has the same size. So in that case, we never had to worry about different lengths because we created these links. Now it's a bit different because we have this data set where each has a different, different length here. Okay, so what we're doing is in this data set class, by the way, it's not completed, I will add a bit more code once I explain this part. So we have a CSV file that we want to open. This could be for example, the training validation and test set here, and then the tokenizer that would be our tiktoken tokenizer. Then we define a maximum length. In this case, this will be determined as we can either say, we want to have a specified length, let's say 10 token IDs, 100 token IDs, or whatever the model supports. Or we can just say, the longest sequence is based on what we have in the training set, like showed you. But yeah, we can restrict this further. For example, if we want to truncate it more, you know, if we, in this case, if we want to have just five or something like that. And why is that useful? If you think about an application where you work with long documents, for example, let's say you want to classify newspaper articles, you want to develop a classifier that categorizes them as sports article, politics, finance, and so forth. maybe you don't need the whole article. Maybe some articles are so long, maybe a hundred thousand tokens or something that it wouldn't be working with your LLM. So you can say, oh, I only want the first thousand tokens because it has already enough information to build a really good classifier. And then I don't need to feed my LLM 100,000 tokens. So you can artificially truncate the length here. And then we define what token we want to use for the padding. So yeah, we read the data set here, and here what we are doing is we are tokenizing the texts. So this is the familiar encode method we used before, and we do this for each row in the data set. And if we don't use the maximum length that we specify here, then we find it ourselves based on the longest example that's similar to what I've done here, where I looked at all these examples and found, oh, this is the longest one. And so based on that, we say, oh, the longest one is 11. So we have to pad everything to 11. So I will implement this method shortly. Otherwise, if we have a max length, then we truncate to the max length here. Um, yeah, and then we pad the, uh, every element or every training example, according to the max length. So we have the encoded text and then for what the difference is between the max length. So for example, if this is, let's see, we have a length of 500 and our max length is 1000, then the difference here is 500. So 1000 minus 500. And we say 500 times pad token ID. So we add 500 padding tokens to the encoded text, which has length 500. So it will have length 1000 after this, and we do it for each example in the text. So we are essentially determining the maximum length. Also, we have it here. And when then we add the number of tokens corresponding to the difference between the actual length and the max length, max length, so that we fill up the length up to the max length, it's what you are seeing here, essentially. Okay, so this is the init method. This happens when we construct the data set. So then next we have a get item method. Let me add this here. So the get item method will retrieve one example. So here it's similar to what we have done before. There's an index. And By the way, this is just like a PyTorch convention of how the data loaders work, the data sets work, and then the data loaders operate on the data sets. So index is a number, so for that we need actually, one second, we need the length method. So the index is a number between zero and the length of the data set. So if we have in our case, let's say for the training set, 1045 examples. This is the index is a number between zero and 1045. And it will select a random entry from the data set. And it will also get the corresponding label. So this is the text. And this is zero or one depending on whether the email is ham or not ham. And then we convert it into tensors. Yeah, and then the only thing I have not explained yet is how we get the longest length. So, yeah, depending on how you want to implement this, I have a verbose version and later in chapter seven, I use also a shorter version. So that's actually very simple. What we're doing is we are just brute force iterating through all the, um, encoded texts, the tokenized texts. And we say, um, if a given text is longer than the max length, then we update the max length. And a shorter way to write this would be just like that, depending on which one you prefer. If you are more familiar with Python, you might find list comprehensions or here like just a generator more readable or here in this case, the more verbose way of doing it. But it's essentially just finding the longest sequence brute force using traditional Python code. Yeah, so that is our data loader, or sorry, not our data loader, our data set, the data loader will come in a few moments. So let's first initialize our data set here. So what I'm doing is I'm initializing it with a training data set first, and then we don't set a max length, which means that the data set will determine the max length based on the longest encoded length, like we just discussed. So this will be my training data set. And then I can print out what is my max length, what is the longest sequence. So if I execute this, I will find it's 120. And it's been a long time. So I grew up in the 90s. And I remember SMS text messaging was very popular nowadays. Everyone uses iMessages or WhatsApp where you don't have any limit How many characters you write? I think I'm not sure, but I think if I remember correctly, the maximum number of characters was 120, which might explain why the longest sample in this training set is 120 in any case. So just as a side note, so we have now the longest training example of 120. So that is how we pad our training set. And then we do the same thing for the validation set and the test set. Now there could be longer examples, not in this case, maybe because based on what I said, text messages were maybe limited to 120 characters. But if you're working in a more general case, there may be examples where you find that a record in the validation set or test set is longer than something that has been in the training set. So here, what we're doing is we are truncating it to the training set, set length. That is because we'll get to that later, because we are going to fine tune a certain position, a certain token ID. I will explain other ways of doing it also later as let's say, bonus material. But for the way we implement the LLM, it is important that the validation and test sets have the same lengths as the training set. So that's why I am restricting that here. So another way you could do it technically is you could also determine the length based on the whole data set before we split it up and then use that length. And by the way, I don't think though, they are longer than 120. So I'm just doing it for best practice reasons. Because in model evaluation, the best practice is also to use the same parameters derived from the training set on the validation set and test set and not use all the data to determine settings because that would indicate we are considering information that would not be available at test time. So we are kind of like almost cheating if we would, um, look at the lengths of the validation and test set. But this is again, a topic for, you know, model evaluation. If you're interested, I mentioned my other article. So here I'm just doing it also for best practice reasons. Um, let's see if I have a figure here. to illustrate where we are. So what we have done now is we have these original text messages, and then we made them all 120 tokens long by adding these padding tokens to 50,256. So this is our data set. And then we have also the class labels, zeros and ones. So that's what we have done so far. But the one step that is missing, I mentioned in the beginning of the section, we are going to create the data loaders. So far, we only created data sets. So let's also create our data loaders. And this is code that is very similar to what you have used before in chapter two, when we set up the data loaders. So here we set the number of workers. I mentioned there might be some issues on certain machines and also in Jupyter notebooks, usually I don't recommend using multiprocessing. So I'm just setting it to zero, but you can increase this and see if it works on your computer. And then we set the batch size to eight and everything else is the same. We are shuffling for the training set. We don't need to shuffle for validation and testing. And then also the drop_last=True, which I explained in a previous video that it will drop the last batch. If the number of examples in the training set is not divisible by the batch size. And yeah, and this is it. So let's execute that and let's maybe also do the dry run. I mentioned before, I like to just iterate over the data set, just to make sure there are no bugs. You can do that also for the validation and test set, and just to make sure the sizes of the input and targets are what we expect. And so we can see each batch has a size of eight, or in this case, it will be the last one, but yeah, it should be fine. So we are just making sure the batch size is indeed eight, and the number of tokens is 120. this looks okay. And the labels, of course, corresponding to the eight examples also have size eight. So this looks all good. And then lastly, we can also just print out the length in terms of how many batches do we have in the training, validation and test set. So we can see we have 130 training batches, 90 validation batches and 38 test batches. And so with that, we have now the data set prepared and to load it. The next step would be loading the model the pre trained model before we can modify the model. And before we can find you in the model on this data set. So this is probably going to be the easiest section ever because we are just going to load the weights that we loaded before the model weights from OpenAI. And the reason why we are doing this is that we need this to fine tune the model. So we are just, in this particular coding session, we are just using the code from chapter five and a lot of the model weights. So for that to work, you have to make sure that in the folder here where we are executing the notebook, that you have the previous chapter code. So from all the previous chapters And this includes the GPT model class and everything we have here in chapter five for the weight loading. So all the code we worked before, I might have it here in a file, previous chapters dot PY on the Python file. And then here also this GPT download function we got in the previous chapter. So this is old code. This is the same code we looked in at in chapter five. We are just going to reuse that now so that we have the model set up and can fine tune it So let me show you Just the basics here. So first I'm just copy pasting it because it's not very interesting at all This is something you have seen before so we just choose the smallest model here the hundred twenty four million parameter model Just like an example input prompt. I think we don't even need that one can get rid of this. And then we have the vocabulary size, the context length, the dropout, and the QKB bias. And then we are from here, just getting the embedding dimensions, the number of transformer blocks here, and then the number of heads in the multi head attention modules. And so this is all what we used in previous chapters, nothing new. This is just getting our configuration. And then I'm going to import the download and not GPT function from the GPT download file. And again, like I mentioned, these are files. So this is a file. And this is a file that are relative to this notebook here. And so in this case, pytorch can just import from these files. So we don't have to copy and paste the code from the previous part. But you can also go in here and just find the code in chapter five and just copy and paste it into into this one here. So it doesn't matter. But just if you want to have it a bit more clean and just only see the code that is new, I prefer usually to just import it from Python functions where I keep it separate. And then in notebook, it's more like focusing on the new code here. And yeah, some more boilerplate code. So this is also from the previous chapter where we prepare this choose model to get the model size. So it's essentially just getting this number here 124 million, and then download this from the OpenAI server. So just going back here, this is just downloading it from the OpenAI server. And yeah, loading the model might take a few moments. And later, I mean, this will be our base model, like I mentioned before, in the meantime, I could maybe go back to this figure and show you what I mean. So this is also called foundation model. Some people like the term or they don't like the term, it depends. Because yeah, there was like an academic paper that invented this term. And sometimes people say, Oh, this is just, you know, just another term I have to remember and everything. And so you can also think of it as the pre-trained model or the base model. And this model that has been pre-trained, the foundation model, pre-trained model or base model, however you want to call it, we are going to then modify it. But in, in most cases, you don't need to train the model from scratch. So what I'm trying to say is we are building the classifier and you can also start with random weights and then just, you know, fine tune the model on the data set that we care about without bothering with a pre-trained model. But the pre-training is actually quite useful because the pre-training teaches the model, you know, to distinguish between different words and learn some meaning of what words are. So this is actually a quite nice foundation to build on. So it's actually usually better to use a pre-trained model and fine tune it for classification compared to Training this model from scratch on classification tasks because we could technically also use the random weights instead of the pre-trained weights But yeah, usually it's why not taking advantage of these pre-trained weights basically where the model already learned Feature extraction inside the transformer blocks and everything so we kind of take advantage of all this processing power that went into it during the pre-training. So we have our model downloaded now and then we will also import some of the convenience functions generateTextSimple and then these two just to make sure that we allotted the model correctly. So what I'm doing here is we are not going to use the model for you know writing text or anything but I just want to make sure that the model works so that there is no bug in the code and everything. So this This should produce exactly the same texts we have seen in Chapter 5. So for example, every effort moves you forward, the first step is to understand the importance of your work, just to make sure the model is loaded correctly. If it generates gibberish, it means maybe we made a typo somewhere. So it's just like a sanity check. Now you might wonder, could we actually use this model to do the classification? So for example, we could have a prompt that says, is the following text spam? Answer with yes or no, and then we provided the text. If I go to chat GPT, I'm pretty sure if I type this in there, it will do a good job and classify this correctly. So it will, for example, say this is a spam text message. And that is because it has been instruction fine-tuned on so much data that it will actually do a good job on that. However, if you don't have a lot of data, if you don't have hundreds of thousands of instruction fine-tuning examples, classification fine-tuning, if you care only about classification, is gonna be more data efficient. We are later in chapter seven, we are going to fine-tune the model on instruction data so that it theoretically can deal with cases like that. But this will be a future chapter or future problem. Um, so for example, here, if we have a pre-trained model and we give it this text, so this prompt, you will see the pre-trained model is not quite capable of dealing with that, so it will generate, um, it will actually repeat the text. Even you can say, uh, see is the following text. So this is the input, sorry, uh, this is the input. And then the second row, this is what it generates. And you can see it's kind of like, um, copy, copying text almost from the beginning here. And that is because it's just a pre-trained model and pre-trained models, um, in theory are only good for generating texts, continuing to write texts, but it's not really capable of answering instructions. So for that, we will need this technique, um, instruction fine tuning for now. We just want to keep it simpler and we just want this model to return zero or one So if model sees only this text, it should just return zero or one whether it's ham or spam The yeah, the thing is also that modern LLMs just like a side note now Some modern LLMs if you pre train them To some extent now it nowadays they can actually answer questions without this explicit step here, the instruction fine-tuning step, and that is because nowadays big companies, based on the papers they release, they do include some instruction Q&A data, something like this, in the pre-training data set so that the model already learns a bit of doing that during the pre-training. But in general, a true statement is if you just have raw text without any specific format, model will be relatively limited, it will continue generating text. But it can't really answer instructions, you can see, this one is a more natural one. It's just, you know, continuing this text from here could be from a book or something like that. If we give it something slightly different, a question, yes or no, it won't say yes or no, it will just, you know, do something unexpected here. Okay, so we determined now this model is not capable of doing spam classification. And Like I said, there are now two routes we can take. One is classification fine tuning or chapter seven instruction fine tuning. Here, we will start with the instruction fine, sorry, the classification fine tuning and leave instruction fine tuning for a future problem. So this video was really just setting up the model, the pre-trained model and we determined it's not capable. In the next section, I will show you how to make it ready for classification fine tuning. Yeah, so in the previous section, we just loaded the weights from OpenAI into our GPT model architecture. And now we are going to get this GPT model architecture ready for the classification fine tuning. So before I go to the code, just to conceptually showing you what's going on or what's going to happen. So here what we are doing is we are looking at the GPT model architecture that we coded in chapter four, and then pre-trained in chapter five and where we just loaded the pre-trained weights into this architecture. Now what we are going to do is we're going to replace this massive output layer that goes from 768 to 50,257 dimensions by a smaller output layer. So why do we have this massive output layer again in the first place? So we do have this token embedding layer that goes from 50,257 potential token IDs into an embedding space. So we carry through these embeddings, we have embeddings of size 768, where this number comes from the fact that we have 768 dimensions as embedding size in the smallest model. If you're using one of the bigger models, you might even have bigger embedding dimensions. But yeah, we are working with a small model, so our embedding dimension 768. That's where this number comes from. And so in the output layer, we have to go back into the vocabulary space. So because ultimately we want the token IDs. So we go from 768 back into the 50,257 dimensions from our tokenizer. And like I said before, we don't actually need that many words if we just want to classify something as ham or spam. So for that ham and spam, there are two words. So we need actually only two tokens, two potential output tokens. So we can go ahead and replace this massive output layer with this smaller output layer where we have, again, still the same embedding size because, you know, we don't really change anything inside the network, but then we only need two potential words, ham or spam. So we replace this by these two output nodes. And in general, we can say that the number of classes equal to the output nodes. A curious reader might note, for example, you might know from classic machine learning or deep learning that for binary classification tasks, so a classification task where we have two classes, you technically only need one output node. But to keep the code in this notebook here more general, we will be using two output nodes for our two classes so that we can can say the number of output nodes is equal to the number of classes in the data set. So for example, if you have another task, so this would work also for any type of task. If you have, for example, a task where you want to classify newspaper articles, so you might have business news, sports news, politics and finance, so you have four different types of articles, you would then have four output nodes. So you can change this based on your task. yeah, since we only have two output classes or two classes, we have two output nodes. So let's go ahead and make these model changes I just described. So first, never a bad idea to just make sure the model is loaded correctly. So we are looking at this model here. Like I mentioned, we have the 50,000 257 to 768 embedding. And then yeah, we are scrolling through the different transformer blocks. So we have Oops, we have, um, 12 transformer blocks. So up to here, that's a 12th transformer block. And then we look at the output layer and this is the layer we want to replace by our smaller layer. So before I do the actual replacement, um, I want to make one more note. And so pre-trained models are actually pretty good at, you know, extracting information from the input text. So this whole network has been trained, you know, to do something useful in the pre-training task. So we don't really need to modify anything here. We can actually just replace this layer and just fine tune this output layer. And I do have an article on that. It's pretty long time ago, but where I analyzed like the trade-offs of training different layers. So let me just see if I can find it here. Fine tuning large language models. And so it's a different architecture and it's a different data set. So let me see. So it is a different model and a different data set that we are working with, but it's, I would say a trend that generalizes in certain aspects where what I'm trying to tell you here is kind of like transferable to our model and our data set. So what we're looking at here is the classification accuracy. It's on a different data set, but it's similar. I mean, in that sense that we are classifying two classes and that's the accuracy here. And there are three bars, there's the test accuracy, the validation accuracy, and the training accuracy for keeping this discussion brief. Let's just focus on the training accuracy, the first bar. And this accuracy is achieved if we train only the last layer. So the last layer means this one here. Now, if we train the last two layers and the last transformer block. So let's say this corresponds to like these two and then the transformer block. If we train this part, we get better accuracy. So we train more things we get from, I would say 76% to 87.5%. Now, if I train even more, like the two last transform blocks. So if I train this whole part here, oh, sorry, what happened? One moment, I somehow pressed some wrong button on my keyboard. Okay, so if I, up to here, if I train this whole part here, we see we get even better performance. Now we have something maybe around, I don't know, like 90%. And yeah, so the more we train, it seems like the better the accuracy, but then we hit a point where we have diminishing returns. So you can see here, there's not much improvement really. I mean, we still keep it around 90% here. And moreover, also, when we look at the training time, how long it takes for the fine tuning here, we are around, I would say, what is it like 2.5 minutes or something, if we only train the last layer, if we train the last layer and the last transform block, it's about the same training to transformer blocks. Yeah, it's a bit slower, 3.5 minutes. But then if we train the whole model here, all the layers, yeah, we see it's seven minutes. It's almost like two to three times what it takes here to train only the last layer, or the last two layers. But you know, it's not that much better. So we spend, especially in this range, more time training, even though we don't really get any returns. And so this is generally true, the simpler the dataset, the more this applies essentially, because for a small simple data set, it's kind of overkill to train everything. In fact, the more we train typically the more data we need to avoid overfitting. And so if we have a small, simple problem like the spam classification problem, it's not really necessary to train this whole model. So we can get away with only, for example, training, um, the last layer, the, um, layer norm here, maybe, and the last transformer block. So long story short, before I go ahead and replace this layer with the smaller layer that I mentioned here, let's actually go ahead and make the model non trainable. So in order to do that, we can freeze the weights. So what we can do is, for example, we can iterate through the model parameters. parameters. So for each parameter, so that would be a weight tensor or bias tensor. In the model, we set the requires gradient argument to false, which then means this weight tensor or bias tensor won't be trained when we are training the model. So we are freezing the model. So this means essentially another word for that is freezing the model or the model parameters, so they won't be updated during the training. And now we can go ahead and replace the output head. So what we go here is output head. And so we replace this. So this one will be trainable because we initialize it with new random weights. And by default, they are trainable. So we are going to use 768. So to make this a bit more general, let me go ahead here and copy this name. So we can add the embedding dimension as a configuration here. So it will also work in case you choose to fine tune a larger model, submit them. And then let's define the number of classes to two. And then we can say in classes, and then also let's set a random seed because We are initializing it here with random weights. Okay, so now we should have a new output layer. So you can see now the output layer is improved. Sorry, not improved, replaced. And now, yeah, this would be what I just showed you. One thing is, according to this, training the last transformer block is not a bad idea. Maybe the two last transformer blocks, that's maybe a reader exercise for you. But I would say we should probably train this last transformer block at least. So what we are going to do is, oops, I don't need to scroll that much. So what we are going to do is this is already unfrozen. We are also going to unfreeze this one and this one. So unfreezing means making it trainable basically. So first we made everything untrainable, only this is trainable. Now we make these things also trainable. And why this last layer norm? I mean, well, you don't have to, But I feel like it's natural or more natural if when we make this trainable and we make this trainable to also make things in between trainable. Otherwise it would be a bit awkward. So what we are doing now is we are making this transformer block here trainable by setting this again to true. So this would be the last transformer block in our model. And then the final layer norm. So this layer norm, we will also make that trainable. And so now, yeah, only the output had the final layer norm and the final transformer block will be trainable. Before I end this session, I wanted to show you one more thing. And so what happens when we give it some input now, now after our modification? So let's just assume we have some simple input here. And so this input is four input tokens corresponding to, let's say, a text message, someone asking us, do you have time? Here again, I just add the batch dimension like I usually did. So because our model expects input batches, because without that, this would be a torch size of just four, but our model expects batches during training and everything. And so let's say our model receives this input. So when we do torch.no_grad the context, context. And then let's say, oops, outputs, model inputs. So if we get that, so let's see how outputs looks like. So you can see it's now, again, still four rows, right? Each row corresponds to each token ID or token input here. But now we only have two output dimensions before in chapter five, we looked at this massive outputs tensor, because before we had 50,256 output dimensions. Now, as expected, we only have these two output dimensions. In particular, we are also, by the way, only interested in fine tuning the last position. So here, what we are looking at is the last token. We will talk more about this, but so technically, we are only considering this last token here. So this one, we are going to not fine tune anything related to those tokens. And the reason is that the last token contains the information about all the other tokens. So yeah, so here's just like a figure showing you what we just did. If we have some input text, we get this tensor, and then we only focus on the last row corresponding to the last token. And the reason is, uh, is it's due to the attention mechanism. So if you remember, it's been a long time, but from chapter three, how attention works, we have this causal mask. So if you consider these roles as the input, um, so when you look at the first input position, do the self-attention is only between do and do itself. And then if we have these two, um, do you, so the, you, the word, you, the context vector for that one contains information about do and you. So we have attention weights for do and you, but so it can't look into the future because of this causal attention mask. So it can only always look to its current position. Right. And so if we consider this token here and we make our decision, so maybe going one step back. Okay. So our task is based on these numbers to classify whether something is spam or not spam. So we could look at this whole thing. We could look only at the first row or the second row. But the most efficient way to do that is to look at the last row and use these numbers to determine whether the input is spam or ham, because this one contains information about everything else. So going here again, the last token, do you have time? It has information about all the other tokens. So the context vector has all the information because we have the attention weights for that. And so the model, when we fine tune it, will learn how to use these attention weights effectively for making the decision whether something is spam or ham. And so that is why we only need to focus on the last position here. But yeah, this is what we are going to do. So we are going to next fine tune the model to do this classification task. And I think this will maybe then also make things a bit more clear. And yeah, and so with that, we have modified the architecture and we will just in a few moments fine tune it. So at the end of the previous section, I mentioned that we are now finally ready for the classification fine tuning. However, there is one more thing we have to take care of, and that is the calculation of the classification loss and accuracy. And so we will be using the accuracy to track the progress during training, and the classification loss is used similar to what we have done in chapter five for optimizing the model weights. So yeah, in the grand scheme of things where we are, we prepared the data set, we initialized the model, we loaded the pre-trained weights, and then we modified in the previous section, the model for classification fine tuning. Now this one more thing entails that we implement the evaluation utilities before we can move on to the fine tuning. So we had to just conceptually explain what is going to happen. So here's a figure that is maybe somewhat familiar to you because here there are similar elements to a figure that you have seen in chapter five, except that this is a bit smaller and simpler. So in chapter five, we explained how we go from the model input to the logits to the soft max probabilities, and then to the token IDs. And here what we're looking at is the similar flow. So we have, for example, you want the lottery as text input, which goes into the LLM. And then what we're looking at here is the logits vector of the last row of the logits. So going back to the previous section, we had, for example, four tokens as input. This could be a text similar to you won the lottery. And then we are looking at the output logits for each of the four token IDs. And what we said is we are going to focus on the last row. So we are going to focus on this row here. And so this is what you are seeing here. So these are the logits corresponding to the last row in the logits tensor. And then We could apply the softmax function to convert this into probabilities. And then in chapter five, what we did is we looked at the position where we have the highest probability and that was our predicted token ID. Now this is exactly the same concept, except that instead of 50,000, 257 dimensions, we only have two dimensions. One is not spam and one is spam or better shown here at the bottom. So index position zero is not spam, index position one is spam. In this case, the highest probability is at the first position. So the model would predict zero, not spam for this particular input. As you might guess, this is not correct, right? I mean, I'm guessing here, but I would say if you receive a text message that says you won the lottery, it's probably going to be spam. And so the model thinks with 99% probability that this is not spam. That's because the model is not trained yet. We want to train it so that this probability becomes higher that this is spam, and then the model outputs a one and vice versa, vice versa. For the second input, do you have time? I would say this is probably not spam. It's maybe a message by a friend asking us if we have time. And then here in this case, the highest probability is at spam. And so the model would output spam. And also again, our goal is to, you know, shift the prediction more to what's not spam, assuming that this is really not spam. Yeah, and so we are going to implement this now in code where we essentially have the same workflow or similar workflow that we had in the previous chapter. And then when we have these predictions, these labels, we can compute the accuracy. So the accuracy is a measure that tells us how often the model is correct. So for example, if we have 100, let's say 100 text messages and the model makes 50 correct predictions. That's a 50% accuracy. It's basically random guessing. We want the model to get a accuracy that is close to 100. That means that it gets almost all the 100 out of 100 messages classified correctly. And so let's first, before we code the accuracy, let's first code what we are seeing here in this figure, this conversion from logits to probabilities to the labels. So, um, let's see, I've worked with the last row here, like I mentioned before, and that was for the reason of the causal attention mask, as I said before, so let's go ahead and just call that our logits. So in reality, these are the last row logits, right? So it's not the whole logits. It's just the last row here that we get with the minus one here. And, um, then let's compute the probabilities. So we do torch.softmax and logits as input and minus one because we have a 2D tensor. And then let's just look at this. And so we can see this is a very small number, something close to zero, where this is 99% essentially 0.999. So this means basically shifting the dot by one position. So we have 0.99, a very high probability. So if we then use our torch arc max on pro bus, you should see a one because this is a much higher value than this one, for example. We can call that our label, for instance, like this. And so this is our predicted label. If we want this as a regular Python integer, we can also use the item method, which will just extract this as an integer. There's one more interesting thing maybe to mention. So we are executing this here on the probes. But as I mentioned before in chapter five, so when we look at the logits and the softmax probabilities, you might notice that the softmax is a strictly increasing function. So that means when we apply a softmax function to any vector, the ordering of the magnitudes will be the same. what I'm trying to say here is if the largest value is at the first position, before the softmax, it will also be at the first position after the softmax. So the it preserves the position of the largest element essentially that we're looking for, it's preserving the whole order of the values, if you would sort them, the sort order would be the same before and after applying the softmax, which means that so the softmax becomes redundant here. So we can actually just do logits here and you will see there will be the same label. So in the sense we, we skip one relatively expensive, I mean, not that expensive, but we, we skip a computation here, which is nice. So we don't really have to apply, apply this function here for training. It is necessary. But as you remember, we also pass the logits directly into the cross entropy loss, where the cross entry cross entropy loss is internally using softmax essentially. So yeah, long story short, when we just care about the label, we don't need the softmax because the relative ordering of the elements is the same. So we can just apply the argmax to the logits and save one computation. So now let's get to implementing the accuracy loader so that we can see how often our model is correct before we start training it. And then of course, we want to see that it improves during the training. So let's call that calc accuracy lauder. And so this will be analogous to the calc loss lauder we implemented in chapter five, except now instead of computing the loss, we compute the accuracy. By the way, we will also implement the calc loss loader again later. But let's for simplicity, just focus on the accuracy loader for now. So we will be able to pass a data loader that could be the training validation or test data set, or order, then the model itself, I like to also pass the device here, so that we can put the data onto the correct device where the model is there are different ways to implement this, but you know, I prefer to do it this way. The number of batches. So similar to the calc loss loader, sometimes we don't need to calculate the loss over the whole data set. So for example, if we just need a quick estimate of the validation set, and our validation set is very large, it would be pretty expensive to iterate over the whole validation set. So sometimes it's sufficient to only take two batches or three batches to estimate the accuracy. So with that, we can control how many batches we want. So it's not a bad idea to put the model into evaluation mode, we might have done this at some earlier point, but just doing it here explicitly to make sure things like dropout and so on are disabled, all the random things in the model. And then we can keep track of the predictions. So the correct predictions, I'm just seeing I made a typo here, the correct predictions and the number of predictions in total that will be used later to calculate the accuracy. And then we can have just like a little sanity check. So if the number of batches is none, then let's just use the whole data set. So here we will do the length of the data loader. So that means use all the data points in the data set. Yeah, so far so good, and let's say otherwise we take the num batches, but let's make a small modification. So we say num batches the minimum value between this and also the length in the data loader. So this will prevent issues where, for example, our data set only has two batches, but we specify 10 and then it would crash or something like that. So here we say whatever number is smaller. So if we define 10, but our data loader only has two, then we only use two. It's just like a little safeguard here. And then we say for i, let's say input batch, target batch in the data loader, enumerate data loader. So here for each of them, We will say if the number is smaller than num batches, we keep working on this, right? So if I is number, sorry, smaller than the batches, then we will first transfer the data onto the target device. So to device and target batch also to device. So yeah, right now I'm just double checking. I just want to make sure one, two, three, four, everything looks correct. Yeah, I usually don't code in a Jupyter notebook. I usually use Visual Studio Code with torch.no_grad. Let's maybe make a, yeah, compute the logits. So here logits equals to model and then the input batch input batch where we have it here. And like I said before, we will only use the last row. Um, and then we convert this to predicted labels and this will be using our computation here. Um, since we have similar, I mean, I think it doesn't hurt just to add the minus one here, similar to what we have done here. I think it's not strictly necessary, but not a bad idea. I forgot a T here. What else? So now we have the predicted labels. And so we can increment the number of, let's say, call this examples, the number of examples we have encountered so far num examples plus equals and then the number of rows here the shape zero should contain the number of examples so this is just could also get that from the input batch it's the same it's the number of rows or number of elements in that case what else so now of course the important part would be the correct predictions. So here, simplest way how we can say how if we have some correct predictions is just to compare it to the target batch. So for example, if I have a torch tensor, let's say labels 123, or we have let's say just 0101. And this is our, let's say correct, let's call it target. And then we have predictions. Let's take this one and change one number here. And so if I compare those two, I can use the equal sign and it will give me a Boolean array where true means it's similar and false means it's not similar. So maybe just lining them up here, then it's a bit more clear. So we are lining them up here. And so what I can do is I can then use the sum to calculate how many of them are correct. So in this case, this should give me a four because I have four correct predictions and I can return this as an item, for example. And what I can do is I can say target.shape zero. This should give me one, two, three, four, five, divided by, oh, let's do it like this, sorry, four divided by five. And this gives me essentially the accuracy, if our data set only consists of five examples. So here, this gives me 0.8 or 80% prediction accuracy, four or five are calculated or predicted correctly. And so we can use that to compute the accuracy here, we can say the number of correct predictions oops is equal oops so sorry the accuracy is equal to the number of correct predictions similar to what we have here divided by the total number which we kept track of here so this is the number of correct predictions divided by the number of examples or total predictions I'm just double checking if everything looks correct I would say this needs to be indented because we do this for each case where this is true if this is not true then we want to of course exit so I think this should work I'm pretty sure I made a typo but we will see in a few moments so let me also define because we're going to need it here the device so the device I usually like to do torch device and then cuda if torch dot cuda oops is available and otherwise we will use the CPU and yeah like I mentioned in chapter 5 you might also want to use just the MPS device but again numbers might slightly differ if you use the MPS device. And then let's put the model to the device and let's just use a random seed. It should not be necessary because we are completely or if we are completely iterating over the data set we don't need a random seed. But if we want to for example only do it for ten batches, how many by the way do we have, let me double check. We had somewhere checked the length of the data set. It's been a while here. So if we only want to take 10 batches, just to, you know, accelerate this a bit, the if we have shuffling in the training set, it will of course give us a different 10 batches. So in that case, we are setting a random seed, so that you should hopefully get the same accuracy that I'm getting. So let's say train accuracy. And And then we initialize our CalculateAccuracyLadder on the training set. TrainLadder, the model, device, and then let's say num batches, let's say 10. So in this case, this hopefully gives us the training accuracy. So let's see if this works or whether I made a typo here somewhere. Yes, so there is something that is not working. correct prediction. So I think this should be correct predictions. And this is it seems to be working. So let's see what the calculation gives us. Okay, so I forgot one more thing. I should have of course, applied the some item here. looks better. Oh, I hope it, it works. Okay. So 0.46. So this is our accuracy. So this is essentially 46.25%. And we can do the same thing also for the validation accuracy. Well, and then the test accuracy. So 45% and then the test accuracy 48%. So they should be all very close together. However, note that we are estimating the accuracy of the training set by only sampling 10 out of the 130 batches. So if If you want to get the exact training accuracy, you can do, yeah, none batches, none. But yeah, this is just like a rough estimate to estimate how accurate the model is. And you can see it's roughly, you know, 50%. It's kind of like guessing here because it has not been trained. So it's just guessing whether income, sorry, input example is zero or one. And if you are just guessing and you have a 50% chance being correct or incorrect, it should roughly be 50%. Yeah, and so this is what we have here for the accuracy loader. Now let's take care of the loss loader because that is also what we need to optimize the model. So for that, let me go back to chapter five. So we had the def calc loss loader somewhere here, loss batch and loss loader. Let me just, let me copy just this one for now. So here we have the loss loader and notice we make one modification. Of course, we do the last row only instead of everything. And then I can actually get rid of the flatten. We don't need to flatten anything anymore. And this should be my loss loader. And then we can use also the same, Sorry, the last batch, you can use the same loss loader. Notice we can't really import this technically from the previous chapter because this one is now different, right? So this, we have changed this slightly. So if you just import this from the previous chapters file, you might get unexpected results because your calc loss loader would be using the calc loss batch from that file, which is slightly different. Maybe it's not even in here. Okay. So it's also maybe better that it's not in here, that I haven't copied it in here. But yeah, so I recommend just defining it separately here and here for this particular chapter. Yeah. Okay. And so this hopefully works. And then we can use it similar to what we have done before. So we can calculate the training set loss like this. And then I can also do the same while it's calculating for the validation and test it. And these are our initial values. So what we want is we want to reduce this to zero and we want to increase this number so it becomes one so 100% accuracy. So yeah, so this is so far what we have set up in terms of how we evaluate our model. And then in the next video, like I promised earlier, we will be finally fine tuning this model. So, we are finally ready to fine-tune the model. Here the headline says, fine-tuning the model on supervised data. Supervised data is just, you know, jargon, deep learning, machine learning jargon, where supervised means that we have a labeled data set. In our case, it's the spam classification data set. So essentially what we are doing in this section is we are fine-tuning the model on the spam classification data set. So yeah, in the grand scheme of things, we took care of these seven steps already. So the remaining steps in this chapter are fine tuning the model, evaluating the model, and then using the model on new data. So if I go to the fine tuning section, there should be a figure here. And this figure is almost identical to the figure we looked at in chapter five when we set up the training loop. So what we are doing is we are still iterating over epochs where one epoch now is one pass over the spam classification data set. Then for each batch in each epoch, we reset the loss gradients from the previous iteration. We calculate the loss on the current batch. So that would be the cross entropy loss. It's still the same cross entropy loss. Then we calculate loss dot backwards. this will calculate the loss gradients that we need for updating the model, and then the optimizer dot step method. So the step method of the optimizer will apply these loss gradients to update the weights. And then we then we train, sorry, then we print the training and validation set losses to make sure the model makes progress. And yeah, I'm just seeing that's funny. So I'm just seeing this is exactly the same figure we had in chapter five. So when I submitted my draft figures for the Manning layout team, maybe the figures looked so similar that the layout team used the same figure also in chapter six, but there should be a subtle difference. So here in step eight, it should say calculate the training accuracy and validation accuracy, because yeah, our model now it's not a text generating model, so our model won't write text. it will only return class labels 0 or 1. So it would make sense during the training to calculate the accuracies to see how good the model is at the current epoch, and also track it over the epochs to see if the model is making progress or not. So yeah, there's a there should be a subtle change here where it says compute training and validation set accuracy. So maybe my figures look too similar, and they just copy and pasted it. Anyways, I will let them know. And hopefully they can update this. Yeah. So what we are going to do now is to implement the training function. And the training function is actually almost identical to the one we used in chapter five. So I'm just copying and pasting it here. And then I will talk through it. Yeah, like I said, it's very similar to train model simple in chapter five. Now it's a classifier that we are going to train. And I do have something also here, like to more lists where we are tracking the training and validation accuracies in addition to the losses. So we can also plot them. Um, what else is new? I mean, everything here is exactly the same. So, um, I, if anything is new here, I would say, um, check out chapter five. So then, yeah, if otherwise, if there's no question, you know, you can ignore this for now, because it's exactly the same as we discussed already in great detail, there's only a little change that I annotated here, here, we are tracking the number of training examples, instead of the tokens, because here, we need the number of examples to calculate the accuracy, essentially. So we will get not to calculate the accuracy, but when we are plotting things in chapter five, we looked at how many tokens we seen how we can say how many examples we have seen, because yet the accuracy calculation happens inside those. So for that, this will already do the tracking internally, like we have defined here. So here we will already also track how many examples we have in the lotter. This is really just later for the plotting. Okay, and then like I said, this is just printing the training and validation loss so that we can and see whether we are making progress during training. And we do the same thing for the training and validation accuracy. But we only do that for each epoch. So here, this is every so and so many steps where this is something we can specify, for example, every 10, 20, 50 steps. But we only calculate the training and validation accuracies after each epoch, because that is relatively expensive. And we have an evaluator so that we can say only for two batches or four batches to keep it a bit cheaper, to make the training loop a bit faster. Because like I said, if we have a large training set, this might take a long time. And ultimately, it's not that important to have a super accurate training accuracy calculation during the training. It's here just for a rough estimate. Later on, once the training has finished, we can compute this on the whole training set. But right now, it's sometimes useful just to keep this relatively cheap, maybe do this only for one, two, three, four, five batches or something like that, instead of all the, what do we have, 130 batches, because that would be slowing down the training quite a lot. Yeah, so this is our training function. There's one more thing we have not defined. This is the evaluate model method or function. And this function is the same function in terms of the code that we used in chapter five. We just have to redefine it now because the calc loss slaughter has changed. So this one is also the same as in chapter five, but we changed the calc loss batch here. So we only use the last token and this one is used somewhere in here, right? And so, and then because this has changed based on this, we also have to redefine this one because if we imported from the previous chapter, it will use the old calculus order, which uses the old calculus batch. So I just recommend for this chapter to redefine this one, this one, and this one, but the code here, you can see it's exactly the same. Okay, so let's execute that. And then we can finally look at the code for executing the training. So what we are looking at here is so I'm importing the time module in Python. So I'm just using that as a rough estimate of how long the training takes. I just wanted to know how long does it take, you know, instead of using my stopwatch, using Python to calculate how long it takes to execute this training. And so how it works is I'm getting the start time. When I started the training, I'm getting the end time. So here's the training function, and then I'm computing what is the end time after the training has finished. And then I compute the difference between the end time and the start time. And so this is in seconds. And then I'm dividing by 60 to get the time in minutes. And I'm just printing this just, you know, for my own curiosity. And so I got about five minutes when I was executing this. And this is on an old computer, Mac Mini. Sorry, a MacBook Air from 2020. And yeah, so it might be a bit slower when I'm executing it now, running the recording, the screen recording, and I'm also running the camera here for the camera recording all on that small little laptop, which might get pretty hot when I'm running this, but I think it should be somewhere between five and 10 minutes, we will see in a few moments. I'm setting the random seed here. So because for the shuffling of the training set and so forth, we have evaluation frequency 50. That means every 50 steps, we will see the loss and then we will calculate the accuracy of the validation set and training set for five batches. Like I mentioned before, to keep it relatively cheap, we will only use five batches. And we will train for five epochs. And the optimizer is exactly the same that we used before. I think I updated the learning rate a bit. So how I determined this, I was running this multiple times, and I was just observing based on the validation set accuracy, what is a better setting. So yeah, hyper parameter tuning by observing the validation set accuracy. And this way to decay is to reduce overfitting. So let's execute this. And I'm just thinking what I could talk about in the meantime. So I do have some bonus material I can maybe show to you. But let's maybe for a few seconds, stay with this execution, I just want to make sure that the model is training correctly. So how do I make sure so I will look at the initial loss, and then I will wait for the 50 steps essentially, to see what the training and validation losses are. And I want to see that this is going down. And if it's going down, then it's looking good, then we will keep training this in the background. And I will maybe go over some of the bonus material. So let's give it a few seconds here. I think it should not be too long, just looking at my device stats. So the computer is getting a bit warm, but it is in moderation here. So it should not be too bad, I hope. I mean, I'm here we go. So we have point two, and then point six, two points, sorry, two point one and point six and two point three point six. So we can see the training set loss is improving the validation set loss is improving. So our settings are good here. Everything seems to be on the right track. So let's keep training and I will talk maybe through some of the bonus material. So if I go to my GitHub repository here, so I have a bunch of bonus material. By the way, the bonus material is stuff that didn't quite fit into the chapters itself, because the book can't be infinitely long. There can only be so many pages in each chapter. There are some guidelines by the publisher too. So there are some interesting topics which are interesting, but not essential. And some readers also honestly might find them distracting. So I just had some additional bonus material that I'm that I'm sharing here. And there were also some questions by readers where I felt like, okay, instead of just writing this answer in an email, let me put together a notebook upload this so that I can reference it in the future. And so for chapter six, I have three sets of bonus material here. So first, additional experiments on fine tuning with fine tuning different layers and using larger models. So here, this is quite a lot. So I ran one 19 additional experiments. So using the same code you are seeing here in this notebook, I put this code in to this file. And then I made modifications. So it's, I mean, I would say it's a lot here, because I'm having 19 different experiments in this file. But I made it so that you don't have to really unless you want to, you don't really have to read the code, you can execute this from the command line. So for each of the 19 experiments here, I'm showing you how you run the experiment. So, for example, this would be exactly the same code that we are running in the notebook. And then, for example, we might want to change the position of the trainable token. So here, trainable token, the first one, so what that means if I go, it's already, no, it's not epoch two. So let me, let me go here. So here we have the logits and then we say we go to the last token. And the reason why we are doing this is because of the causal attention mask. Like I explained here that the last token has the information about all the other tokens. The first token only has information about itself. And so what you are looking at here is a control experiment where I am, you know, only training the first token. So what I'm essentially doing here is I am replacing this by a zero where I take this first row and what we would expect is pretty bad results because like I mentioned before, the first token only contains information by itself. And so when you run this row two, we can go to row two and see what we get. We get still 78% on the training set and 75% on the test set. So this means that, so it is still actually classifying certain cases correctly. Why is that? It might be that in certain cases, the spam is like very obvious based on the first talk, maybe if it's all capital letters or if it's a dollar sign or something like that. So there is apparently enough information to make a classification that is better than a random prediction. So we get 75% for balanced data set with 50, 50% non spam and spam, we would get or expect a 50% accuracy. So it's actually quite interesting. So the first token contains quite a lot of information, but compared to the baseline here, the baseline is 95%. When we train the last token, of course it's worse. So I'm just explaining. So this is a control experiment. I would not recommend it, but yeah, this is one thing to do. Then, um, trainable layer, last layer. So this would be training only the last layer instead of also training the last, um, uh, transformer block. So we can see when we only trained the last layer instead of, um, the last transformer block here, this trains the last transformer block. This only trains the last layer. We get only 70% accuracy. Row four so I can also just read it from here here. We are training the last two blocks So remember I showed you this here this figure where we looked at Training only the last layer and here the last two transform blocks in our code We only trained the last transform block. Sorry and last two transform blocks in our case we are training only the last block what happens in our case if we train the last two blocks and What we can see, the accuracy goes up slightly. So from 95 to 98%, but also the time increases. By the way, here, I think I was using a different machine, not the MacBook Air I'm running this on right now. I think I was turning this on a GPU, a 100. So that's why it's faster. On the laptop, it takes about five to 10 minutes. Yeah, so there are all bunch of experiments. Here's an experiment where I'm running it on all. So I'm updating all the parameters instead of only the last two blocks. And you can see there are diminishing returns. So here we have 98%. Here we have 96%. I suspect this is because of overfitting because you can see here the accuracy, the training accuracy goes up, validation accuracy goes down compared to here. So someone might expect or I would expect this is probably due to more overfitting. So we are training too much because the data set is too simple here. Um, yeah. And you can go through all the other ones. There are a lot of experiments. So there's also a technique called Laura, which stands for low rank adaptation. Let me by the way, see how far we are in the training for, so we're almost done. So yeah, I encourage you maybe to go through this on your own time then. there's also Laura in Appendix E, which is a technique for parameter efficient fine tuning. So here also really nice accuracy. Yeah, lots, lots of experiments. One, I think important thing I should say is, we will get back to that later. When we prepared the training set, we, so that's what this longest train stands for, we had 120 characters and we were padding everything to the same length. So that means position 120 is important because that's the token that's trained. And if we have new examples, let's say we get new text messages, even though it has nothing to do with the training anymore, we still have to pad them to 120 characters because we only trained this particular position, token position essentially. And so if you don't want that, if you want to have a variable length, you could technically set the batch size to one. And if we set the batch size to one, we don't need any padding. So because in the training set, we did the padding to fill up a batch with multiple examples, and then a tensor needs to have the same roll length for each roll. So here, yeah. So here, if you set a batch size without padding, essentially, you can say patch size, batch size one, and then you won't have padding. You can also here do this with a batch size of eight without padding, and how I did this is I was sim, so maybe too technical, but I was simulating a batch size with a technique called gradient accumulation. So let me see, I hope I, so I have not prepared anything here, so I'm doing this very spontaneously, but I do think I have gradient accumulation. I do have an article on that. So if you're interested how gradient accumulation works, you maybe want to read through this. So it's simulating the batch size by iterating multiple times here, by iterating multiple times, but then not using the step and the zero grad. So some people might have wondered why is this zero grad necessary when we train a model in PyTorch? So why does PyTorch not take care of this automatically for us? Why do we as users have to worry about it? And the reason is that it allows more flexibility so that we can experiment with different things as researchers. And here, if we want to accumulate the loss over multiple batches, we can use the zero grad in the step only every so many epochs. For example, if I want a batch size of eight, but a batch size of eight doesn't fit into my computer, it exceeds the RAM or the GPU RAM, I maybe, I want still batch size eight because it's more stable loss, but I might have to do a workaround where I have an actual batch size of one. I treat the model with a batch size of one, but then I do only the APOC update, sorry, the batch update every eight examples. And in that case, I simulate the batch size of eight by not updating after each step. So if that is confusing, sorry, it's also not the topic of this video. I maybe encourage you to check out this article. It will probably be more clear if you sit down and read through this article. I can maybe in the future also make a separate video on this sometime. It's just like a simple trick. Long story short, and this is what I was doing in here. So with that, I have still the same batch size that I had before up here, but now I don't have any padding. And you can see actually it's good for the accuracy. So the reason why we are doing the padding is just to keep things simple. In the next chapter and chapter seven, we will take also a slightly different approach. Yeah, and so there are more experiments, if you are curious. And here are the commands to run these experiments. And then here is also like a write-up where I explain in words what each of the rows is doing and what the baseline is. So the first one is the baseline, row one, the default, and then versus row 17, I'm explaining what I did there and what it means and so forth. So if you are curious about certain things, why we are doing the things we are doing in this chapter, this bonus material might answer some of your questions because yeah, these are additional ways how you can train it also with larger models, what is the accuracy here and so forth. So this is one set of bonus material. And then I do also have this bonus material where I'm applying this to a bigger data set. So this is the IMDB movie review data set where there are 50,000 movie reviews, positive and negative movie reviews. And the task is for the model to predict whether the reviewer liked the movie or not based on the text. Yeah, and so I'm running the model from this chapter only for one epoch because it's a larger data set. It's more expensive. And then I'm comparing it to a model called BERT. So BERT is, so if I go to the attention, it's all you need paper. So GPT models, I think I mentioned this before, GPT models, all the LLMs that you're using like in ChatGPT, Anthropic Cloud, Google Gemini, DeepSeq, they're all decoder modules. So the decoder is what you're seeing here on the right-hand side. So this is the whole transformer architecture and the transformer architecture consists of an encoder and a decoder. And the encoder, so this was originally developed for language translation. And so here the encoder was encoding the input sentence, creating a representation and then passing it to the decoder. And the decoder is generating the output text. It turns out that the encoder is not necessary because as you might know, the chat GPT models are really good at text translation. So it's not necessary to have this encoder. But long story short, the BERT model on the other hand, so chat GPT, the GPT model we implemented, DeepSeq, Google Gemini and so forth, they are this decoder part. But BERT models are models that are just the encoder part. So BERT models can't really generate text. they are more meant for generating a representation of the text that we can work with in terms of classification and so forth. And so the BERT model I am training here is an encoder. And encoders are traditionally slightly better at text classification. So I'm comparing this GPT model to this BERT model here. And you can see in this case, it's not necessarily true. So this BERT model is actually worse than this GPT model. Yeah. The stillbird is a smaller version of that. This is actually quite good. So, um, for some reason, the smaller distilled bird model is better than a big bird model. And so here you can see it's almost the same accuracy. Uh, there's something called Roberta, which is another flavor of an encoder and this gets 92.95%. There's actually, um, a few weeks ago, there was a new model called modern bird, which I think is also really great. It's just like a modern version of the encoder. But yeah, I'm saying that because if you care about classification, you might want to check out encoder models. Here in this chapter, the reason why we are training the GPT model is, first, it works quite well, right? So we see it is actually working quite well. And secondly, it's a good introduction to fine tuning before we do the instruction fine tuning in the next chapter. Okay, so this is one more bonus material you might find interesting. And the third one here is a simple user interface. If you are interested, taking the model that we have just trained, putting a user interface around it so you can interact with it, similar to ChatGPD you can, so you can write this text here and it will say spam or not spam. So it's just like a nicer interface than our Jupyter Notebook. So if you are curious, it's just very simple code here based on Chainlet. So just very simple. Okay, I think I talked for a long time. I see our model finished training. It just took 11 minutes on this computer. It used to be faster. I think it's all that recording stuff I'm running here, but we can see that the training loss went down. It almost went down to zero, which is really great. The validation loss too. So they're both in the same ballpark, which is actually really great. So this indicates that there is minimal overfitting and the training and validation accuracies are also relatively similar indicating there's only very minimal overfitting in this case here, based on that, we might say there's even no overfitting because validation loss is smaller than the training loss. If there was overfitting, usually we would see the training loss being better lower or better than the validation loss. This could also be though because we estimate based on the number of batches and that is in our case a very small number, it's just five. So one question now that I'm thinking about this could also be that you might ask, why do we actually not optimize the training accuracy directly? So why do we minimize the loss? Why don't we just maximize the training accuracy with a loss function? And that is the simple answer is because accuracy is not differentiable. So that would require talking a bit about calculus and so forth, how we calculate gradients. But since accuracy is not a smooth function, we can't really calculate the gradients of the accuracy. And that is why we don't, you know, directly optimize or maximize the accuracy. Um, yep. So we have now our results. So one more thing before ending this probably very long video is to calculate, sorry to plot the results so that we can also visually see them. So here I have a general function to plot the values, nothing really interesting. It's just based on Matplotlib, similar to what we have done before. And then let me first plot the losses so we can see the loss goes down. So I'm just looking at things we were looking at here, but it's always a bit simpler to look at it in the plot. So we can see things much more easily because here we would have to track the numbers and look at them. And here we can see all the loss goes down, which is great. And they stay approximately the same, which is also great. So there's minimal overfitting the model, yeah, learns to generalize pretty well. And then let's also look at the accuracies. So here we can see they start off a bit shifted. This again is only an estimate based on five batches. So when we calculate the accuracy over the whole training order and validation order might look a bit different. But yeah, we can see it's relatively similar. So minimal overfitting, some overfitting maybe here, but not too bad. So if we then lastly compute, this might take a while. So let me just start running it. And then I'll talk through this. So here we are now computing the accuracy over the whole training set, the whole validation set and the whole test set. So what it will do is it will use the default parameters for are we here for the num batches, and then we can get an idea what is the actual training accuracy? What is the actual validation accuracy and what is most importantly, the test accuracy. So it might take a while. So yeah, and then once we have finished that, there is only one more section left in the next video, where I will tell you how to use this on new data. But I will do it separately. Let's just stay patient. Maybe in the meantime, coming back to this chain that is just like one way of building a nice user interface. You can run it locally or also remotely and then have a nicer interface. Because everything in the book here we are doing to understand how the internal work. So if I were to develop an app, for example, I would take a different approach. So in notebook, it's a great way to have plots and everything in the same place. But if you are developing an application, you might want to structure the code a bit differently. So for example, having Python scripts, and in this case, where was I? Sorry, organizing it like I've done here or even differently. And yeah, here, everything otherwise is the same. So I am defining the path, the tokenizer and so forth. I'm loading the checkpoint, I'm loading my model that I've trained. And then I have a function to basically execute this. I also should say I'm not affiliated by any means with chain that I just found that this is a nice minimal example. Some other people use streamlit, for example. Also, lightning AI has a great lit surf library, where lit surf is also really, so I, I used to work with let's serve a lot. I still do. It's a very nice fast way of serving models. So also something worth checking out. It's a open source library. So there are lots of options. Our approach here is focused on looking at the code to understand how things work without using any external libraries if possible, so that we get the maximal understanding and so the calculation has finished. So I can finally stop rambling on about coding design and talk about the results. So we can see we get a 97% training accuracy. We get a 97% validation accuracy and 95% test accuracy. So based on this, I would say there is no overfitting, but maybe because that is usually what happens when you use a validation set, I tweaked the settings so much to get a good validation accuracy that I have some slight bias towards this validation set. And if I have a totally unbiased data set, a new data set I have not worked with before, like here, I can see this is slightly lower. It's expected usually because I used the validation set quite a bit when I was, for example, determining the learning rate, the weight decay, and so forth. And that's why it's usually a good idea to have a test set where it's like an independent evaluation. And the same can be said about these additional experiments. So here I am looking at the test accuracy. And if you were to do that and select the best model on the, based on the test set here, you would again introduce a slight bias. So in that case, it would be a good idea to have a second test set to evaluate the performance of the final model after you selected based on this test set. and yeah if you're interested in model evaluation and selection I did have this paper here that I recommend I'm pretty we're both there and explain all the different trade-offs and everything that might be a good companion if you are interested in building a classifier but for now let me take a short break and I will be back with a next video on using this model in practice on new examples So, we have arrived in the final section of this chapter using the LLM as a spam classifier. This essentially means that we are now implementing a small convenience function so that we can use the model that we have just trained on new texts. For example, if we have a new text message that we can classify whether this text message is spam or not spam. So in the grand scheme of things where we are right now is we are here in step 10. We prepared the data set, we downloaded the data set, we loaded the pre-trained weights. Most importantly, we fine tuned the model, we evaluated the model, and now we get to use that model. So for that, let me define a function called, let's call it classify, oops, that's my text. of these days, I have to find a better keyboard position. I'm usually not such a bad typer. It's just because of the recording, I have my keyboard at an awkward height here. So of course, what we want to classify as the text, we will need the model, we will need the tokenizer to prepare our input data, we will need the device to make sure the data is on the same device as the model, we can or we should define a max length, so that we can say how much padding we need to make sure the examples are padded to the length that we or that our model expects based on the training. And then of course, when we talk about paddings or padding, we also need the pad token ID. And for that, we are going to use the you're going to use the end of text token 50,256. Now, of course, the first step to preparing the text is of course, like always, encoding it, tokenizing it, getting the token IDs here. Then we, let me make a helper variable here. Let's call that supported context length. So here, I will just assign this length of the inputs that the model supports based on our configuration file, We know it's 1024, but to keep that function a bit more, this variable, the way we extract this information, but more general, we can actually just get that from the positional embedding matrix, right? Because the matrix here, the number of rows is the context length. So that's what I'm assigning here. And then we can accordingly prepare the length of our input IDs. For example, if it's a very long tensor, right? So if it's very long, we might want to truncate this, right? So we want to truncate it down to supported context length, because otherwise the model will crash. And if we have a max length defined, so let's do it like this. If we have max length, it will use that one, assuming that the max length is smaller than the context length. So here it will now, oh, I think I made a typo. I, it will now get whatever of those two values is smaller. So in case, in our case, max length is 120. So it will use 120 and truncate them to 120 of the input IDs is let's say 200 or something like that. Now we also have to take care of the case where the input IDs are smaller, right? So if we have input IDs of token IDs, 20 of them in a text message, we want to pad them to 120 to make sure it's similar to what the model has seen in the training set. So I mentioned in the bonus material in the previous section that there are different ways of doing it. But in our case, the way we have set things up, the model always expects the last token ID at a certain position. So we have to make sure we apply the padding to make sure the position is always at the same point. So how do we do this? Let's do, I mean, there are multiple ways. Let's just do it simple here. We use the patent token ID. And then we calculate, let's do it like this. How many do we need? The max length. So if we have max length 120 and our actual length is, let's say 60, it will add 60 tokens. So we can calculate the size, just using length, because this is still a Python list. So if our input is 60, max length is 120, it will add 20 pet token IDs to our input tensors, token IDs. Okay, so far so good. So one more thing, our model doesn't want Python list, it wants PyTorch tensors. So we have to convert this to a tensor inputs IDs, and then can also specify the device. So it's directly initialized on the device instead of doing it like this. I mean, we can also do it like this, but doing it like this is even better because it will initialize it directly on the device. And then our model also likes batches. So we are adding the batch dimension here. Next, of course, the model needs to generate the logits. So let's do no grad because we're not training anymore. We get the logits based on the inputs IDs. And then, of course, we are interested in the last token. Okay, so that's what we have here. Most interestingly, we want the predicted label. And for that, similar to before, we can use the torch arc max on the logits. And then of course, it's 2d denser. So we do a minus one. What else? So next, I think this is it. So we can, based on that, we can actually return the prediction. So we have the predicted label. So we can just return it. I mean, we can return zero or one, we can directly return this, for example, as a number. We can also just say if, if it's one return spam, it's maybe a bit nicer spam if predicted label equals one and else not spam. I hope this works. It looks good so far. And so we could technically now give it some sample. Let me just prepare some sample here. So here we have, you are a winner. If you are a winner, you have been specially selected to receive $1,000 cash or a $2,000 reward. I mean, might be legit, but to me this sounds pretty much like spam. Maybe I'm just like this person who always deletes these messages, but my first intuition would tell me in this day and age, this is spam. So let's see how good our model is doing. If I have no typo this time, if I have implemented correctly and it does seem like I did everything correctly here. So we, or the model predicts that this is spam. So let's do another one. So let's classify, I have some other text here that we'll just provide, let's classify another one. Hey, just wanted to check if you are still on for dinner tonight, let me know. And this is usually, in my opinion, something that sounds legit, of course, depends on the sender, but yeah, let's see what our model thinks. So in this case, it's not spam. And so with that, we have now a spam classifier that we can use. I mentioned before, you can put a nice interface on it, like this chainlet interface, or you can develop your own independent interface if you know some basic web interface programming and so forth, whatever you like. but this is how the code works. This is what underlies our spam classifier and it should work now on text messages. Of course, you can also, if you're interested, extend this to other problems like email spam classification, document classification, all kinds of things. So, but one thing, if you want to use this later, one thing that might be useful is saving the model. So similar to what we have done in chapter five, we can use the model.state dict here and save the model. We can give it some name like review or text classifier.pth. In case you are wondering in the book, I think I always say review classifier. It might be an artifact because I first worked with this movie review data set. So, and I figured out, okay, this is too large. It takes too long to train. It might be nicer for readers to work with a spam classifier. So maybe I should have just called it text classifier instead of review classifier, but in case you're wondering. And then, yeah, this will save the model. So we can go in here and see it should have, I don't see it right now. Oh, I'm in chapter five, actually, because I think I had it open for something else. Here, we can now take a look and see it's saved here as text classifier, and then we can lot this in a new session. So, um, if I were to initialize a new notebook for, because I'm lazy right now, I'm not doing it, but you have seen how it works in chapter five. So we can not the state dict with a torch lot function and then, uh, the name, uh, text classifier here, and then we can also say where we want to load this on our device that we have specified. And then this will load the state dict like this. And hopefully this works all keys matched successfully. And yeah, with that, we have now our spam classifier that we can use and this I hope was a nice introduction to fine tuning how we update a model that has been pre-trained. I'm just scrolling up because in the next chapter, it's actually my favorite chapter, we will be fine-tuning the model for becoming a personal assistant, like instruction fine-tuning the model to do other things, not just returning class labels, but answering with full sentences and doing different types of things and so forth. So stay tuned for chapter seven where we will be instruction fine tuning the model.
