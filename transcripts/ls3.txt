Hi and welcome back everyone to chapter 3, coding attention mechanisms. So this is a supplementary coding along video for the Build a Large Language Model from Scratch book. And here I'm trying to walk you through some of the code examples to explain what attention mechanisms are and what role they play in the large language model. This is actually a very technical chapter, maybe the most technical chapter in the book, because it's similar to implementing the engine of a car, where the engine is the most complex and the most technical aspect of a car, in my opinion. But it's also a very important topic because it helps us really understand what the LLM is doing and why LLMs are, let's say, different than other types of models. For instance, the transformer architecture introduced this self-attention mechanism that we are going to code in this chapter. And it's been very transformational in terms of developing these large language models. And also in the grand scheme of things, just to take a step back here, what we are trying to do here in this video or in general, what I'm trying to do with the book is to help you understand how large language models work. We are going to build a large language model in the next chapter, actually, and then we're going to train it in chapter 5. But we are still, you know, keeping things more self-contained and small for educational purposes. This will be a fully functional large language model. However, it's more on the smaller scale compared to, let's say, the latest chat GPT models. So personally, how I would see it or compare it is that we are here implementing an LLM that is more similar to an old Ford Mustang, for example, from the 1960s. It's a real car. It drives. However, it's not the latest and greatest car. It's not like a Ferrari, which has been engineered with millions, if not billions of dollars, a lot of expensive machinery. So this would be a car that I would say, that is impossible to build by a single person. You need a really a big team for that or a team of highly professional experts. And you need also a lot of high precision machinery, where with this old Ford Mustang, you might know that it is possible actually to maybe not build the whole car, but to restore it and to fix it by yourself. So if you're interested in cars and so in that sense, the LLM we are building here, it's not a high performance car. It's more like a simpler car that we can work on ourselves. However, it still helps us understand how cars work if we are building the simpler car. And the reason why we're doing this is we can do it by ourselves without having to spend millions of dollars on, let's say, GPU compute to train a large scale model. However, anyways, the methods we are going to introduce here are the same. So there's lots we can learn here. And in particular, we're going to start here with section 3.3, attending to different parts of the input with self-attention, where self-attention is a mechanism to parse the input data. So for instance, if I go to the chapter itself, just to show you where we are in the grand scheme of things of this book or this video series here. So in the first chapter, we talked a bit about setting up our Python coding environment. And then in chapter 2, we talked about the data preparation and sampling. So here we download the text data and we formatted the text data, we tokenized it, and we set up the PyTorch data loaders. And we are going to use those later when we are training the LLM. Now we are implementing this attention mechanism, one of the core parts of the LLM. And then later on in the next chapter, we will be implementing the LLM architecture. So if I go further to chapter 3, or I'm already in chapter 3, if I go further to this figure here, this is an outline of the topics we are going to cover in chapter 3. So they are all centered around the self-attention mechanism. However, we are going to approach this step by step. First, we are going to start with a simplified version of self-attention, just to highlight the big picture concept. Then we are moving from the simplified self-attention to the real self-attention mechanism. And then we are going to add on specific things that are used currently in the decoder LLMs, the popular types of LLMs, and that is causal self-attention. So this is the concept of causal self-attention. And then we are going to take it one step further and implement multi-head attention, which is essentially a more advanced version of causal attention. But we will see exactly what that means later on. Right now in this section, in section 3.3, we are going to be focused on this simplified self-attention to introduce the concept of self-attention gently. So the reason why we are actually developing self-attention or why we care about it is to address a shortcoming that other types of neural networks had. So I don't want to go over all the details here. If you're interested, you can maybe read the chapter in its entirety. But the main idea here is that previous types of architectures, for example, recurrent neural networks, when they translated from one sequence to another sequence, for instance, here this would be German to English translation, what they had to do is they had to memorize a state of the input. They were reading the whole input. And then before they could generate the output, they had to kind of save a summary or like a latent representation, a condensed representation of the input, and then using that input step by step, generating the output. If you are not familiar with recurrent neural networks, don't worry about it. It's not really important for the remainder of this chapter or this book at all. I'm mentioning it here for I would say completeness for those who are familiar with RNNs. Now what's important, though, is that here the shortcoming is when you translate text from one language to the other, and you can't really look at the full input sentence. You can only have like this intermediate state. You might lose information and then it leads to an un-optimal or suboptimal translation. So the idea in self-attention is for each step when we generate text, so whether we translate text or whether we answer questions, we get information from a text or we do anything with text really, where we generate text, at any time we can look at the whole input. So that's the core idea of self-attention. So for instance, if we have a translation task and we are translating the next token, so this is the German sentence and this would translate into, "can you help me?" So here we generated the first output. Now we are here translating the second output. At each step, even though we are at the second output, it cannot only look at the previously generated token. It can also look at all the other input tokens. So it has access, so there's a mechanism to access all the input tokens that we have provided as the user and what's more important even is that we give it selective access. So for example, in this case, when this sentence is translated into English, it's looking at all the input sentences, but I try to visualize it here with these dots. Each input token has a different importance. For example, this first word has a medium importance and these two words have a very small importance, very small dots here, where this token has a relatively high importance because it's relatively important for translating this particular token. So if you know German, "du" is the German word for "you". So in this case, this is actually a very important word for translating this word. And so here we are assigning a higher weight. We make this token more important. And in a similar case, the LLM is doing this automatically. So it's assigning these important weights to the different inputs. And these important weight or importance weights are the so-called attention scores. And we are going to compute the attention scores and sometimes also called attention weights. But there's actually a small distinction between attention scores and weights that we will get to later on also in this video. So the short story here is that when we are generating text at any time, we can reference the whole input text. If you as a user, you ask a question, give me the five most important facts of my input document. The LLM at any time when it generates text, it can reference the whole input without having to store an intermediate representation. So it's actually a very powerful mechanism. And so yeah, in the grand scheme of things, so what we are going to do now is we are going to implement this self-attention mechanism. In the previous chapter 2 videos, we covered the pre-processing steps. Now we are assuming we have already processed our data and we are jumping directly into this self-attention module. And then later on in the next chapter, we will be implementing all the other parts that go into LLM. So one more figure before I go to the code examples. Here is a summary of what we are trying to accomplish with the self-attention mechanism. I mentioned this referencing of all the input, but on a more like zoomed in level, what we are trying to accomplish is we are going to transform the inputs into a different type of representation. So in chapter 2, what we did is we took an input text, for example, "your journey starts" "... step". Let's say this is an input sentence that I truncated a bit here for brevity. So we can fit it onto a figure. So if we have an input text like this, remember from the previous chapter, we are tokenizing this. And then we are converting the token IDs into so-called vector embeddings. So embedding vectors and each vector here just for simplicity has only 3 values. So we talked about this more in the previous chapter 2 videos. So where we have a vector representation. And now here, what I'm showing you is the vector representations of all the inputs. What we are going to do now is we are going to consider one of these inputs that we want to transform into a so-called context vector. It's called context vector because this Z here, it's also an embedding vector, but it has the information of all the inputs. And so this is, if I scroll up a bit, it is kind of connected to what I told you earlier here. So when we are generating this output token, so this specific token, it has information about all the input tokens. It's actually a weighted sum over all the input tokens. So you can see that this one is more important, but all of them are involved in generating this one here. And so when I scroll down again, this is what you can see here. We have all the inputs here and they are all involved in generating this output vector here. They have different importances though. So here these alpha values, alpha to 1, to 2, to 3, and to T, they are all different weights. And this is a measure of how much of this input vector will be represented in here. So it's maybe a very mathematical concept. linear algebra concept in a sense, because we are going to compute a dot product here. But you can, yeah, maybe we will see in the code, maybe it will be more intuitive once we look actually at the code. But so this is the big picture level. We also, one more thing I should say is we are going to focus on the second input. So here X2, so X2 is the focus here. And this will give us the context vector with respect to this input. All the inputs play a role here, but you have a specific set of alpha weights. So you can see there's always a two in here to 1, to 2, to 3, and to T. So this means this is the weight of the first input with respect to this one. And this is the weight of the second input with respect to the second input. That's why it's 2 2. And then it's the weight of the third input, 3, with respect to the second one and so forth. So this context vector is specific to this second one here. Why it's the second one, it's just because I chose it for this example. Ultimately, we are going to do it for all of them. So if I go up one more time here, here, I also picked the second one and the second one looks at all the input tokens. But this is also true if we look at the first one, it would similarly also look at all the input tokens or the third one and so forth. We just have to pick one now for the example, because otherwise, if we start picking multiple ones, it gets too complicated. We will get to that later because ultimately we do this computation. So we do this computation here for all the vectors. But to keep it simple, we are going to focus on X2 as the reference. And the reference is also often called the query. So let me actually go to the code and then we can actually look at this in a more concrete way where it's maybe a little bit less abstract and maybe this helps with understanding what is actually happening here. So let me copy a tensor here I have. So we are going to use TensorFlow again. And so this is the complete sentence similar to the figures I showed you. So your journey starts with one step. This is my input tensor. And each row here is the embedding vector corresponding to this token. So I'm, for simplicity, assuming each word is a token. But you know, it could be a bit different. It could be split into two tokens, depending on how a tokenizer works. And tokenizers was a topic in chapter 2. For simplicity, we are just assuming each token is one word. And then we have this three dimensional input vector embedding for each of these words. So going back to the figures one more time. So here I'm showing you. So here this is, if I go up one more time, sorry. Here this is the complete, let's say, procedure. We are going from inputs to this context vector and we have these intermediate weights. But we don't know yet how to compute those. So we will take it step by step. So we are going to break this down into smaller steps that get us to this context vector. So the first step we are going to take is we are going to compute these intermediate values. So before these alpha values, I don't want to scroll up again, but the alpha values, they are the so-called attention weights. Before we compute the attention weights, we call the, we compute the so-called attention scores, which are a non-normalized or un-normalized version of the attention weights. The attention weights will come later. This is more like one step towards these attention weights. How they are computed is by computing a dot product between an input and each value here in this sequence. Again, like I mentioned before, we are taking example 2, X2, as our query. And then for this query, we are comparing it to each, to each of the inputs here. And so one thing, again, I want to highlight is we are talking about a simple self-attention mechanism. Later on, we will talk about the real self-attention mechanism, but you know, to keep things, I would say step by step and at least making it a bit more approachable, I wanted to start with a simpler version because we can always make it more complicated later on. But I think this nicely highlights like the idea behind it. So how we compute these values is by taking the dot product between the two. And in linear algebra, the dot product, yeah, it gives us a sense of similarity. It's like, it's a measure of how similar two, two vectors are essentially. I mean, there's also, there are things involved like, yeah, taking the vector norm, normalizing the vectors, length normalization and so forth. But for simplicity, we're not doing that. We are just computing the dot product. And you can think of it as a way to measure the similarity between two vectors. So if I scroll down, yeah, let me just go to the code notebook, it might be easier. So we have now this input tensor here. And so this input tensor contains 1, 2, 3, 4, 5, 6. We're going to focus on one at a time. So let's consider as our input vector, let's call this the input query. We are considering, let's say this one. So I can also reference this as inputs or let's take the second one because that's what we had in the figure, right? If you recall here, we have X2. So we are going to also use X2 here. And X2 would correspond to the second row because Python is indexed one. So X2 would be the second row, 0, 1. Yeah. Okay. So this is our input query. We can print it out. There we go. So this is our input query. And we do the same thing now considering a different vector. Let's just choose the first one here. So let's call this input, input, let's say zero or input one because then we are consistent with this number. It's maybe more intuitive. And so we are going to call this inputs 0. So this is the vector we have here. Now what we're going to do now is a manual approach. So we are going to compute something here that is taking the element element-wise, oh yeah, multiplying the vectors element-wise. So if I do just brute force by hand, just to illustrate it, we multiply the first value with a second value here, then this value times this value plus this value times this value. And so we get this value out here. So this is essentially this vector dot producted with this one. And there is a nicer way to do this in PyTorch. We can do actually torch dot and then take this one and this one. And we get the same value as you can see. So this is a so-called dot product. That's what we're doing here. Now, yeah, we could do this actually for all the different inputs here. We only did it for the first one, but it would be a lot of work, right? So we have to either write it like this or this for each one. So what we could do is we could actually write a for loop. So if we do a PyTorch or let's do a Python for loop first. If I do a Python for loop, just copied it here. So what we can do is we can say for each element in my inputs value, we are going to, we have a result here and we are going to add them to it. So it's essentially what I've been doing here. So for each element. So if I print it out for element in inputs zero, it's like print element. So it's for each element here, right? And then we have index, which is a different way of accessing this. This should not change if I do it like this. Oops, iterated over one D tensor. Oh, I forgot an `enumerate`, I think. So I'm just `idx` is just a position. So this is the same, still the same here. You can see we are just iterating over it. But why I'm using the index here is that I can now do the same thing for the query. So we have this input query, right? So I can now also index this one. So let me change it nicer. Well, let's just leave it like it is. So here I can now multiply those two and then let me just print them. So I have these and then I can actually sum them. So one way would be let's do starting with zero and then let's just add them up. So we're adding them up. And then let's double check result. So you can see what I've been doing here is the same that I've been doing here. But now I can do it for all the different vectors, right? So I can go through those. Actually, one second, I should probably use a variable here. Otherwise, we will get always the same results. So we can change the vector here. So the vector 1, 2, 3, 4, 5, 6, and so forth. So this is the first one. That should be the same that we got before. Then we can do the same thing with the next one, another one, and so forth. Again, this is still a lot of work, right? So one way to make this actually easier is to use the dot products, right? So I could technically go in here and do that. I don't need that one. Then I could do this. So we don't need this also then, just making it more efficient. So I can do inputs, I, and input query like this. So we don't need this one. But see, it doesn't change. So it's the same as before. We are just replacing this manual summation with this dot product. And if I do the zero, it should give me this one or this one basically. So this is one way we can automate this. Now, going one step further, this code here, what it does is it's doing the same thing, except now we are going to do it for all of them all at once. Instead of me having to change this here like this all the time, we are going to have a for loop that does that. So for each input x_i, so the I here for each I in this collection of inputs, we are going to compute the dot product with a query. So we call that input query, right? And we're going to store them in this output tensor because yeah, it's one. So in PyTorch or sorry, in Python, we could use a list and append to a list, but in PyTorch we have vectors and tensors and they have a fixed size. So it is possible to extend those, but it's very inefficient. Usually tensors are a fixed size. So we have to, for efficiency, say how big our tensor should be. And so here we are saying it has the shape six, six, because that's how many rows we have 1, 2, 3, 4, 5, 6. So that's what we're doing here. We are instantiating this and then we are filling in the values that we got here. So if I print this, you can see these values here are the same I'm getting here. So if I have a 2, 0, 1, 2, so this is this value here, right? And so if I want to get 0, 1, 2, 3, this value, this is the same as putting a 3 here. You can see these are the same. So all we have done now is we have made this a bit more convenient. We use this torch dot method instead of us doing this for loop manually and filling in the values. So going back, and by the way, I was a bit, I skimmed over this, but I think there is a typo here. So you can see these are the same values. This should be actually correct in the PDF version because I'm 100% sure that this was fixed or I fixed this. So I have to contact the Manning layout team to also update this here in this website version. And yeah, so if you're a bit confused why these are the same, they shouldn't be the same. So they should actually be different values. Pretty sure in the print version, it's correct. And I will just contact the Manning team to also update it here. Anyway, so we have implemented this now. So we have these, these values. So these values here are the values you are seeing here. And yeah, for visual purposes, I truncated them. So 0.9 really refers to 0.95 and then 1.4 corresponds to 1.4 here. So instead of rounding them, I truncated them because then I thought it's easier to identify them here because otherwise it would be 1.5 and then you would be looking here and where's the 1.5 here, right? So that that's basically why, yeah, why these numbers are different. They were a bit too large or too long to have them here in the figure. So I just truncated them. Okay. So we have done this step now. We have these scores. Now the next step would be going from the scores to the attention weights. And again, the whole reason why we are doing that is it's a measure of similarity. How, yeah, how similar is the input to this query? So if I look at these values here, or maybe it's easier to look at, like look at them here, you can see this is a higher value than this one. So 1.4 is higher than 0.9. So one might say, oh, journey here. The second word journey is more similar to the input, which happens to be also journey. So it's, it's a way of saying, oh, they are more relevant to each other than this one, which is a bit different. Now these values later on, there are weight values involved. So here we are talking about attention weights, but there are parameter weights like a new network weights that are going to be trained in chapter 5. And then these scores are optimized for the task at hand. So sometimes depending on the input, one word is more important than the other. And the network will learn how to recognize which word is important with its weight training, basically. So here, you know, it's a very simple attention mechanism, like I mentioned before. So I wouldn't take these values too seriously. It's more for highlighting what is the actual computation that is happening in the attention mechanism. So what we have done so far is we took the dot product between an input and each of the other inputs, we got these scores. Now we are going to normalize these scores so that they sum up to one. So it's, it's a normalization and this is done to so that we don't get crazy, large numbers, and it helps also with the optimization process to have smaller numbers. It's more like a cosmetic thing to do. And so there are different ways we can normalize. So you probably would think of the most intuitive way of dividing each score by the sum, right? So that would be one way to do it. So if I have, let's say attention weights to, let's call this temporary because we are going to introduce another method later, later. So attention scores too. And then what we could do, we can just divide this by the sum and yeah, probably you already know what happens now. This will be then giving you the normalized scores. You can see that. And they are all smaller than one, but what's more important is of course, that they now sum up, oops, sum up to one. So just making the properties a bit nicer for normalization purposes. And there's one actually more common way of doing that. It's the so-called softmax function. So I'm going to define the softmax function here myself. And this is something I wouldn't use in practice because it's not very stable numerically for certain values, but just to illustrate it, it's basically the exponential of the input divided by the exponential value and then similarly the sum. So this is similar to what we've done here. So this will also give us normalized values. So we called that, let's call that attention scores too. So the reason why I'm calling this attention scores too, is that it is with respect to the query for the second input token. So that's why, why the two, we would technically do that for all the other ones too, but we would have to change the query. And that is something we are going to do in the next video. So, or the next section. So, but yeah, sticking with our normalization here. So here we have again, a normalized tensor. The values are slightly different, not that much different. You can see it's, it's roughly the same. If you would round them, you could probably get very similar values here, but it has slightly nicer properties during optimization when we are training the neural network. So it's, it's again, just, yeah, a small tweak to get better optimization when we are training the network. And so, yeah, this is how we normalize the weights, how we get from here to here. Now there's one more thing I mentioned, this is a simple softmax version. So it might not be ideal in certain values might be unstable. So I highly recommend actually in practice, if you're doing this to use yeah, the PyTorch version of that. So softmax, and then let's do this. Oops. My mouse keeps sliding off my desk because it's at an angle. I'm sorry. But so yeah, here, what we are doing is we are just using the PyTorch version. So in this case, you can see that the values are exactly the same, but there are certain values where this one will fail or be unstable. And this one is a more optimized stable version. So whenever there is a PyTorch function that is already implemented, it is highly recommended to use that one over a self implementation. However, self implementations are always nice for understanding how things work. So you can, by looking at this mathematically understand what's happening, because with this one, yeah, it's a bit more abstracted. So it's harder to understand how this works. So what we have done now is we computed those scores and there's one more thing in the section I want to do, and that is computing the context vector. So if you don't mind, let me scroll up one more time. So I promised I would not do that, but it's maybe the last time for the section. So in the beginning, we talked about taking the inputs, computing the attention weights, this is all what we have already done, and then we compute this context vector. So we have this, the inputs, we have the tension weights, but we don't have the context vector yet. And that is what I'm doing now. So what we are going to do is we are going to use those as weights to compute the weighted sum over these inputs. To make this a bit more clear, I think I have a nicer finger here somewhere here. So we have the inputs again, and then for each input, we multiply it with this attention weight for each one separately. And then we just sum up all these vectors and then we get that context vector. And again, the idea is of this context vector that it combines the information from all the input vectors in a sense that those with a higher attention weight, they play a larger role in the output. So in this, yeah, output vector here, the weighted sum. So weighted sum, you can also think almost as averaging, but with the weight where each one has a different importance. Okay, let's actually do that. So I have some code I will put in here just so that it is a bit quicker, because I realize it's a very long section. So what we are going to do here, we are going to initialize them with zeros. So we have an empty vector. Let me show this. So we have three values and that is the desired size of our context vector. If I go here again, we want three output values and it's the same shape as the input, we have three inputs and we want three outputs. Now what we're going to do is, so we have the query again, is our second query, which is why we call that the second context vector, like back to, because later on in the next section, we're going to do this for all the other vectors right now. We are still laser focused on Z2, the computation with respect to the second input token. So the second input token to all the other inputs, and by the way, that's also why it's called self-attention. So that is, we are considering one of the input and then we compute the attention to itself, to the whole sequence, to each input there. Okay, so we have the second input as the query. We have these zeros, just initial values. And then we are going to iterate over all the vectors in the input. So maybe let me show this. Print x_i. Of course. So, and these, if I go up here, these are those. So looking at them, they will look familiar to you. So we are just iterating over all these rows, one row at a time. And then we multiply them with our attention weights. And again, I'm using this trick where we have the index position. So we are indexing. So maybe I can print it out here to make it more clear. So we have index zero, one, two, and I could actually get this value by doing this, the same thing. Now we use this index so that we can also access the weights. So the weights are those here. So before maybe I do this, let me comment this out and this is maybe easier to see. So we will have X1. Oh, let's do it like this. Print attention weight. Yeah. Um, yeah, there we go. Yeah. I have my keyboard at a slightly different angle for the recording, which is making typing a bit more unintuitive for me. Okay. So, yeah. So we are just looking at the attention weight and then the associated, um, yeah, vector, we can also change this to make it more intuitive to this. There is an arrow here. Oh yeah. Attention weights is not defined. Did I not, ah, I see, um, I should have saved those. Still not defined. Well, this happens when you, oh yeah, there we go. Okay. So we have this first attention weight. We multiply it with this first vector. We have the second attention weight. We multiply it with a second vector and so forth. And this is what is happening here. So make this a bit more intuitive. So we have this multiplication here. This one with this one, this one with this one, and then we are adding them up. So, uh, like this. So this is plus equal, which means we are adding it on top of this one. So it's a weighted sum because we are waiting it. We are waiting the input with this attention weight, and then we are adding it all up on top of each other. So the result, let me comment this out. So the result is this one here, and this is the context vector. So this is the context vector that we are seeing here. Again, the truncation of 0.4, 0.6 and 0.5. So 0.4, 0.6 and 0.5. And so what we have done now, I realized it was very technical. So I encourage you to maybe step through this a bit more slowly by yourself. But yeah, what we have done now is we computed this context vector with respect to the second input element. In the next video, I will show you how we can do that for all the other ones. So, cause it is a lot of work if we would have to repeat this because you have to compute the attention weight. So what we would have to do is we would have to change the query. For example, if we want the first input token, we have to change it to this one here, and then we would have to change it here and here, and then we have to also compute those in the first place. So we have to go back, change this one and so forth. So it would be a lot of work. So in the next video, I'm going to show you how we can generalize that to all the other inputs. So in this section, we are going to continue with our simple self-attention mechanism. The subtitle here or the section title is the simple self-attention mechanism without trainable weights, because we are still talking about a simple self-attention mechanism. In later videos, we will be introducing trainable weights that help the model optimize the computations. But here we are still talking about the simple self-attention mechanism. We are still talking about the simple self-attention mechanism that we introduced in the previous section. Now we are going to generalize this a bit more now, because in the previous section, what we did is we focused only on one of the inputs as the query and then computed the attention weights between the query and all the other values or the other inputs. And then we computed the so-called context vector with respect to the second input query, which is why you can see the two here. That's the second query. So this is the context vector with respect to the second query. Nonetheless, it contains information about all the other inputs. So now we are going to generalize that again in the previous section. I want to make this crystal clear. So it's a bit more, I think, intuitive to understand what we are doing here. In the previous section, we computed the attention weights only with respect to this second input vector. Now we are going to compute all the other input attention weights here. You can see this is a 6x6 matrix because it's for each input. We have 6 inputs. We compute the attention weight for each other input. And that's why it's called self-attention because it's with respect to the input sequence itself. Okay. So let's actually do this by borrowing some of the code from the previous section. So we need to first, so this was the computation of the context vector, which involved the attention weights. So we have to first compute these attention weights. So here let's borrow this one. And what we've done in the previous section is we computed it with respect to the second input token, like I mentioned. Now we are going to generalize this. So let me just get rid of some of the code comments and it might be a bit easier to read. So what we have is we have an iteration over the inputs and then we compute these attention scores. Now let's actually modify this a bit. So let's make this a bit more general. So instead of saying input query, let's say x_j, and then instead of saying attention scores with respect to the second input token, let's just call that attention scores and then do ij. So this will be now a matrix. Um, sorry, this will be the matrix. This is a specific value, but so we have now a matrix attention scores that has the shape six by six. And if I write ij, this would be a specific value in this attention matrix. So we have this six by six attention score matrix. So let's actually initialize that. So for that, we are going to use torch.empty(6, 6). So this is our 6x6 matrix. And then what we are going to do is let's get rid of this because this is only for the second row, right? So we want to do it for all the rows. Now we are going to compute these values here where each ij is each, each cell here. So what we are missing is we are missing, um, we're missing one for loop. So what we have to do here is we have to move for j, um, x_j in enumerate, and then we indent this and have it like this. So what we have here now is we have a double for loop and we are computing now the dot product between each input and each input. So x_i, and x_j, if we iterate like this, it will be this 1, 0, 0, then 0, 1, 0, 2, 0, 3, and then 0, sorry, 1, one, one, two, one, three, and so forth. So it's going to be iterating over this whole matrix, the 6x6 matrix via this j and j here. So if I do it like this, this would be then our matrix here. So this matrix, now the values look a bit different. You can see it's 0.2, we have 0.9 and so forth. And this is because these are the attention scores. They are not normalized yet. So we have to also apply the softmax normalization and we will get there in a moment. So I wanted to show you one trick though. So this is how we do it manually using dot products, but there's a concept called matrix multiplication. And we can actually, instead of doing two for loops, we could do a matrix multiplication, which is much faster because in PyTorch, for loops are actually quite slow because they can't really be optimized that much compared to a matrix multiplications, which have very efficient implementations on the computer. So we can actually compute the same thing here by doing a matrix multiplication. So if you're not familiar with matrix multiplications, it is, I would say a topic for a separate video perhaps. But yeah, I don't want to make these videos too long because then we would end up doing a whole course on deep neural networks and linear algebra. And then three weeks later, we'll get to the self-attention mechanism. So if you're not familiar with matrix multiplication, I would say this is essentially an automated or shortcut way of doing these two nested for loops. And you can see if I didn't make a mistake here, we should get the same values. So you can see these are exactly the same values. We are just automating this now. So these values here, they do look different though, from these here, which are the attention weights. So what we forgot is we forgot the softmax. So we computed previously this softmax. So that is what we are going to do now. So let's do the softmax. So attention scores, it's a convention. Attention scores refer to the unnormalized values, and then we can do attention weights and normalize them. Like this. Let's print them. And so these are the normalized weights. So you noticed I did type something like dim=1 here. So what does that mean? So it's essentially so that the weights along this axis sum up to 1, I hope. So actually let's take this one and do a torch.sum. Must be a tensor, not a list. Oh yeah. So 999. Okay. Let's try another one. One too many. Yeah. They do some minus rounding errors. They do, should sum up to 1. So we could also actually do this automatically instead of testing all of them, can do attention_weights.sum them. So you can see they do sum up to 1. So each, each of them basically. And if you play a paid close attention, let's see. I actually didn't. So let's see previously in the previous section, we computed these attention weights with respect to the second input. So this is with respect to the second input. So this year should technically match what we have here. And as you can see, oops, let's just do it like this. So as you can see, this one matches this one. Now what we have done is we have automated this one. So we have automated this whole computation here with this attention weight and everything in a few lines of code. So technically the only two lines of code we have is building the attention scores and then the attention weights. So we have everything in two lines of code that we have done previously in the previous video with a lot of work for one of them. Now we have it for all of them. So that is how we generalize things. We are missing one thing though. If I go back, we computed the context vector after the attention weights. So that is still something we have to do. So how do we do that? How do we compute the context vectors? Again, there's a handy way of doing that with matrix multiplication. So I can type context vex and then the attention weights, then matrix multiplication with all the inputs. So instead, so this is generalizing what we had in a previous section. This is generalizing this computation. But if we do it manually, we have to have four for loops. We have to have one for loop over the inputs, compute the attention weights. And then we have another for loop for computing those weighted sums. And then we have to do it for all the vectors and it's a lot of for loops. So here we just do matrix multiplication for the attention weights, another matrix multiplication for context vectors, and then we are done. So this is it. So we are done. And yeah, again, if you pay close attention, this one should resemble the context vector we got in the previous section. So let's see, where is it here? So this is context vector two. Let me just copy the values here and paste them below. And yeah, we can see those are indeed the same, but now we have done it for all the other ones too, so by swapping out what is our query and yeah, so this is how we make things nice and compact. So we have now, if I clean this up a bit, we have now very few lines of code, very few lines of code to compute the attention weights and then the context vectors. So I can do it like this, and then you can see, this is all we need to compute the context vectors for our simple attention mechanism. Yep. So we have now these for all the different inputs. In previous sections, we build a very simple version of self-attention for educational purposes, to have a gentle introduction to the concept of self-attention. Now we are going to introduce these so-called trainable weights. Trainable weights. And the idea here is that these are model weight parameters that are going to be optimized later during the model training in chapter 5. So this will help the model to produce better outputs by learning better attention weights. And this is by the way, also the way the real self-attention mechanism works. In this section, we're not going to implement the whole self-attention mechanism that is used in GPT models. We'll come later, later in this chapter, but this is one step closer to what's really happening in GPT models in terms of the attention mechanism. So just to take a small step back to section 3.3.1, what we have done there is we computed these so-called context vectors. So we had the inputs with an input sentence. These are the vector embeddings. Then we computed the attention weights. And then based on the attention weights, we computed the weighted sum over the inputs to come at these or to get these context vectors. What we're going to do now is very, very similar, except that we are now introducing additional trainable weights, but the goal is still to compute this context vector, to jump a bit ahead and to show you one figure here. So here, so what we are going to do is we are still working with the input sentence, the same input sentence, but now we have a context vector that only has two dimensions. And I should mention it's two dimensions. So that I want, because I wanted to show you that this dimensionality is arbitrary. I could choose two, I could choose four, but it's no longer dependent on the input size here. So before it had to have the same size as the input vector, because we were computing the weighted sum over all the inputs. So if we wanted to add those, they had to have the same size as the input. Now here we are more flexible because we are introducing these query key and value parameters. And by the way, I'm using very, very small embeddings here and context vectors for illustration purposes in reality, when we are training the model later in chapter 5, these are much larger. Uh, in the case of the smallest GPT two model, for example, this is, um, top of my head 768 and for the biggest one, I think it's 2000, 1,600. So in reality, when we are training LLMs, they are much larger than 3 and 2. It's more like a thousand something or even multiple thousand of dimensions. Uh, here it's really just kept small for illustration purposes, but the size here, the main point is it's no longer dependent on the input size. It's a flexible size. I can choose or we can choose. And this is determined by these, um, weight matrices. Uh, one more thing here is we compute these weight matrices. Um, so they are WQ WK and WV they stand for query key and value. So we compute these query key and value vectors by doing a matrix multiplication between the input and these weight matrices. And then based on those, we compute still as before, similar to before these attention scores. And then we convert the attention scores into attention weights that sum up to one. And then we compute the context vector. Um, yeah, but new is the attention weights and that we have these intermediate values here. Um, okay. So again, also for simplicity, similar to section 3.3.1, we are only looking at this context vector with respect to the second input. So you can see this, this, um, index position two, similar to here, because we are focusing on the second input. Of course, in reality, we want to compute the context vector for each of the inputs, but this is more work. We will do this later to just have an introduction to the topic. We will focus only on one of them at a time. Um, yeah, and notice that the query here is derived from the second input. And this query is also reused here and here, but again, similar to before the context vector combines all the inputs. It's still with respect to the second input though, because this query is derived from the second input, the context vectors for the other inputs for the first input would look different because the first input would have a different query vector that we would use everywhere. Okay. So let's maybe jump into the code. And by the way, why it's called KV and Q that's because yeah, K, um, for query, sorry, K for key V for value and Q for query. And this is, um, I think I have a section on this in the book. It's based on database terminology, but again, I don't want to go over all the details here, um, because I realized the video 3.3.1 was already very long and I don't want to make infinitely long videos here. I want to focus on the code examples. So let's jump into the code. So, um, to begin with what I wanted to do is I wanted to first define a few values here that we are going to use. And so before we had this inputs here, this was our inputs tensor where we had the 3d embedding for the first, um, token, the second token, and so forth. Sorry, third token, fourth token, fifth and sixth token. And so what we're going to do is we are going to assign this to, um, a placeholder variable. Let's call that X two to keep it shorter. And then we also have a placeholder value, uh, D in. And so the, in would be just the dimensionality of, of this one. So let's do this. Maybe more general like this. So this would be the number of columns. So three dimensions. I could also write three here, but to make it more general, to show that it depends on the actual size, because this might be different depending on what you choose for the embedding size. Um, but for this one, we can choose it as a user. We can choose our output embedding size. So that's the output embedding size here of each of these thingies. Okay. Now we have to find those. So just some placeholders. The next part would be generating the weight parameters. Now I'm setting up a random seed here that, so that you get exactly the same values I'm getting. So we will now generate the weight for the query, the key and the values. And for that, we are going to use the torch.random function. So this is generating random values in the shape three by two. So we have three by two matrix. And in addition to make it trainable, we can use torch parameter later. We will replace this by a different method, but in general, torch parameter is a wrapper around a tensor in PyTorch to make it trainable. So this will be then a trainable matrix. It should be torch and, and so this is a trainable merit, uh, matrix. And you can see there's this requires grad to true, which means that it is a trainable, uh, matrix. It requires a gradient basically. Um, but we won't be doing training until chapter 5. So we do the same thing now for the key and the value, just like this. And they are all different. So query key and value will be different as you can see here. So maybe let me do it like this. W query and then W key. So you can see they're all different, randomly initialized matrices. Now, what we are going to do next is we are going to compute the query here, the Q2. So how are we going to do that? So let me define a query as with respect to the second input. So we know what input it corresponds to. So this is corresponding to the second input that we defined here. And then we do the matrix multiplication with the query. And so what it really does here is it's transforming a linear transformation. Uh, it's transforming the three dimensional embedding into a two dimensional one. So you can see, this is now a two dimensional embedding. It's yeah. Matrix multiplication. I don't want to go too much into linear algebra here, but it's essentially a linear transformation projecting the three dimensional tensor into a two dimensional one. So if I, maybe just for a moment, print this out and then just really briefly, because I don't want to make these videos too long because then you get bored and it takes us a lot of time. So I'm always recording them in the evening after work and it is always getting pretty late and dark anyway. So we do have this input that is three values here. And then if we do the dot product with this column, that would be our first value. And then if we do this dot product between this one and this one, this would be our second value, essentially. And so we are projecting this three dimensional input into a two dimensional, uh, yeah, vector. Now we do the same thing for the keys and values. So now we have the query. Now let me go back to the figure one more time. So the query is reused everywhere here. But the keys are unique. So you can see here, I'm drawing this, um, WQ, but here I'm not drawing it because I'm reusing it here because it's with respect to the second input. The key though, and the value, um, vectors, they're unique. They are always depending on the input here at each position. So for that, it's a bit different. So what we are going to do is we are, uh, doing the matrix multiplication with the whole, whole input, um, tensor. So we're going to do it like this. And yeah, so this should be then having the shape, maybe pause the video to have like a small challenge, but this should have the weight, sorry, the dimension of the inputs 6 and the output dimension 2. It's because it's a six times three matrix times 3x2. So it's 6x2. Let's double check if I am correct here. Um, W key, I think. So it's a 6 times 2 dimensional output. Um, and so if I do keys, so this one would correspond to this key here. The second one should correspond to this one and so forth. Okay. So now we have these values. The next one, the next one would be computing these attention scores. So as you recall, uh, previously we call, uh, computed the attention scores by doing a dot product between the query and the input. So going back one more time, we computed this one here. Well, let's, let's do this one. We computed the input here as the dot product between this input and the query, which is this one, or in this case, we computed the, the attention score by multiplying or doing a dot product between this input and itself. So this X_2 dot X_2. Now it's a bit different. Now we are not working with these directly anymore. We are going to work with these here. We're going to do the dot product between Q two and K two. So let me do that. So for that, let's define keys with respect. So to the second input, it's a bit easier than the Python indexing because it starts at zero. So I want to have the nice number two here that corresponds to the two here. And then we compute the attention score to two. So two, two, because it's the second input with respect to the second input as the query. So this is a two, two here. And this is like the torch dot product, the torch query two, and then keys two. Keys two is this one. Yeah, it should work. Let's just print it out. So this is one point eight, something, and this should represent this one here. Now we would have to do the same thing for the other ones, which is a lot of work. So we would do this manually for, um, one point two and up to two T. So in our case, two, six, so this would be a lot of typing if we had to, you know, do this for all of them. So there's again, one trick that we can do. We can compute this all in one go. We can use again, our friend, the matrix multiplication by doing this matrix multiplication, these attention scores are now computed for all of them. So we can see, for example, the second one, just to double check matches this one and this one, this key should be the one corresponding, sorry, the attention score should be the one corresponding to this one, one point two here. The dot is very small. It's not a 12. It's a one point two. Okay. So far, so good. Next, what we have to do is we have to compute the normalized attention weights. And remember from the previous sections, how we did that, we computed those by using the soft max function and PyTorch. So we had torch.softmax. And then what we did is, um, attention scores here, the ones with respect to the second input, and then as a new thing here, we use a normalization term. So let me just define this. So the keys here, they have dimension two, so just as a placeholder, the dimension of the keys. Um, and so here, what we are going to do is we're going to take the key here, what we are going to do is we are going to use that the square root of that, so we can do the square root in Python like this still have to get used to my new keyboard position here. I am having it a bit different than I'm usually typing it, but so in Python, just to illustrate the square root, I mean, could do square root `import math` and then `math.sqrt`, for example, the square root of 4 should be 2, right? So we could do it like that, but an easier way, instead of importing here, we could just do it like this. So that's the same thing, essentially. So that's what I'm doing here. And this will keep them in a reasonable range. So this was originally proposed in the attention is all you need paper in the 2016 or 2017 paper "Attention is All You Need" that introduced the original transformer architecture and self-attention mechanism. So, but yeah, it's the same softmax we used in the previous section, except that we have now this normalization. So let's see. Let's call them attention weights with respect to the second input. Then we can look at how they look like. And so, yeah, these are our attention weights. And if I didn't make a mistake here, they should all sum up to one. Let's double check this. So, yeah, they nicely sum up to one and one more step. So if you counted, there's one more step, the context vector. Now we have the attention weights. That's one more thing. We have to now compute the context vector before in the previous sections. But what we did is we computed those as the weighted sum over the inputs. Now we are not working directly with the inputs anymore. We are working with these intermediate values. And so like shown here with this arrow, we compute the context vectors by applying the attention weights as the weighting to the values instead of the inputs. Previous sections used the inputs. Now we are going to use these values. So maybe just briefly as a big picture, we have these intermediate values that are the result of this matrix multiplication. So it's like a linear transformation of the input and the query and the key. They are involved in computing the attention scores and attention weights. And then the value is involved in computing the context vector by multiplying it with the attention weights. So in the case here, all of them are involved, but they are involved in different steps. Now these are already used up for computing the attention weights, but now we are going to use the value with this attention weight to compute the context vector. And the context vector is again over all the inputs, but it's still with respect to the second input because the query is everywhere used from the second input. But still the context vector combines the information of all the inputs from everything here. Why do we have these? So there are actually papers. I think I referenced one in the book. There are papers that show it doesn't have to be that complicated. You can use fewer weight matrices, but this happens to work very well empirically. So this is a way that just happens to work well. You get slightly better performance. I think there was even a discussion on GitHub for the book chapter. Maybe I can post this later. But a reader also experimented with reducing the number of matrix multiplications. And yeah, the results were also pretty good. So there are different ways you can implement this. What I'm showing you here is the original implementation as it is used in the transformer paper and in most LLMs essentially. And it's mainly based on empirical performance. Okay. So let's get back to the context vector step here. So the context vector is now computed as the sum, the weighted sum over these values. And so context back to, and then we use the attention weights that we just computed and do a matrix multiplication. And then we should get all the context vectors. So there should be 6, so sorry, one context vector for all the 6 inputs. So they are the weighted sum over all the values. And so, yeah, this is our context vector here, 0.3, 0.8. You can see again, the truncated values, 0.3 and 0.8. Oops. Now what we've done here is kind of analogous to section 3.3.1, where we computed everything with respect to the second input. Then in section 3.3.2, we generalized that for all the other input tokens. In a similar fashion in 3.4.2, we are going to generalize this to compute the context vectors also for input 1, 2, 3, 4, 5, and 6. In this section, we are going to generalize what we have implemented in the previous section. And that means that we are now computing the context vector for all the other inputs. Previously, we have only done that with respect to the second input, using that as the query. Now we are going to generalize it so that we get the context vector also for all the other inputs. So how we are going to do that is by just reusing the code that we have written before. So here I'm just instantiating a placeholder SelfAttention class, a PyTorch class, so that we can use this self-attention concept a bit more, I would say compactly, without having to manually have multiple Jupyter Notebook cells and so forth, which is very error-prone. So we'll organize everything into one class. And then we are going to reuse the code that we have above here. So here, just going to take this one and put it here in our constructor. So this will initialize these weight matrices when we create a new object. I need a self here in addition. Okay. Next, what we're going to do is we need the attention score computation. So I had it right here. So we will put that in the forward because this is what we instantiate. And then we are going to use it in the forward call here. So like this, let's call it values. And then we do the same for the queries. Now we are going to do it for all the queries before we only did it for the second query. Okay. What is missing is the attention score and weight computation. So the attention scores here, right? So we put it right below here. And now it's with respect to the queries. And we're going to reuse the softmax. Again, with respect to all the attention scores. Yep. And I think this is about it or the context vector computation, which we can copy from here. There we go. And this should be about it. So let's give it a try. Let's use our torch.manual_seed(123). And then let's call that self-attention version one, because I will show you a slightly improved version in a few moments. And we have to give it our input and output dimensions, which are used here to instantiate the weights. And then we can call this one on the input. So just maybe before I execute it, the inputs are our input embeddings. So the first token, second token, and so forth. So I'm calling it on our inputs here and let's see what happens. So yeah, this gives us some values and it works nicely here that now we generate our inputs. Now we generalize that and we get the context vectors for each of the inputs. Just to double check that these numbers are correct. We know what this one should be. This is the second context vector, second row. So we know what that should be. So by the way, again, each one corresponds to one token. So this is just like this might be easier. So this one is the context vector using this as a query. This is the context vector using this as the query and so forth. And this one should match. The second one should match what we have computed previously. So the second input as the query. And we can see this here is actually the same as this one. So our implementation here is correct. Now I wanted to show you a slightly better way of implementing this. So we can call this version two. And so in PyTorch, there's a concept called the linear layer that is also used in what is multi-layer perceptrons, for example. And so what we can do is we can use instead of creating these ourselves, we can take advantage of this torch and end up linear. And I shouldn't have removed those. We still need that. So we can actually also create this linear layer. It's just like a slightly simpler way. And this will create a linear layer that has a weight matrix. And it has a bias parameter. I can show it maybe here just as an example. Let's do a 2 by 3. And then you can see there is a weight matrix here. And there's also this bias tensor. We don't need the bias tensor. We just want a weight matrix. So what we do is we can set this to bias faults. And to make it maybe more general, we can call it QKV bias. Some LLMs use that and some LLMs don't use that. So it really depends on the implementation. It's more popular nowadays not to use it. But GPT-2, for example, originally used it. So it really depends on the LLM. It's not necessary to use it. So modern LLMs don't use it anymore. So that's why I'm setting it to faults here. Okay. So now we have those. So it's a slight modification. We replaced our manual parameter generation with this `Linear`. So we don't have to call torch.random. And the initialization here of the initial weights, it's also slightly more optimal, these values. So there's a weight initialization scheme that takes into account the dimensions here and so forth. So it usually results in better training dynamics if we just use this and then linear layer compared to using our torch.random here, which we haven't normalized. So these are just small random values. And here there's a slightly better weight initialization under the hood, basically. So we're taking advantage of that. Now this changes only slightly. So how this changes is that now we have these and they are called like this, where we have the inputs as the input and so forth. So I can copy it down here and the rest should be the same. Let me just update it, values. And this should be it. Let's double check if everything works. But don't expect the exact same results as above here. That's because the weight initialization here is different. So we have different initial weights and that is why you won't notice the same values. I think the reason is I call them key and value instead of keys and values. Yeah. So, yeah, this works, but the values look a bit funny. They're a bit large. I mean, they would be optimized during the training, but we can also just use a different random seed, get smaller values, which look a bit better. But this is something that would get optimized during the neural network training. And yeah, so in this section, we generalized here, we generalized our computation to generate the context vectors for all the other inputs. In addition, we also have this nice compact SelfAttention class now that uses the linear layers and that we can use. We are one step closer now to completing the engine of our LLM, which means implementing the SelfAttention class that is going to be used in chapter 5 when we are pre-training the LLM. Now, one more concept we are introducing here is a so-called causal attention mask. And the purpose here is hiding future words. But before we get to the hiding of future words, just to recap the big picture view here of what we have done so far in chapter 3. So before, at the beginning of this chapter, we implemented a simplified SelfAttention class for mainly educational purposes to illustrate how self-attention works. And then we extended this with these trainable weights to implement the real self-attention mechanism that is then going to be optimized in the pre-training. But on top of that, we are going to modify the self-attention mechanism, adding this causal attention mask that hides certain words inside the mechanism. So to illustrate what I mean, I have a figure here. So before we computed all these attention weights as shown here, if we have an input, your journey starts with one step. What is happening is we are computing the attention weight for each combination of the input with itself. So for example, if we have the sentence, your journey, and then the LLM is supposed to generate the next word, let's say the next word is starts, one thing that happens is when we are generating the word starts, it has access to all the words in this input. So even though it hasn't seen, let's say, future words, like with one step, for example, it has the attention weights for these future words. And maybe to make this a bit more, let's say, simpler to follow, let me type this here. Your journey starts with one step. So when we have input text like this, like I explained earlier in chapter 2, we want to train the LLM to generate one token at a time. So the first token it might see is your, and then it's supposed to generate journey, right? So when it's supposed to generate journey, it's not supposed to see those tokens yet, so they should be hidden. And so in order to accomplish that, we have to mask them because during the pre-training, this is available because we have this in our training set, but we don't want the LLM to see that yet. We only want to see, or you want the LLM to see only the next token. So what we're going to do is, we are going to mask out all these values here so that when the LLM receives the second input journey, it cannot really see anything beyond that. So it's really, when it's supposed to generate the token starts, it's not seeing these other tokens in the input. We are hiding it. So it's essentially a modification to the self-attention mechanism to hide future words. So how are we going to do that? So let me show this in code, and we are going to use for simplicity, let's use those things here. So we implemented previously the SelfAttentionV2 class, and we are going to use that just to keep it a bit simpler so that we can modify it because we are going to create a modified SelfAttention class that is then the causal SelfAttention class. So just to play around with this for a bit though, before we implement this class, let me just do it like this. And then, of course, I have to use this here instead of self. Okay, let's see if this works. So now we have computed these attention weights, and these attention weights are the ones you are seeing here, and we are going to mask everything above the diagonal now. So one way we can do this is by creating a mask. And so this mask, I just copied the code here, it's torch.tril. So in this torch.tril, based on one side, this torch.tril, based on ones, it will create this triangular mask here essentially, where we have ones below the diagonal and zeros above the diagonal. And then what I can do very simply is I can take this one and multiply it by mask simple. And what will happen is if we multiply a value by one, it won't change, but if we multiply a value by zero, the value will be zeroed out. So let's call that masked simple, and then we will print out how it looks like. And so now you can see we masked these values. So in the first row, everything, but the first value is here is zeroed, so they're all zero. And then in the second row, we have the two tokens here, and everything after the two tokens is zeroed out. Now, one thing you might notice that has changed is that now the values in each row, they don't sum up to one anymore, right? So, and it is actually good for optimization purposes to have them sum up to 1. So what we can do is we can use our normalization that I introduced way back early in this chapter, just using the .sum method. And yeah, then essentially divide each value or each row by the sum of the values in each row. And mathematically, this should come out as normalizing these values so that they sum up to one. We did that earlier. I think it was early in this chapter where we used a similar method. So that was before we introduced the softmax. So what we have now is we have normalized it such that each row now contains, again, values that sum up to one. And I do think I have a nice figure here somewhere. Yep, here. So what we have done so far is we took the unnormalized attention scores. So if I go back, we have the attention scores here. Then what we've done is we computed the normalized attention weights. That's with a softmax here. And then we computed the masked values, which is what we have done here. And then we normalized those so that they sum up to one again in each row. And that is what we have here. So that is one way we can do that. So now we have things that sum up to one in each row again. So that is one way how we can compute this mask here. So this is what we've just computed. Now, there is actually a simpler way. So this is 1, 2, 3, 4 steps. There's one simpler way of accomplishing exactly the same thing. And that is a little trick here. I should have a figure on this here. So what we can do is instead of computing the softmax and then masking and renormalizing, we could actually do this in one go. So where we have the attention scores, they are unnormalized. And then we mask them. Instead of applying the softmax, we mask them. So before, just take one step back here. Before, we applied the softmax and then mask them. Now we're doing it the other way around. We are first masking it. And then we are computing the softmax. And so this is only three steps. It's a bit simpler. So that is one trick. If you are familiar with, let's say, some basic math concepts, this is one trick. We can just make it a bit simpler. So I'm again to show you how this works. I'm again creating a mask here. So this mask has now minus inf values above the diagonal. And so minus inf stands for negative infinity. It's a very small number, like minus 999999999, and so forth. And if you are familiar with some basic math concepts, if you take e to the power of a very small value, like a negative value. Let me try this, like this, maybe. This should be close to zero, essentially. Oops, torch x. It should be a tensor. One moment. So that's one thing when working with PyTorch. So you can see it comes out as zero. And it's the same one as if I would do it like this. I think this one should work. Oh, yeah. The same problem again. Oh, yeah. I have a double inf. I have a minus here and a minus here. So either this one or this one should do the job. So what I'm showing you here is what we have here. So the minus inf. And if we take the exponent of that, it comes out as zero. And if you recall previously when we implemented the softmax, let me search for this here. So you can see we computed like this here, like the exponent, right? So what we are doing is we are taking a shortcut based on this mathematical property. And we are now computing this mask and apply this mask directly to the attention weights. And then what we do is we can compute the softmax again. So this is the same softmax function we used before. If I scroll up again. So right here. So you can see this is the same thing. Except now we are going to apply to the mask the values instead of the attention scores. But this part is essentially the same. I could also make it more consistent. Like this. And this should give me also the masked attention weights. And these are exactly the same masked attention weights that we have seen earlier here. Except now it's one less step. So now we only have three steps instead of four steps. And so, yeah, this is our causal attention mechanism. And later on, we will also add this to our CausalAttention class, the compact class. But I want to introduce one more step in the next section before we actually modify our compact SelfAttention class. In the previous section, we introduced the so-called causal mask to hide future words in the input so that the LLM can't cheat and access future words when it's generating the next word. Now we are going to introduce another type of mask, a so-called dropout mask. So what is the dropout mask? So the dropout mask will pick random positions in this attention weight matrix and then mask them out. And so this dropout is a very popular concept in deep learning. It has been around maybe for 10 years or longer. And it's just applied to various layers to reduce overfitting. And similar here, we can apply a dropout mask to the attention mechanism so that the model learns to rely less on certain positions. So here it's just a random selection of certain positions. And then we apply this mask on top of our causal mask to mask out these positions. And you can see if we apply this mask to things that are already masked out here above the diagonal, nothing will happen. But it will additionally also mask out certain things in the input to reduce the reliance on these positions to reduce overfitting. I should say, though, this is not very commonly used anymore when training LLMs. I'm adding it here for completeness because the OpenAI team for the GPT-2 model, they used a dropout mechanism. But more modern LLMs, they are not using it anymore. The reason why I'm introducing it here is just for completeness so that we are implementing the original architecture. But if you are training LLMs, you can also just deactivate that or just omit this dropout mask. But yeah, just for completeness, let's see how dropout works, how we mask these things. So luckily, so PyTorch has a layer called dropout. So it's actually quite simple to add this on top of our existing procedures. And it asks you to specify the dropout rate. So 0.5 here means 50%. So this means, for example, we are dropping half or 50% of the positions. So let's assign this to layer. And then let me also add a random seed, torch.manual_seed, so that you hopefully get the same results that I do. However, I should say there's one thing that is particular about dropout, and that is that it has a little implementation detail or bug, depends on how you want to call it, in PyTorch, that on certain hardware or operating systems, it is not fully deterministic. So you might get slightly different results. I reported this a while back to the PyTorch team on the GitHub tracker, so it's not resolved yet, because they said it's not a high priority issue, because I guess dropout is not that popular in the first place anymore. But yeah, so just that you are aware, you might get slightly different results, but that doesn't really matter that much. Now, one more thing, just to toy around with a simple example, let me create a tensor of ones. Let's do a six by six. So it's exactly the same size as our attention weight matrix. And so I can now call layer example. So I'm calling my dropout layer on this example, and let's see what happens. So yeah, what you can see is, certain positions are zeroed out now. So just random positions here. By the way, if I change the random seed, you will see it's different positions. So it's just a random way of masking out values. Now, one thing here is though that the other values, notice that they are now larger. And so why is that? So to maintain approximately the same sum in each row, it is rescaling the values. So for example, the formula is, I think it should be one over one minus dropout rate. So if we have this formula here, and my dropout rate is, what did I say above? 0.5, if I put 0.5 here, this should come out at two. So if I have a smaller dropout rate, this should be then a different value. So we are essentially rescaling our values here to account for the fact that we are missing certain values. So it's just so that if you consider fully connected layers, the next layer that receives the output from this layer, it receives about the same summed value. So if we would sum those values and these, they should be in the same order of magnitude before and after. Of course, this is a very small example, so it doesn't, let's say, turn out as nicely that it is exactly like that. But it's the rough idea that when we reduce half of the values here, if we remove half of them, the other ones are scaled up, so it's compensating for that. And so this is essentially how dropout works. And then we can apply our layer to our attention weights and then have the dropped out attention weights, essentially. So this mask is what you are seeing here. And now we are, again, one step closer to completing the engine, the attention mechanism for the LLM. In the previous two sections, we introduced two types of masks, a causal attention mask and a dropout mask. Now, let's actually add those masks to our compact SelfAttention class, which brings us one step closer to completing the engine of the LLM, the self-attention mask that we are going to use when we are training the LLM. So let me copy this one. This is our SelfAttention class here. And I think I just saw also a little mistake here. So notice that we have an X here and we have inputs here. So this should also be X. And speaking of inputs, let's now also work with a batch of inputs. So that would be similar to the output that the data loader in Chapter 2 returns. So inputs, again, is a tensor we use. Inputs, again, is a tensor we had, consisting of 1, 2, 3, 4, 5, 6 input tokens. And each input token consists of a three-dimensional vector. Now, to create a batch, just for simplicity, I'm going to just stack two inputs on top of each other, which should be across the first dimension. So to give me a batch of inputs, let's call that batch. And this should now, if I didn't make a mistake, be a two-by-six-by-three-dimensional tensor. Let's see. So it's two inputs. So two times the inputs tensor. And each inputs tensor has six tokens. And each token is a three-dimensional vector. So now we are going to work with this batch. Next, let's make a few modifications to our class here. Let's also call this causal attention. And we are going to add now the causal attention mask and the dropout mask. So let's add a dropout mask here. And then we are going... So this is the dropout rate. And now we are going to add the dropout layer. And then dropout, which uses the dropout rate. Next, we are going to implement the causal mask. So I'm just copying it here. And so the causal mask here is similar to what we have defined before, except that I am going to register this now as a buffer. So let's call this just for simplicity mask. And why am I doing this? Why am I doing this is, in PyTorch, we could optionally later on train a model on the GPU. You don't have to, but if you have a GPU, later on when we define the model, we can move it over to a device, for example, a CUDA GPU device. Now, PyTorch will move over all the weight parameters, all the layers here, but it won't do that, for example, for arbitrary tensors like those. By the way, I define the context length here as the size of the mask. So I should also ideally put that here, context length. So PyTorch doesn't move over tensors like that. So we have to register those tensors. And this is called register buffer. And then if we do that, this mask also gets moved over to the model. And we have to do the same thing here. It gets moved over to the model. Yeah, and we could technically also generate the mask on the fly here, down here. It's just a design decision so that we don't have to regenerate this mask every time. We are just generating one mask once with a maximum supported context length. And then we are going to reuse that here when we apply the causal attention mask. So let me just copy and paste the relevant code. So this is just a small update here. So notice that I added a few things. I have now this masked fill now. And in PyTorch, by convention, everything that ends with an underscore is an in-place operation. So in-place means it doesn't create a copy as an intermediate value. It's as if these operations exist, it's a efficiency thing. But nothing to worry about. This is just like a small note here that this will happen in place. So I don't need, for example, I don't need anything like this because it already happens in place. So I don't need to assign anything. This will already modify this existing attention scores. And yeah, and then we have this bool thing where it's applying the mask to those that are non-zero. So we apply the mask, the minus infinity mask that we defined in a previous section here. So we are going to apply this one. So we are setting, essentially, sorry, we are setting those minus infinity values. And then we are applying the softmax function to scale it. And then we apply dropout. So essentially what we are doing is we are applying this mask to our attention score. So it's exactly the same thing that we have here. And then after that, we also apply the dropout mask to our attention weights. So we apply two masks, the causal mask and then the dropout mask. And then we compute the context vectors similar to what we have done before. So let's actually see if I didn't make a mistake. And let's modify this here, causal. I have a few more inputs now, the context length and the dropout rate. So we can define dropout. Let's just set it to zero, by the way, because like I mentioned before, it's not commonly used anymore anyways. And then context length, this should be depending on the batch input. So our batch has 6 input tokens. So let's say our attention mechanism supports up to 6 input tokens. So I can do shape one, where zero, one, it should give me this 6 here, as you can see. Yeah, and let me add white space here and then we can call it on our batch here. Maybe, yeah, let's try it out. Unmatched, I think I have one, too many of these. NumTokens is not defined. Yeah, so one more thing I should probably add is I should define num_tokens. So we can here, for example, define the batch size, numTokens and then the input dimension based on our input. So this is two in our case, because our batch is 2, num_tokens is 6, and this would be the dimension for the embedding, so the input embedding. So, oops, yeah, this works. So I actually wanted to make a new line here, sorry. So this comes from X, which is batch in our case. And this has, it's a two by, let's say 2 by 6 by 3 tensor, right? So this is, we are just extracting out these dimension here, these dimensions here, but in a more general case. So instead of, yeah, hard coding those, we extract them directly from the input that goes in here. Okay, and yeah, as you already saw, this works. So now we have this CausalAttention class, which is a nice compact CausalAttention class to compute the context vectors. So you can see there are a bunch of things. This thing is similar to this thing, and that is because if we look at our batch, we duplicated the inputs. So just for simplicity, this is the first input, and this is the same thing because we created the batch by duplicating it. In reality, when we are using our data a lot later, of course they will be different. So different inputs in our batch, but this is just for educational purposes. And then for each three dimensional input, we now have a two dimensional context vector. So for the second input, this would be the corresponding context vector and so forth. So yeah, we have now a nice CausalAttention class. We just implemented a self-attention mechanism with a causal attention mask. Now we are almost done with the self-attention mechanism that we are going to use in the LLM. There's just one more thing we are going to add, and that is the so-called multi-head attention. So if you are familiar with the attention is all you need paper shown here. This is the original paper by Google in 2017 that introduced the original transformer architecture. So LLMs like GPT and so forth, they use this right subpart here, which is also called the decoder submodule. So the left part is not commonly used anymore in LLMs. So this was a more, let's say, complicated architecture than was really needed. So on the right hand side, this would be what's similar to GPT style LLMs today. And you can see there's something called the masked multi-head attention module. And that is what we are going to implement in this section. So before, the causal attention module would be the same as masked attention. So masked here is a more general term that refers to a type of masking. In the previous section, we introduced a specific type of mask, the causal mask. And by the way, they were also using a causal attention mask. But yeah, whether you call it masked attention or causal attention in this context is the same thing. But our CausalAttention class that we implemented, it would be a single-head attention module, a masked single-head attention module. Now we are going to implement a multi-head attention module. So a multi-head attention module is similar, except that we have now multiple single-head attention modules stacked on top of each other. So if you are familiar with convolutional networks, just to draw an analogy, so a convolutional network is an older type of architecture that has nothing to do, it can be used for text extraction, but it was originally also designed for image extraction. So if it's unfamiliar to you, maybe don't worry about it too much. But the idea was that we had, well, we still have and still use this type of network to extract different types of information from an image automatically. And there are so-called "channels" here or different filters. So the different filters extract different types of information from the image. And in that sense, that's what we are also trying to accomplish here. We have multiple heads or multiple of these self-attention modules to extract different types of information from the input. So the idea is really to extract more and different types of information. And this works because each single-head attention module is initialized with a different set of weights. And then each of them will learn slightly different features during the training. And then it's a way of extracting more and more diverse information from the input. To show this to you in visualization here, so maybe let me see if I have a simpler figure here. So before we had this inputs tensor, where inputs was a six-by-three-dimensional tensor, we had six tokens, and then each token was embedded in a three-dimensional space. And then what we did is we had a single-head attention module, the causal attention module, that created a six-by-two-dimensional context vector. So the context vector still had six numbers of tokens, or six rows corresponding to the six tokens. But each row was a two-dimensional embedding now instead of three dimensions. We said before the two and the three, they are arbitrary. We can choose them. But to keep things simple, let's assume we had three input dimensions and two output dimensions. And what came out was this tensor Z. So here it's called Z1. So just think of it as Z. Maybe to also show it to you here. So our input was a batch consisting of two input examples. And this first one would be the inputs X. So here we have the 6 rows and the 3 columns, the 6 rows corresponding to the 6 tokens, and the 3 columns corresponding to this three-dimensional embedding. And then the context vector that we got out of the causal attention mechanism was also a 6-by-2. Also, we had 6 numbers of tokens and now 2 dimensions as outputs. So before it was 3. Now we have 2, but the number of rows is the same. Anyways, so we had that. Now we are going to create a second context tensor. So before we only had Z1. Now what we are going to create is Z1 and Z2. So this would be a case where we have two heads. So one and two. The number of heads, it's arbitrary. It's a hyperparameter, for example, in GPT-2 models. It's, I think, I believe it's between 16 and 25. We will see actually exactly how many there are in chapter 5. We usually use more than two heads here. I'm just using two heads for visualization purposes so it fits nicely onto the slide. And then we create two of these context tensors and then we concatenate them. So you can see we have two context tensors where each has an output dimension of two. And when we concatenate them, we have now an output dimension of four because it's two times two. And this is what we are going to build. And how we are going to build that is by simply stacking two of the single causal attention mechanisms that we implemented in the previous video on top of each other. So in the previous video, we had the causal attention mechanism that had a weight matrix for the query, the key, and the value outputs. And then each the query weight matrix created the queries, the key weight matrix created the keys, and so forth. Now we have a second set of weights. Before we had only WQ. Now we have WQ1 and WQ2. So we are having a second stack. You can simply think of it as doing the same thing twice almost and then concatenating. So at the end, we have also two context tensors like mentioned before. And we create this combined context tensor. So let's maybe see how that looks like in code because I think that will make things a bit more clear. So what we're going to do now is we are going to create a class. Let's call that the multi-head attention wrapper. And then we use the torch module here. And then let's do the init constructor. And here, yeah, one thing just sometimes I tend to forget that we are just inheriting features from the module class. Oops. And then one thing is we are going to implement this in a simple way for now. Later in the next video, I will show you a more efficient way. But the simplest way would be really just stacking these causal attention mechanisms. I will just copy and paste it from the previous video. So previously what we had is we had this CausalAttention class. And we are now instantiating an object from this class. Let me add the missing parenthesis. So we are instantiating this object. And we are going to instantiate it a number of times. So for example, for a number of times where the number of times is determined by the number of heads. So for example, if we set the number of heads to 2, let's add it actually here. So the number of heads could be 2. And then this would be creating two causal attention heads. Let me also copy those over actually. And then also the QKV bias. We're not going to use that one, but just for completeness. So that is how we initialize or how we would, for example, initialize multiple of our causal attention modules. One more thing though is in PyTorch, let me just make this a bit nicer. In PyTorch, I recommend using the nn.module list here because it will give it some nice properties with the buffers and everything I mentioned before. And so this would be now a stack of multiple CausalAttention classes or objects of this class that we implemented in the previous video. Now let's actually define the forward method to use that. So the one way, of course, we could use that is by just calling head on the input for each head in self.heads. So this is a list of multiple single head attention mechanisms. And we are just iterating over those. So let's make this a list. And in PyTorch, the simplest way to combine them would be concatenating them. So we do that along the last axis here. And this should be giving us our concatenated context sensors. And this is really all there is to it. I believe if we looked at the figures, it looks a bit more complicated because there's a lot of stuff going on. So each layer here is showing the causal attention mechanism. But really, it's just multiple of those on top of each other. And we already did the hard work and implemented this. So we are just stacking it here. Then let's actually use this torch manual seed just to make sure you get similar results. Let's just define some variables. So the context length would be batch.shape 1 because we have the batch 2 times 6 times 3. So we have two inputs. Each has six tokens. And then the output dimension or input dimension of each token is 3. And then we can also define the input dimension here again and maybe also that output dimension. So the input dimension is 3 in the figure. Or we can also make it more general. The shape can extract it from the input. And then let's say for the output one, we do 2. And again, like I mentioned, this is a hyperparameter. So we will use much larger values later on when we implement the whole LLM. But let me code this one. And I'm just going to copy this here. So we have to find those. Dropout, let's just for simplicity set it to zero. And yeah, this is technically it. And I hope it works. It does not super needs an argument. Yeah, I always make a mistake here. I think I just forgot the parentheses. And now the shapes are not aligned. 12 by 3 can't be multiplied by 2 by 2. So I think I made a small mistake somewhere. Queries here. So let me see. I can see I should have written a different number. I should have written minus one because we want the three dimensions and not the two dimensions here. So that was the mistake. Yeah, so this is it. I could also just write three here. And this should work. Now we have this multi-head attention mechanism. So the first two values here, they correspond to the first context vector and the second one to the second. So what we are doing here is recreating these two sets of tensors. And then we are concatenating the results. And this is really it. We are now technically done with the self-attention mechanism, the one that we implemented in the previous section, the multi-head attention mechanism. But I wanted to show you one more way to implement multi-head attention a bit more efficiently. So previously, one downside of the implementation was that we were stacking multiple attention modules. And then when we were calling them in the forward method, that was a sequential call here. So we had to iterate through those sequentially. I mean, not us, but the computer. And this is not very efficient for, let's say, GPUs or even CPUs when we train this. Because technically, we could execute the calculation in parallel, parallelizing things under the hood using, for example, matrix multiplication. Because for loops are always a bit slow in Python because one for loop has to wait for the other for loop to finish. But since these attention heads all operate independently of each other, there's no need for one to wait for the other. So I'm going to show you an alternative implementation. And this is a bit technical. So it's a lot of code. Took me a long time to write this code. I will walk through this a bit. But yeah, I didn't want to retype everything because it's easy to make mistakes in certain cases. But I will walk you through the big picture idea here. So here again, similar to before, we get the context vectors of shape one, two, three, four, five, six, six times four. This is similar to what we have here. The values are a bit different because we use a different initialization here. But we'll get to that in a moment. But mathematically, the operations in there, they are giving us the same results. So you could technically take either one of them, plug them into an LLM, and then train them in the LLM. And yeah, when we get to chapter 5, maybe as an exercise, you can swap out the attention mechanisms and see that the model will train with either one of them. It's just that this one is faster during the training. So what is happening here? So let me go through this step by step. So what's happening here is we are still initializing the same variables as before. And then we have this assertion here. We are saying the output dimension, the dimension we have here, has to be divisible by the number of heads. So if we have output dimension four and we divide it by the number of heads, let's say two, this should be not giving you any remainder. So because this determines the dimension of each head. So here, if this is four, this is two, we have a two-dimensional head dimension. And the head dimension can think of it as the dimension also of the context vector before we concatenate them. Now, maybe let's make, or let's just keep it like it is for now. And going a bit further down here, now what we have is we have a query, a key and a value like we had before, but they are bigger now. So before we had the out of two. So the out was equaling two. Now we have it set down here to four, which means these are bigger weight matrices compared to our causal attention module before. So we are just, instead of concatenating multiple causal attention mechanisms, we are just using a bigger weight matrix here. I think I have a figure on that somewhere here. I can show you maybe better what I mean. So before, in the previous section, what we did is this one. So we had the inputs and then we had a stack of two weight matrices. Here I'm only showing you the queries, but the same would apply to the keys and values. And then we multiplied the inputs with the query and got the queries here. And then we did the second multiplication and got the second set of queries. And we did the same for the keys and the values. We got two attention matrices and two context vectors and we concatenated them. And this is a value, a reasonable thing to do. A valid thing to do, but this is inefficient unless we do this at the same time. So in our previous implementation, we had the Python for loop where this was inefficient because it happened sequentially. So it was first doing this and then was doing this. And now one way we can do both of them at once is by combining those into one weight matrix. So it's a linear algebra concept. But if you concatenate those weight matrices to one bigger weight matrix, like here, then what you could do is you can do the multiplication between the inputs and the weight matrix here, and then you get the queries out here. And then when you split those, you get similar to above here, two query sets. And what's happening here is mathematically the equivalent thing. You get exactly the same values, but this one is cheaper because you can do this in one go. So the way it's implemented, this can be one matrix multiplication. And splitting is incredibly cheap in Python and PyTorch. This one is expensive before because we had a for loop doing them separately. So that's the whole idea between what I'm showing you here, that we have now these bigger matrices. And then here we compute the keys, queries, and so forth. So this one here that you're seeing corresponds now to this bigger queries. And then what you are seeing here is the splitting. So here we are performing this splitting operation. And we are going to use the view. It's just reshaping it. And so we are splitting it by introducing the number of heads. So before the input dimension of each of these is the batch size, the number of tokens, and the output embedding size. So for example, this would be two, if we consider our batch, six tokens. So two times six, and then four, two, six, four. And then we are going to split this into two, six, two, and two. So we are splitting this dimension, the four, into two and two. And this is why I have this one here, because it has to be divisible, right? So we want this head dimension to be nicely divisible so that we can split this one up into this one. Okay, and this is all we do. And then we similarly compute the attention scores, the mask, the weights, get the context vectors, and so forth. There's one more thing though here, the contiguous view. This is where we are combining the context vectors. So we are now going from number of heads, head to dim. So we do everything separately here. So we have this numHeads and headDim. And now we are combining it again into the out so that we get the concatenated context vector. So if I go back up again, this is essentially performing this concatenation here again. And why am I using contiguous in view? This is just because we kind of fractured the memory at some point. So in PyTorch, the tensors are held in memory. And with all operations, they got a bit fractured. And it didn't allow me to do the view without the contiguous. It was complaining that things are scattered in memory and it's inefficient. And here we are just reorganizing things internally in the computer memory, just for efficiency reasons. And by the way, you don't even have to write it like this. You could also just write, instead of this, you could just write reshape. This doesn't need the contiguous. However, since I used in this book view before and I introduced view in the appendix, I didn't want to overcomplicate things and I was sticking with view. But technically, view and reshape are similar. In PyTorch, except reshape creates a copy. And this one does an in-place modification. Or it doesn't copy anything in memory. So in this case, we need to manually rearrange the memory. But here, by the way, this is a technical detail. You don't really have to think about it too hard. Just maybe consider reshape here. It doesn't really matter. And so that is what is happening. So let's just see, make sure that it still works. So yeah, this works. And by the way, I can also show you what I meant. It's not super interesting, but if I do this, it will complain about this. So we can use either reshape or this contiguous, whatever you prefer. And yeah, that is what I wanted to say here. And why for? This is arbitrary. Technically, here it happened to be like that because each of them, we had two heads and each of them had a dimension of four. But now with our new modification, we are actually even more flexible. So we can also still have an output dimension of two if we want to. So that would be also more flexible. So we can control even the output size more easily. Yeah. And one more thing I should say, I was talking about efficiency here. This is one way to implement this more efficiently. But if you are interested in more bonus material, I know this is very technical now. So you can feel free to stop this video and go to the next chapter if you want to. But if you're curious, I implemented many different types of attention mechanisms and compared them in terms of the efficiency. So let me just go back. Maybe I've clicked on the wrong thing. If I scroll down, chapter 3, comparing efficient multi-head attention implementations here. There we go. So here what I have is I have a notebook where I compare different attention mechanisms. This is based on PyTorch 2.5. Right now PyTorch 2.6 is out and it becomes actually even easier. So here I have, for example, the causal attention multi-head attention wrapper that we implemented first. So this would be corresponding to this one. Next, I have what we've just implemented, the more efficient version. This is what we've just implemented. Then I have another way of implementing it here with combined QKV matrices. This was suggested by a reader. And here what we are doing is instead of having a separate value key and query matrix, we only have one matrix here. So it's a three times as large matrix. So instead of having, let me scroll down here. Instead of having them separate from QKV, we have now one that is essentially three times the size. And then similar to what we've done before, we are going to break it down later after the matrix multiplication. It's a similar concept to what we have done here. And then breaking it down here where we have multiplication with a bigger matrix and then we split it. But now we go even one level further where instead of having three different matrices, we have one big matrix. So that is what you're seeing here. Then I have just for fun because I like Einstein summation. I have an implementation using Einstein summation. It's just a different notation here. So maybe don't worry about it too hard. It's just like a little thing I had fun with. Then here is PyTorch's scaled dot product attention. So this is a PyTorch official implementation of the attention mechanism. So I'm using it right here. So instead of me performing the attention score calculation and so forth, so this is replacing the part down here. So here we are letting PyTorch take care of the attention score and weight computation. Then there's another one. So this one by default, if you have a GPU that supports multi-head attention, sorry, flash attention, this will use flash attention, which is an implementation that is more efficient. Here I have another implementation doing the same thing for benchmark purposes without a flash attention. There is another way to implement this. It's class in PyTorch, the multi-head attention class. So I don't want to go into too much detail here because it might be boring by now. Get another implementation. And there's also PyTorch's new FlexAttention. And so you've seen there are nine different ways of implementing the self-attention mechanism in PyTorch. They should be all mathematically equivalent. And then I benchmark them. So here first on a CPU. So you can see causal attention is slightly slower than the other one, the one we implemented. For some reason, the combined QKV is slower, but we will revisit this in a few moments. My Einstein summation is actually the slowest, but it's fun. And then you can see the PyTorch implementations are actually faster, except for this multi-head attention class. So this was on CPU. I did the same benchmarks on GPU. Let me just scroll down to visualizations here that I have. So you can see all of them compared to each other. Nine different attention mechanisms. I see this should be a nine here. And so what you can see is that the fastest one is using multi-head attention with the PyTorch scaled dot product function. If it's using flash attention. And you can see this one is the slowest FlexAttention. We will revisit this in a few moments. So we can also see the sequential attention class that we implemented in a previous video is slower than this more efficient version that we just implemented in the section. And the QKV combination is slightly slower than this. So this is actually a quite nice implementation we have here, given that it's a from scratch implementation. Of course, PyTorch native one is faster because it's more optimized, but this is actually not too bad on what we have so far. And then I've done the same thing with both the forward and a backward pass. If backward pass is a new term for you, this will be explained more in chapter 5 when we are training the neural network. So here more benchmarking, similar relationship between the numbers. And then I also added compilation. So Torch compile is a nice function to make things faster, to compile a static graph. And here what we can see is nice in the compiled case, the QKV combination is actually slightly faster than ours here. But you can see ours is actually quite nice. It has not a big disadvantage compared to PyTorch native one anymore. It's slightly slower, but the compilation helps optimizing this. And you can also see FlexAttention is now fast. So for FlexAttention, you definitely need to use compile because otherwise it's very slow. So it's meant to be compiled. But yeah, you can see overall our implementation here, what we have this MHA code, which is what we have here is not too bad after all. And this is really a nice readable from scratch implementation where we can really understand what's going on. And yeah, so with that, this chapter was covering the self-attention mechanism. In the next chapter, we will be putting together all the remaining blocks. So we will be coding all the other parts of the LLM. And then in chapter 5, we will be actually training that.
