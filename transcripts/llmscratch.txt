Yeah, welcome back to Chapter 4 of the Supplementary Coding Along series for the Build a Large Language Model from Scratch book. Here in Chapter 4, we are going to implement, finally, the GPT model that we are going to pre-train and fine-tune in upcoming chapters. So this chapter is focused on coding the LLM architecture that we are going to use in upcoming chapters. Just to show you where we are in the grand scheme of things. So before, we already implemented the data preparation and sampling where we had token IDs that we embedded into a multi-dimensional embedding space. And also in the previous chapter, we implemented the attention mechanism. It's essentially the engine of the LLM that does the core computation. Now we are going to complete the architecture. So if you think of the attention mechanism as, let's say, the motor of a car, you can think of this one now as putting on all the remaining parts, the wheels, the steering wheel and everything else around the motor, basically the seats and so forth. So let's do this in a gentle way because it's a big topic to implement a whole LLM architecture. So I think the best way to understand what the architecture looks like is to show you a dummy class. So here this dummy class is coding some placeholders and we will be later on replacing these placeholders by the actual code. I think I also have a figure here somewhere that might be helpful. So just to show you again where we are in this book. So previously, again, we tokenized text, we had the token embeddings, and now we're going to implement this core model here. And in this figure, I see this one. So what you can see here is the GPT model that we are going to implement. And so it receives tokenized text and then the embedding layers, we already coded them in the previous chapter, but they are technically part of the model. So we have these embedding layers, which you can see here. So we have the token embeddings and the positional embeddings. They were covered in chapter two, where we had a mapping from the token IDs to an embedding dimension that we can define. The input size of the embedding layer was the vocabulary size, all the unique words in the training corpus. In the position embeddings, they had the context size. So how many tokens can fit into a context? We had a very simple one, only six tokens, I think, in chapter two. But in reality, this could be, for example, 256,024 and so forth, depending on how much context we want to support in the LLM. And then now we are going to add a few more things. So let me show this also in the context of the figure. So one of these things is the transformer block. So an LLM consists of multiple of these transformer blocks, and each of the transformer blocks has a number of elements that we will talk about later in this chapter. But one of the core elements is the masked multi-head attention module, which we implemented in the previous chapter. And then we are going to add some output layers. And that is essentially how the LLM architecture looks like conceptually. Now going back to our dummy GPT model, this is coding what I've just shown you in the figure. So we have these embedding layers, we have optionally dropout. And then here is this transformer block. And like I just mentioned, it gets repeated a number of times. So here we are going to repeat it a number of times where the number of times is specified by N layers. So CFG stands for, it's just short for configuration. So this is a Python dictionary, as I will show you later, that contains some configuration. Maybe I can actually just add it here so that it's more clear for some reason. Yeah, there we go here. So we have a vocab size. This is the number of unique tokens in the input, also in the training set. So this is based on the GPT-2 tokenizer that we are using from tiktoken. Then we can specify a context length, how long the context is that the LLM can support. Here we are choosing 1024, but yeah, modern LLMs, they can also support longer context sizes. However, it also depends on the hardware, what your hardware supports. So the longer the context size, the more expensive the computation and the more hardware requirements you need. The embedding size, this is the embedding size for the token embeddings. So previously in chapter two, we talked about three-dimensional embeddings. Now we are going to scale this up to a bit larger, 768. The number of heads in the multi-head attention module that we implemented in the previous chapter. Here the number of layers, it's called N layers, but it really refers to the transformer blocks, how many transformer blocks we want to repeat. The dropout rate and whether we want to use the bias in the query key and value linear layers. Yeah, and this is just a configuration file that we are going to pass as a CFG into this dummy model. The reason why I'm calling this CFG is to keep it short so it doesn't go off here in the cell and becomes too long because otherwise it would be harder to read. Yeah, and so we have these transformer blocks. And then we have something called a LayerNorm here. We will get to that later. And then we have this output layer. And so you can see what's interesting is the input layer maps from the vocabulary size into the embedding space. And then the output layer maps back from the embedding space to the vocabulary size. So this will allow us to generate words because what we are doing is we are transforming the token IDs, the words into an intermediate representation, the embedding space, like a vector representation. And then later on, we will transfer them back into this original vocabulary size space back into token IDs, essentially, so that the LLM generates text. So these are the elements. And here in the forward method, we are going to use these elements. So we create the embeddings. We add dropout optionally. Then we apply the transformer blocks. So when I call this one, thanks to the sequential, it will put it through all the different transformer blocks. Yeah, and then we have the output layer here. So if we consider this as our LLM, this looks very simple because we have these placeholders. But just to make this work in the case of the video, before we fill in the placeholder with real code, I have also placeholder code. So you can see for now, I'm calling this the dummy transformer block, and it's really not doing anything. It's just returning the input later. We will fill this block with actual code that will explain what the transformer block does exactly. But as you already have seen here, one part of the transformer block is the masked multi head attention module. So we will add that later on here and a few other things. And then LayerNorm, we will also discuss that separately. It's essentially a normalization layer that gets used both in the transformer block and here, essentially. But these are details for now, just think of it as the big picture concept. And we can already use this, for example, if we have some inputs here. So we are using the tick token tokenizer from chapter two, we are loading it here. And then we create a small, small batch here. So the batch is, I can just show it to you, hope it works, yeah, there we go. So the batch is consisting of two inputs, one and two. And so the first input text is "every effort moves you". And the second input text is "every day holds a" and then so forth. And the task of the LLM is to complete this input text. So we can see this is the first text and we have one, two, three, four tokens. So in this case, each word represents a token. It's not always true. Sometimes a word gets split into multiple tokens. But to keep the example simple, I chose words where each word is exactly one token. Then we are here using the tokenizer to encode it, turn it into a tensor. And so what you're seeing here are the token IDs corresponding to these texts. So this is an input that our LLM will receive during training. And then the task is for the LLM to generate or predict the next word in this text when we are pre-training it. Pre-training will be the topic of the next chapter. So here I'm just explaining what the input and output looks like. So here we are now initializing a new dummy GPT model using the configurations I have specified here. So this would be really, we can also make it more clear. This would be the CFG that you are seeing everywhere here, this dictionary. And then we can already use the model. So the model is initialized with random values for the layers, the embedding layers. And later we will also have the, or we already have a linear layer here. So similar to before, these will be initialized with random values. And those will be trained later when we are optimizing the LLM. Yeah, so that is initializing the model. And then here we are calling the model and the output of the model, they are usually referred to as logits. So it's like a deep learning jargon, but in deep learning, in the field of deep learning, the last layer, the last linear layer that returns the outputs of the linear layer, those values are referred to as logits. I have, I taught statistics classes, I was a statistics faculty a few years back... It feels like a lifetime ago, but this was, there's a lot of theory... like there's a reason why it's called logits. It's something I have also videos for if you're interested, but for now, just refer to it maybe as, or think of it as the last layer outputs before we pass it to any other function. So we have not defined this one, of course. It's a recurring theme here in these videos that I forget to define or execute some cells. So what you can see here is a very large tensor and this tensor is two by four by 50,257. And so the reason for this dimension is, yeah, we have two inputs. So here I can maybe show this for reference, batch dot shape is also, yeah, two rows. So that is where the two comes from. And then we have the four tokens in each column. So here that's the four, but now we have 50,257. So these are the logits for each of the tokens essentially. So this dimension comes from the fact that we have an output layer here that generates this vocab size. So each token, before each token was represented by here in this case, 768 dimensional vector in chapter two, we used a three dimensional vector. So that is what goes in. And then what comes out here is the same as the vocabulary size. So 50,257 dimensional vector. So this sounds like complicated. So how does that turn back into token IDs? So for that, we will actually look at this closer in chapter five. So what we have to do then is we have to optimize the LLM. We have to train the LLM and the LLM will learn that the next token it tries to predict. So if we give it this input, the vocabulary token ID corresponding to this one will have the largest score in this logits vector. And then we extract the largest score, get the token ID, and then we turn it back with our decode method. We replace encode with decode. We replace that into a word. So the token ID to a word, but this is already maybe too much detail because this will be covered in chapter five. For now, just consider we are generating some values here. So we are generating these 50,257 dimensional vectors for each of the token IDs and we will take care of them later once we have trained the LLM. But yeah, I hope so far you have a good understanding at least how the architecture looks like. So that we have this GPT model where it receives tokenized text. Then it goes through embedding layers. We have these transformer blocks. And then we have this output layer that produces these 50,257 dimensional vectors, which are for now just placeholder values. And later on, we will fill in the details. And yeah, in the upcoming videos, we will also fill in the details in terms of what goes exactly on in this transformer block. We are now talking about normalizing activations with layer normalization. So in the grand scheme of things, what we've done before is we introduced this GPT model architecture where we had several ingredients. One was the embedding layers that we introduced in chapter two. We had the masked multi-head attention module, which we talked about in chapter three. And now there are several other things. So we had this dummy GPT placeholder for now, where we said we are going to fill in the blanks in the upcoming sections. And that is what we are going to do now for one of these ingredients. And that is the LayerNorm. So LayerNorm is short for layer normalization. It's a concept that was introduced back in 2016. It's a classic deep learning concept, and it's about normalizing the outputs of a given layer that go into the next layer so that it has a nice property for optimization. And to make this a bit more clear, I think let's take a look at the code examples I've prepared. So I think this will show you exactly what's going on there. And so for the code examples, I'm trying to keep it a bit more conceptual, simple. So we will be using layer normalization in the context of the real LLM. Just for simplicity, though, in this video, I want to use a smaller example. So for that, I'm just setting a random seed, and then I'm making a smaller example here. Let's use torch.randn, which will generate a random sample of two by five here just to keep it small. So just looking at this, it will be two samples here, and each sample has five dimensions. So we have a five dimensional input example, similar to what we have done before in previous chapters. For example, in chapter two, we talked about a token that was embedded in a three dimensional space. Similarly here, just think of it as a token embedded in a five dimensional space just to have some more numbers here. Again, like in reality, we talked about this earlier, the embedding dimensions might be bigger in real LLM cases. So here, 768. Here for simplicity, just let's work with a smaller example so that we can fit it on a figure. Actually, I should have a nice figure here somewhere. So what's happening essentially is that we have these five input values now for a given example. And now what we're trying to do is we are trying to put them through a small neural network, for example, this one here, and then we get output values and we want to make sure that these output values are in a certain value range. So in this case, if we just apply the neural network layer, what we might get is a mean average of point one three across these values and a variance of point three nine, which is actually not too bad. So this might actually be just fine. But if you're familiar with classic deep neural networks, there might be problems like vanishing gradient problems and exploding gradient problems and normalizing the inputs to a given layer. And then essentially also the inputs to the next layer helps with stabilizing the training. So the outputs of a given layer like those, they would go into the next layer in a deep neural network because we have many layers in the network. And if we have, let's say, very small values or very large values, that might be not optimal. It might lead to unstable training. So what we are trying to accomplish here is we're trying to add a certain layer called layer normalization to normalize these outputs that go into the next layer such that they have mean zero and variance one. That is our goal here. So let's consider our inputs here. And then let's also implement a small layer like I've showed you before in the figure. So let's use sequential here. And then let's do Linear, which creates the neural network layer. And we have one, two, three, four, five. We have five dimensions. So the input is five and the output, let's use six to match what we have shown here. So we have this layer here. And then let's actually apply this layer to the batch example. Maybe one more thing we could do technically is adding a non-linearity. So non-linearity could be ReLU, for example, it's a non-linear activation function. And yeah, that is if you have worked with deep neural networks before, usually we pair a linear layer with a non-linear activation function. Because otherwise, if we only have linear layers in a neural network, the sum or the combination of linear layers would be a linear function and that would not be a very useful neural network. So usually we would add non-linearity so that the network can learn more complex things. So let's do this and look at the output here. So what we can see here is, yeah, we get some output values, two examples. So the first one corresponds to this example and this one corresponds to this example transformed now through this mini neural network. So the first row here is really what we are seeing if we focus on one example, what we are seeing here. And so before we start with any layer normalization or normalization in general, let's just take a look at the mean and the variance of this. So we could, for example, call mean here and yeah, if I call just mean like that and we'll see what happens is not what we expect or it depends on what you expect, but here it's not what we want. So here we get one value and this is the average or the mean of this whole thing. But we want actually the mean across this first example. So there are multiple ways we can compute the mean. We can compute this mean of this whole thing, but this would be mixing multiple examples and this is not what we want. The other one we could do is we can do them zero here. And so here, what this one will be is the mean across each of those. So if you consider the mean, this and this would correspond to this one, this and this correspond to this one and so forth. And that's also not what we want because we're again mixing different samples. And this is actually a different normalization scheme called batch normalization. That's what batch normalization does if you're familiar with classic deep learning. But batch normalization is actually not really recommended for LLMs because the way when we train your networks, large networks, and we want to split them across multiple GPUs, for example, one way would be splitting the samples across multiple GPUs and then we would end up with small samples per each GPU. And that is not very good for batch normalization, which is very unstable then and so forth. So that is why, long story short, we're using this concept called layer normalization, where layer normalization is independent of the sample size. It's normalizing across this dimension here. So in order to get this dimension, we use the column dimension one. So this will be then normalizing here, not normalizing, but computing the mean here. So this one, this value is the mean of the first sample across the features. I mentioned I could, by the way, also type minus one here. So to explain a bit more, what we have here is shape two by six. So if I type zero, it will be normalize computing the mean across this first dimension zero. But this is the rows, right? So for each, we would normalize that or compute the mean like that. So what we want is we want the mean across the column dimension, like basically going over the columns here. So for that, we go to the second dimension, so zero, one, and put a one here. And so this is what we're doing. And by the way, we could also write minus one, which would be more general. So for example, if I had an extra dimension here for some reason, this one would still give me the same results, right? But if I write one, now this would give me the, again, mean across this dimension zero, one, which is not what we want, right? So this, just to show you, will be slightly different. What we want is we want the mean across here, the feature dimension. Assuming the feature dimension is always the last dimension, we can just use the more general minus one here. So let me just get rid of this again to make this a bit simpler. Now one more thing is it's still a bit harder to read here. So we have these two values and one corresponds to the first one and one to the second row. We can actually keep the original dimensions, we can do keep them equals true here. And then this one will keep the dimensions. You can see it's these two. So the two dimensions, the two rows, it will keep those, which is a bit nicer. So this would be the mean, let's just call that mean. And we can do the same for the variance here. Let's do this. So again, we can see what we want is a variance of one for nice properties, but we have a different variance. So our goal here is to normalize the data such that this will be the mean of ones and the variance of zeros. So that's basically what we are trying to accomplish here. And so one way we can accomplish this is just step by step by centering around the mean. So if we have an output and we subtract the mean, this will give us the output centered at the mean. So for example, just a simple example, if you consider those consisting of just the value five, five, five, five, five, five, five, five, five. If I subtract the average, which would be in this case, five, five minus five is zero, right? So doing this should give me zero, right? So here what we get is this actually, you can see it's very close to zero. It's not exactly zero. So this is a scientific notation in PyTorch. So this would mean basically zero point nine leading zeros and then seven, four, five. Actually to make this a bit nicer, we can set the print options and then there should be a SI mode. We can set that to false and then it will look hopefully a bit nicer. Yeah. So you can see it's basically zero and zero. So that is what we've done here just to also make this a bit nicer again. And now this is how we center at zero. The other one that we do or what we want to do is we want to also give it a variance of one. So for that, we divide by the standard deviation. So the standard deviation is the square root of the variance. Actually I could go into more detail here why standard deviation, but that would be a whole lecture by itself. I used to be a statistics professor teaching these things. But to keep things simple, maybe just assume this is the formula for normalization subtracting the mean and dividing by the standard deviation. And then so this one should be, let's just call that normed and this one should still be mean zero. Now in addition to that, we should also have, we can just do it here, a variance of one. So that's what we see here. So far so good. So we accomplished that. That is one part of layer normalization. There's a bit more to layer normalization. It's actually a neural network layer that learns parameters. So let me just copy in here the LayerNorm class I implemented from scratch and to show you a bit more what I mean by that. So what we see here is a neural network class and we have an initialization here like the constructor method and there are a couple of things. So one thing here is that we have parameters. And as you maybe remember from chapter three, parameters are trainable neural network weights. So here we have the same sizes and bedding dimension in our case. If we consider this one, this should be a six. So we have six values here and the parameter will be making those six values trainable and we call that the scaling parameter. Similarly, we have a shift parameter and this is zeros. I will get it in a moment why it's ones and zeros. And then may for now ignore the apps. So for now what we have here is exactly what we had before. So what we had before, let me also just comment this out and get back to that later, the unbiased here. So what we had before is the normalization. We had computed the mean and the variance and then we subtracted the mean and divided by the standard deviation. This is exactly. So if I write it like this, this is exactly what we had before the normalization here. Now we are adding one more concept and that is this apps. So what is the apps doing? So this is preventing a division by zero error. So if we have square root of zero, basically zero, and we prevent basically dividing by zero. So we add a small value here. It's just like a small placeholder value. So that's zero point zero zero zero zero one. So I could also just write it like this. And yeah, so that's what we are doing here. So far so good. So we have those things and there's something new now. And there's the scale and the shift here. So to explain what happens, let's maybe tackle it like this. So we have, let's just ignore this for the moment. So what we let's let's do it like this. So because the division comes before, sorry, not correctly. So let's do it like this. It's maybe easier. So what we're doing here is we are subtracting the mean, right? And so here we optionally add some value back. So we subtract the value and then we add some value back. And if we check the original value of shift, that's zeros. So we are essentially adding zero to it, which is similar to not adding anything here, right? So this doesn't have any effect out of the box because it's zeros. However, the network during the training later in chapter five, it might learn certain values that are more beneficial for the normalization. So it might, but it could technically, what might learn is to just undo the mean centering. So if the network finds, well, subtracting the mean is actually not good here. It's not helping me learn, I can just learn shift to be similar to the mean and then I'm undoing the subtraction, right? So if I, what I mean is if I subtract the values five and the network learns the shift to be five, I'm adding back five, then it's if nothing has happened here. So in that case, it gives the network the option to undo this normalization, long story short. And then similarly, we can do the same thing here for this division by the standard deviation. So here the scale, it's not multiplying the value, right? And so by default, this is a one. So by default, we are multiplying it by a one, which doesn't do anything. It's the same as just keeping this, oops, keeping this value like here, like this, right? However, the network... again, it's a parameter, so it might learn different values. It might learn this value here instead of scale, and then it will undo this division basically, right? So it's just giving the option for the network to learn certain values, to transform the values slightly differently. And this is essentially how a LayerNorm layer looks like. And by default, it will be the same as our normalization due to the fact how these ones and zeros are set here. Now, yeah, let's actually try this in practice. Let's see how it works. Let's do a LayerNorm, and then we have an embedding dimension character or value attribute argument, sorry, function argument, method argument. And so we have this, and then we apply it to our, let's do the output here. And yeah, these are our transformed values. Let's actually double check, let's just call them temp, or like just outputs normed, something like that. Okay, and let's just see how they look like again. Now let's double check if they are indeed having the properties that we desired. So that would be mean of one, sorry, mean zero and standard deviation and variance of one. Okay, and let's double check so we can see indeed mean zero and standard deviation of hopefully one,close to one here. So basically one minus rounding errors. The one thing I commented out here is this `unbiased`. So this is just like a minor detail. So there's a, if you're familiar with statistics, population statistics and sample statistics, there's a concept like a Bessel's correction term when you compute the variance or standard deviation that you divide instead of divide by n, the sample size, you divide by n minus one. So `unbiased` is basically `unbiased=True` would be the population statistic, and `unbiased=False` would be the sample statistics. So when we, so this would be a whole again, lecture by itself, I used to be a statistics professor and was teaching these things, but I don't want to turn this into a statistic lecture here. But what we have here is we could technically, let's see if that works, set this to false. You can see the values are slightly different. And that is because we have a very small sample size. So here, if I increase this sample size, make it really large, let's say, it should be then also very large. I should also make this actually large. Okay. Like this. Oh, this is very slow now. Shouldn't have done that, but you should technically see, can I interrupt this? Not responding. I made it maybe too large, apologies. I hope it doesn't crash the recording. But in any case, if you make this really large, you should see this should be one basically. So this should not, this is only making a difference if you have very small values. And in network training, as I mentioned before, we have an embedding dimension of 768. So it shouldn't really make a difference at all, whether we use one or not, what actually worked. Let's maybe try again. You can see here it's now almost one. So the bigger the input is, the less of a difference this correction term makes. And it's really just an implementation detail. It does not matter at all for regular embedding sizes. The reason why I'm setting this explicitly here is that's what the GPT team did when they used TensorFlow to train GPT. And here we are re-implementing things in PyTorch. And PyTorch has different default values than TensorFlow. So that's why I'm just setting this to mimic the same behavior that the original OpenAI GPT team did when or used when they were training the model. Long story short, if this was all a bit confusing with this unbiased setting, you know, it's just an implementation detail. I would not worry about it too much. It's not that important. The main message here is that we are normalizing values so that they have mean zero and variance one, which will give the network nicer properties, nicer gradient properties during the optimization. And this is one building block, the normalization layer in the transformer network, the GPT network here, the LLM. So we are using it in several places. We used it in this place, the final norm here. And we are also going to use it in a transformer block when we are implementing the transformer block later. Anyways, I hope this was useful and explained a bit what layer normalization is. And in the next video, we will tackle some of the other placeholders. Let's now talk about implementing a feed-forward network with GELU activations. Here the feed-forward network is a small neural network contained in the larger neural network, the LLM. And the GELU activations are nonlinear activation functions. So it's essentially a concept in deep learning where we have linear layers and nonlinear activation functions. So just before we go into these details in the grand scheme of things where we are, so our goal is still to implement the final GPT architecture that we want to train in the next chapter. However, the GPT architecture consists of multiple building blocks that we are working through sequentially here. So we already covered layer normalization. So first we coded this dummy architecture. Then we covered layer normalization where we were coding this normalization layer that goes between other neural network layers. And now we are going to code the GELU activation that also goes between linear units or neural network layers. So why do we need nonlinear activations in the first place? I was just digging out an older figure from a university lecture I gave when I was a professor back in the day. So here, this is like a multi-layer perceptron architecture that consists of multiple layers. So we have the inputs, a first hidden layer and a second hidden layer and then the output layer. And so this is a classic multi-layer perceptron that you may have seen in the context of deep learning. And what's not shown here are the nonlinear activation functions. So let's scroll a bit down. So I had it usually as an exam question. What happens if we don't have the nonlinear activation functions? Just briefly, the goal here also in the grand scheme of things of the large language model is to extract features from the input data and the features go through the neural network layers. And at the end of the day, we want to generate outputs. And for the feature extraction, we want the network to learn something useful that helps the network to generate the correct output. Now ideally, this is a toy problem, but if we have something like this, and this is a classification task, has nothing to do with neural networks, sorry, with LLMs, but for illustration purposes, think of this as a challenging classification problem where the network should detect whether something is a square or a circle based on some numeric input. And ideally, the network would learn something like this, like a decision boundary where it can separate these squares from these axes. So they look a bit different from here, but the goal is essentially separating two types of inputs here. And if we don't have these nonlinear activation functions, what happens is essentially that we only have linear layers and the combination of linear layers is a linear function too. So the network will only learn linear functions. And then what it means is the network will never really learn to extract useful information from the input. So it's kind of like limiting the network severely if we don't have nonlinear activation functions. And so nonlinear activation functions, there are a whole bunch of them. So the sigmoid one is a classic one. There's a 10H function, hard 10H, and I think I even have like a fun figure somewhere here. There was even like this meme where someone was like mapping nonlinear activation functions to dance moves and so forth. And so there's a whole bunch of nonlinear activation functions. And the one that we are going to talk about today is not even on here yet. It's called GELU, which is somewhat related to ReLU, but also not quite. And we will see in this video how things are related. So going back to some of the figures here, again, where the nonlinear activation function fits in is if we have a neural network and we have multiple linear layers, the GELU function sits between linear layers. So it's usually a linear layer followed by a nonlinear activation function. You can also see that here where we have a linear layer and then the nonlinear activation function. This whole thing here, a feedforward module, is the module that is later coded in the transformer block. So we haven't talked about the transformer block yet. We will talk about it later, but I can actually maybe already show you the figure so it's a bit more clear where things fit into the grand scheme of things. So this transformer block is part of the LLM, and this gets repeated a number of times. And inside this transformer block, there is the so-called feedforward module. And part of the feedforward module is this setup where we have a linear layer, a nonlinear activation function, and a linear layer. And in this video, we are going to take a look at what this GELU activation function is. So yeah, a lot of background information, I hope. This is not too confusing. Essentially, we are just coding a small little function inside this whole thing. And the GELU function goes back to a paper called Gaussian Error Linear Units. And here in this paper, they proposed a function that is essentially a bit better than the ReLU for optimization purposes and so forth. And one more thing here. So the ReLU function, oh, that's actually a nice figure here, how it relates to the GELU function. So the ReLU function is a function that is maybe the simplest nonlinear activation function. What it is doing is if the negative, so if the inputs here on the x-axis, if they are negative, they are thresholded to zero. So the y-axis is the output, the x-axis are the inputs. If the inputs are negative or below zero, the output here is zero. Otherwise, it will maintain the input. So if the input is one, the output is one. If the input is two, the output is two and so forth. And the GELU is very similar. You can see it's slightly different. It has some beneficial mathematical properties for normalization or for optimization during the training. So if you're interested, you can read through this paper. But it's not super essential. You can think of it just as a nonlinear activation function, one of the many nonlinear activation functions. You can actually train also the LLM with other types of nonlinear activation functions. It's just a choice that the original developers of the GPT model made. And in fact, later LLMs, for example, the Lama models, they used something called a Swiglu activation function, which is a gated unit with, I think, Silu activation function. So it's just like a choice some people make. So it's not super crucial. The only thing that is crucial is that we use a nonlinear activation function. Which one? Well, it depends. It might sometimes work better than others, but I wouldn't get too serious about which type to use here. One thing about the GELU activation function is that it comes actually in two flavors. So this is the mathematical formulation of the GELU function. But the original GPT model, it was implemented in TensorFlow. And they used this approximation here. So this mathematical formula. I don't know 100% because I haven't talked to the authors of the original GPT architecture, but I can assume, or I would assume that this is probably due to efficiency reasons that they use this approximation. So it's approximately the same, but it's approximated and is maybe a bit faster. Again, it doesn't really matter in the grand scheme of things. But because we want to mimic the original GPT architecture, we are also going to implement this version here in PyTorch. So this was like a lot of build up, a lot of background information. So let's actually get to the code. And I think once you see the code, you will feel like, OK, this is actually much simpler than you thought compared to the paper and everything. So here, what I copy and pasted here is my re-implementation of the approximate GELU function. And you can see it's a very similar module, has a forward method, and it receives some input. And then it applies this formula, it multiplies the input by 0.5, and then multiplies it by 1 plus this 10h, and so forth. And so this is just a mathematical approximation of the GELU function as described in that paper. And yeah, if we use that, let me just also copy that code here, we can visualize it. I'm using Matplotlib here. And I'm going to apply to some sample data in the range between minus 3 and 3. So the input, going back to the paper, I'm just testing it between minus 3 and 3. That's what I'm doing here. And also for completeness, I'm comparing it to the ReLU function in PyTorch. And here, it's just plotting code. So let's see how that looks like. Yeah, so maybe I should maybe interrupt this. I think, of course, I forgot to execute this code cell, I think it should be pretty fast. I think my computer is still suffering a bit because in the previous video, I executed this large matrix multiplication, so I should probably restart it at some point. But yeah, we can see the figure. So we have minus 3 to 3 here as the input. And then you can see the output here. And yeah, it is ultimately same idea as the ReLU function except it's a bit smoother here, which has nicer properties during optimization when we compute the gradients. And then just to go back to the chapter here. So I talked about this little unit that we will need actually an upcoming section when we implement the transformer block. And so we are going to implement the code for this. I think it was further up here. So we're going to implement this one. So let's do this. And this will be something we are going to use later. So what is happening here? So we have a feed forward module, and we have a sequential here, which means it will in the forward method, it will execute everything that is inside the sequential in a sequential fashion. We have a linear layer. So this goes from the embedding dimension. And if we go back up here, the embedding dimension was 768. So it goes from 768 to four times 768. So we have 768 to four times 768. And so this should be something with I think it's 3100, let me just calculate it, 3072. So it's 3072. Let's just put it here. And then it goes from 3072 back to 768 here. So this is essentially a network that is the opposite shape of an hourglass, where we go instead of an hourglass, we go from large to small to large. Here we go from small to large to small. I do think I have a figure here. So here you can think of it as this. We have the small embedding dimension. We make it four times larger, and then we make it small again. Why do we do that? So this is, you know, it's like a trick almost in deep neural networks where this adds a lot of parameters and these parameters learn to extract some information from the inputs. It's essentially can think of it as massaging the input to, you know, get to extract information for it, that then during the optimization, when the matrices are optimized, help the Yeah, learn to extract relevant information and do something useful with that. And in this case, in the case of LLMs, it's correctly generating the next word in the text or input data. Yeah. And this is our mini neural network that we are going to use in Chapter 5, sorry, in the transformer block, but then also in the LLM that we are going to train in Chapter 5. We can initialize it. So let's go here and do FFN and then let's do feed forward, oops, maybe execute this, of course. It's a running gag by now, I think. So we can use the, so we have the CFG, which stands for configuration, which we have defined up here. So this is our configuration here. So let's just use that and put it in here. And that is our mini neural network. And then we can try it on some sample data. So we can just for simplicity, create some random data again. So let's do two by three by 768. So a batch of two input samples, each one has three tokens. And then each token embedding is 768 dimensional. It's just a sample. Later we will work with real data. But yeah, just to show you what happens. So here we get the same output shape. And that is because we have this input dimension that is also generated here. So immediately there is a larger value of four times as large, the 3000. But what we get out is still 768 because we map from here to larger and then from larger back again. And why is it four times? So this is again a hyperparameter. So it could be as well be, you know, it could be two, it could be eight, it could be one, it could be anything. This is something the original authors of the GPT paper chose. And again, like I mentioned before, we want to mimic the original GPT architecture. And also why I'm so hung up on the GPT architecture, why we want to mimic it exactly, is that later in chapter five, we will download the available weights that OpenAI shared on the website and then we will log that into our code here. And so there are weight parameters. So if I go here and then let's say layers, right? So this one, you can see there are these weight parameters. So when we have this one, this is the first linear layer, and then we have the weights here. These are currently random weights, but later we will train them and we will also load the original OpenAI weights. And we have to have the exact same dimension that they have if we want to load their weights into our model. So we have to match these things exactly so that the weights are compatible. And that's why I'm trying to really make sure everything else is the same. We use the same hyperparameter here. We use the same approximation here that they used in TensorFlow and so forth. So that's why it's important for me to match these things. And in this section, now you saw the GELU function. In the next video, we will tackle another building block of the LLM architecture. In this section, we are going to talk about adding shortcut connections. So, so far, our goal is still to implement this final GPT architecture. We started off with this GPT backbone, this dummy GPT placeholder code, and then we filled in individual building blocks. For example, in the beginning, the layer normalization, and then in the previous section, we talked about the GELU nonlinear activation function, and we implemented this small feedforward neural network module that we are going to use in the transformer block. However, before we get to this transformer block here, there's one more thing we want to implement, and that is the shortcut connections here. So shortcut connections, they go back to a paper called Deep Residual Learning for Image Recognition, and here the authors implemented a convolutional neural network back then, and they proposed these shortcut connections, or sometimes also called residual connections. So to show you what those are, here's maybe a nice figure. So what they do is, so just to orient ourselves here, what we are looking at is like this, something similar to what we have just coded in the previous section, this little neural network module where you have a weight layer, essentially the NN linear layer, a nonlinear activation function, ReLU or GELU, and then another linear layer. And so this is actually similar to what we are looking at here. Now, what they added is this arrow here. So what they have is they have f of x, where you can think of this here as f of x. So f of x is really this part. It's a function where the input goes through a linear layer, a nonlinear activation, and a linear layer. So you can think of it as a function, and then you add back the input. So input is x, the original input, you add it back to the result of this one here. And so why is this being done? What's the point here? So if you have a very deep neural network, you can have problems like exporting gradients or vanishing gradients. And so, for example, if this doesn't learn anything useful, or let's say it goes to zero due to the vanishing gradient problem, the network can still use this shortcut. So the shortcut, it doesn't diminish anything. rescuing the network. If this is not, you're learning anything useful. So in that sense, you maintain the gradient signal, you maintain a signal during the training to help the network learn something. Otherwise, if you have a very deep neural network, there might be no signal anymore. And then you have a vanishing gradient problem or other problems. And then the network will fail if one of these submodules fails. Because in a network, everything is sequential, these layers are sequential, and they depend on each other. And so if one of them does something that's nonsense, then all the other ones will suffer, all the other ones that come below here. So here, as a backdoor, you can kind of learn, or the network can rely on this to skip these layers if they are not useful. It's essentially like a little backdoor here, you can think of it like that. Let's see how that works out in code. So I have also a figure here where you can see a deep neural network with many layers. And we are looking at the gradients here. And you can see the gradients are relatively small. So in neural network optimization, we use an algorithm called backpropagation. We will use that in chapter five. In this book, we're not really going over the fundamentals of deep learning, because this would be another 500 page book. So here we are really laser focused on LMS. But yeah, some of these deep neural network concepts are relevant. So in this deep neural network, it's also a convention to draw neural networks from bottom to top. So this is why sometimes when you're looking at these diagrams, why is this flipped around? This is really a convention in deep learning where people tend to draw these diagrams from the bottom to the top. So when we follow the inputs here from the bottom to the top, the optimization in the backpropagation algorithm, it goes the other way around. So you start with the output. There's a loss function that's not shown here that is usually used. And then you back propagate. That's why it's called backpropagation. You go backwards. And yeah, you can see the gradient signal becomes weaker. So you have a 0.005, 0.0013, and it becomes weaker. And that makes it harder to learn for the network to learn how to update these weight parameters in these linear layers. And here on the right hand side, you see the same architecture, but I added shortcut connections. And you can see here, so by the way, I'm not making these numbers up. We have actually, I have code. We will look at this in a few moments. What you can see is when I add these shortcut connections, you can see that we have much larger gradient values. We have here a 1.32 and then 0.26, 0.32, 0.2, 0.2. So we have much larger values and that helps the network to train better because there's more information for updating these weights here. So let's actually take a look at the code. And let me show you that this is not just a concept, that it actually works. So I just copy-pasted this here. This is a neural network where we have one, two, three, four, five of these modules, sequential modules, and each module consists of a linear layer and a nonlinear activation function. So what you're looking at here is the same thing that you can see here. So one, two, three, four, five. You can see one, two, three, four, five. And then the forward method is computed by calling these layers. So we iterate over the layers here and we're calling them. And then optionally, we add this shortcut connection. So I'm saying optionally because just for experimentation purposes, I added an argument here, use shortcut that we can set to true or false, whether to use the shortcut or not. So if we are not using the shortcut, it's just like a regular neural network without shortcut. And if we have this shortcut, then it will add these little shortcut connections. And similar to the paper, what will happen is it will add the input back to the output of the module. So that's what you're seeing here. Oh, sorry. What you're seeing here and here, that's without shortcut connection. There's one exception here. If X shape equals layer output shape. So this is essentially when we are, it's just one way, a simple way how I implemented this. Once we are here at the end, we won't add a shortcut connection because yeah, that wouldn't work here, right? So there's no point adding a shortcut connection to the output. Yeah. So this is it. And then I also have a utility function, print gradients, where we get the output. We have some target. So the target really is a placeholder here. Yeah, how deep neural network training works, we usually calculate a loss function, and then we compute the difference between the value that the network outputs and the target in the loss function. That is like a measure of, let's say, how similar they are. And based on that, we back propagate. Now later on in chapter five, the targets will be the next tokens we want to generate. And the loss function will be across entropy loss. For now, you know, don't worry about these concepts too much. They are just placeholders here. MSE loss is essentially just the squared difference, the mean squared error loss, just for simplicity, so that we have something to back propagate. It's just, yeah, we need any loss function here and just want to keep it simple and self-contained. Has nothing to do with tokens now. This will come later in chapter five. Okay. So here, let's execute that. And then, of course, we need some values. So for the layer sizes, let's do three, three, three, three, and one. The one here, the output size is one, because what we want is something that has the same dimension as the target. So what we want is our network to produce a value that is close as possible to the target. And then we optimize it and so forth. Here we don't really care about the optimization. We just want to compute the gradients. Then let's do some sample input. So the input is input size is three. So we need a sample input that also has three values, just, you know, for example, one zero white minus one. Why have I chosen those? Just randomly, just something that came to mind. You can try out different values. And yeah. And then let's do a random seed here. So the random seed is so that you get the same results that I get. And then let's first initialize our network here. So this is our example network with out shortcut connections. So this is printing the gradients without shortcut connections. And you can see these are very small values. So the values in the figure are actually based on the code here. So you can see those kind of match. And then we can do the same thing with shortcut connections. Yeah. And you can see now the values are much larger. So with these shortcut connections, we lift up these gradient values. The network later on, it will also, it will be more than just lifting up the gradient values here in the beginning. It will also, you know, learn how to optimize, optimize these weight matrices. And if there's something that goes wrong during the training, these shortcut connections can always compensate and rescue the network there from totally failing. Because yeah, one more time, you can see everything is dependent on each other. So if you have something that doesn't really work here, the later layers in the forward pass, they are not affected. And likewise, if we do backpropagation, if something fails here, this layer is not affected. It can learn how to skip here. And that's the whole idea behind shortcut connections. Now we are ready to code the transformer block here. So the transformer block combines multiple modules, for example, the attention module, the multi-head attention module we coded earlier in chapter three. And then it will also have a bunch of linear layers where for the linear layers, we also introduced several concepts, for example, layer normalization, GELU activations, and then also the shortcut connections. So going into this figure here again, what we have done so far in this chapter is we coded the GPT backbone, like the placeholder architecture. And then we were filling in the blanks, the placeholders, for example, layer normalization, the GELU activation, the feed forward module, and then also the shortcut connections in the previous section. Now we have all the building blocks here to code the transformer block, which is a very essential core part of the GPT architecture. So the name transformer block, it's derived from the original transformer architecture proposed in the attention is all you need paper. And if we go to the paper, there is this very influential figure that you might have seen before. We already talked about this briefly in a previous section. So there's an encoder sub module and a decoder module. And the decoder module is what's used in modern LLMs and GPT like LLMs. So the encoder module, you can forget about it for now. It's not used in modern LLMs like GPT. It only focuses on this right sub part here for text generation. And there is this gray block here, and this is the so-called transformer block. So to show it to you in a different figure, maybe that also helps. Here we have the whole GPT model. And yeah, there are several aspects to it. We have the tokenized text that goes into the model. For example, every effort moves you as the input text. There are embedding layers, the token and positional embedding layers in the GPT model. And then we already coded the masked multi-head attention module. Now the masked multi-head attention module is part of this bigger transformer block that combines some other aspects. And here it looks like there's only one transformer block. But as we will see also later, the transformer block is repeated a number of times in this GPT model. So it's actually a very important model or module. And the transformer block takes up most of the LLM. It's repeated a lot of times, and it's a really important aspect of the LLM. And in this section, we are going to look inside what's contained in this transformer block. So let's go to section 4.5 here. And here we see such a transformer block where it receives embeddings of token embeddings. For example, we have input. Every effort moves you four tokens, and they get encoded into 768 dimensional token embeddings, as we talked about earlier. And these go into this transformer block here. And in the transformer block, we have a layer normalization layer. We have the masked multi-head attention module from the previous chapter. We have dropout optionally. And then here we also, you can see we have the shortcut connections. So this is like one sub-module. And then we have another LayerNorm. We have this feed-forward module, where the feed-forward module consists of a linear layer, a GELU activation, and another linear layer. And then optionally, we also have dropout again here. And then this feeds to the next transformer block a number of times. So there are usually in a LLM, there are multiple of these blocks following after each other. But we will get to that in more detail in the next section. Here in this section, we are just concerned with coding this transformer block. So let's get to it and see how we can implement it in code. So for that, let me make a class transformer block, and we are going to use again an n.module from PyTorch. And let me just actually copy and paste some of the code so it's a bit faster, because otherwise the videos are always so long. So here we have the constructor. It again receives our configuration file. Then we have the multi-head attention module from the previous chapter. So I should ideally import it from the previous chapter here. So to make this work, you would have to go in here, make a new file, for example, and call this previous chapters. And in this file, we need to have this multi-head attention code from the previous chapter, otherwise it won't work. So let me copy and paste the code inside here. So this is the code from the previous chapter, where I have just copy and pasted some of the functions we have defined, the data set from chapter two, but then also most importantly, the multi-head attention module from chapter three, because we have to save this because we are going to import this relative to this notebook. So it's a file, previous chapters that contains this multi-head attention class. Okay, so let's work with this now. And let me briefly discuss what we are looking at here. So we have the multi-head attention class, and it is using the configuration file for the input embeddings, embed them, that's 768. The output dimensions, they're also 768 in our case. So this is what we defined up here at the beginning of this chapter. Now we define a context length, how long our context is allowed to be. Then we define the number of heads, whether we want to use dropout or not. So the dropout rate and the QKB bias, whether we want to use the bias unit in the linear layers. So this is all things we covered in chapter three, so I don't want to go into too much detail here. More interestingly, we have now this feedforward module, which we defined in a previous section. And then we also have two places where we have LayerNorm. So the reason why we instantiate two separate LayerNorm objects is that they have trainable parameters and we are using them at two different places. And in both places, we want the network to optimize the parameter. So we won't share the parameters. We won't be like one module that is used in two places, it will be two distinct layer normalization modules. And then the dropout for another place. So there are multiple places where we use dropout. I think it will become more clear once I implement the forward method. Let's do it like this. And in the forward method, one thing that we need is, of course, the input, input X, and then let me also copy the code here. So it's a bit faster in the video. So here, what we are looking at is how we are using the things we have defined here in the transformer block. And this will be mimicking what you are seeing here. We have a LayerNorm, multi-head attention, optionally dropout. We have the shortcut connection here, another LayerNorm, then the feedforward module, and then optionally dropout again, and then a shortcut connection. So this is the first LayerNorm here. So first, I should say we are saving this shortcut connection because we are going to add this later. So this is what we are creating here, this first shortcut. Then we have LayerNorm one, which you can see here. Then we have the attention, which corresponds to this one, the dropout for this shortcut connection here. I'm just calling this shortcut connection because before it goes in here, you can also give it a different name. It's just something I came up with, not very creative here. And then we are adding this shortcut connection back. So this part corresponds to this first block here that you can see inside the transformer block. Now, next, we have another LayerNorm. So the LayerNorm is here. Again, before that, we save the shortcut connection. The shortcut connection you can see here, right? So then we have a second LayerNorm, then the feedforward module, which you can see here. This is calling the feedforward module. And then we have another dropout here, and then we add back the shortcut connection. And this is essentially what you are seeing here. This is the transformer block. And to be honest, there's not much really that goes on inside here. It is essentially just encapsulating multiple of these ideas. And the reason why we even create this transformer block and why we don't add this directly to the LLM is so that we can reuse that. So if I go up again to what we have done in the beginning of this chapter, so you can see we have these embedding layers, then we have dropout defined here. Maybe ignore that for now. What's interesting is here that we are repeating the transformer block. So there is an argument here or a value we define as n layers, and here we have 12 layers. So that means we are repeating this transformer block 12 times. And then we have a final LayerNorm and output layer. But so that we can make this code very concise, we encapsulate this in this transformer block class, which you can see here, which we have just coded. So let's just make sure that it works. So I executed the cell, not making the mistake I'm usually making. And then we are trying it out in practice. I'm using a random seed again so that you get the same results that I do. And then let's do a small, small example. Let's do two examples with four tokens each. So this would be something similar to here, where we have one, two, three, four tokens. And let's just assume they are embedded in a 768-dimensional space, which corresponds to what we have defined here. We will be working with real data soon. So for this part, just to keep it small and self-contained, I'm using this random input. But we will be working with real data soon in chapter five, so not too far away. And then we define the transformer block. So we do a transformer block, and then we provide the configuration file that we had at the top of this notebook. And then we can use it. We can do output equals block, and then we have the input here. And that's essentially it. So I can execute this and just take a look at the input shape. So the input shape is 24768, which makes sense. And the output shape should be similar. So here, take a look at the output shape as the similar size. And why is that important? That's because we have multiple of these transformer blocks, as I mentioned before. And so if we want the next transformer block to be compatible with this transformer block, the input has to be the same size as the output. Otherwise, we will have some complaints in the feedforward network if suddenly the input dimension changes. So yeah, that is all there is for now about the transformer block. And in the next section, we will put everything together into the GPT model architecture. Now we have all the building blocks together to code the GPT model architecture. Before we jump into the code, let me just show you a visual showing you the whole GPT model architecture in one figure. So here, what you can see is the GPT model architecture. And there are, of course, familiar parts to it. The input text that is tokenized and then embedded with the token embedding layers and the position embedding layer. And then we have an optional dropout in the center here, the box here. That is the transformer block that we have just implemented in the previous section. And you can see it's repeated 12 times. Why 12? That's a design decision. You as the designer, you can choose this number. But I'm using 12 here because that's what the original GPT team used for their smallest model. But I will also show you later that we can change this number and have differently sized GPT models. Yeah. And in the transformer block, you see all the familiar parts that we implemented in the previous section. Then we have a LayerNorm and an output layer. Now what's interesting is that the output layer doesn't output words. It's outputting numbers. So what do these numbers mean? If you pay close attention, you might see that there are four input tokens and there are four rows here. So each row here corresponds to one of the input tokens. However, instead of being a word, you can see this is a vector here. So each token is represented by a vector. And this vector is 50,257 dimensional. Where do these dimensions come from? So here in the input layer, what we do is we convert a token ID that corresponds to a 50,257 entry vocabulary into an embedding vector. So we go from this huge vocabulary space into an embedding space. And here at the output layer, we go back from an embedding space into the vocabulary space. So there's one more thing that we have to do to convert these vectors back into words, but we will leave that part for the next section here. We're just focused on coding the architecture itself. So yeah, let's jump in and let's get working here on the architecture. And because I'm lazy, or maybe because I thought it all through, it depends on how you wouldn't want to think of me, I will just copy and paste this dummy GPT model class that we implemented earlier. So let me just copy and paste this to make my life here a bit easier. So yeah, I'm copying and pasting it here. And then what we can do is we can replace all the dummy parts, because now we are going to implement the real architecture, can just delete those. We get rid of placeholder because it's no longer placeholder, because our dummy transformer block is now the real transformer block. And then I can do the same thing here for the LayerNorm. And if I see it correctly, we don't have any other placeholders here anymore. So this one should be the working GPT model architecture. We can actually try it out. So let me initialize a random seed here so you get the same results I do. And then we can instantiate the model. So I'm going to initialize it with the configuration file we had earlier at the beginning of this chapter. And let's actually use some real data now. So we had somewhere real data. Let me scroll up a bit here. So here, we defined batch as two inputs, input text that were embedded into token embeddings. Let me just copy and paste here so that we can make sure this actually still works. So yeah, we have batch two inputs, and we can then compute the output here, batch. And then we can double check maybe the input. This should be two times four, right? Oops. Should be two times four, two examples with four tokens. And then output should be two times four times the vocabulary size. So let's double check. So two times four, and then each of the tokens is embedded in a 50,257 dimensional space. And we will take care of converting this in the next video. For now, I wanted to show you one more thing. So here, I have this file, sorry, the dictionary that I defined at the beginning of this chapter. And I gave it this naming 124M, which stands for 124 million parameters. And so where does this number come from? We can actually calculate the number of parameters. And in PyTorch, there is a function or method called number of elements here, number of elements. So what we can do is we can compute, for example, the number of elements in batch. And this should be eight, right? We have two times four elements. So here, this is just a convenient way to compute how many values are in a given tensor. So we can use that to sum over all the elements in the model. So for p in model parameters, so parameters are all the weight matrices, for example, and bias vectors that are in the model, all the trainable things that we have, basically. That's including the QKV matrices, but also the LayerNorm and everything. So yeah, and then we can print this out. I made a typo here, should be number. And yeah, you can see it's a large number. To maybe show this a bit easier, let's use f string. Let's see if I get this right. F strings, if I don't use them in a while, I usually forget the syntax. But I think if I add a comma here, yeah. So we can see it's 163 million parameters that we have here. And so how does this number match with 124? I just told you we have 124 million parameters. And there's one thing I have not explained yet. So there is in the original GPT model architecture, some weight sharing going on. So I told you before that this one maps from the vocabulary space into the embedding space. And then this one goes the other way around. It maps back from the embedding space into the vocabulary space. So what we can do is we can take a look at these weight matrices. So we could type model and then what is it? Token embedding. So let's take a look at the weight matrix and see how big it is. So we can see it's 50,257 times 768. And if we look at the output here, let me copy this one and then I will place this with head. This has exactly the same size. So one trick, because let's say we assume this is learning a good mapping, we're mapping from vocabulary space into embedding space. And we might assume that the same in reverse might work well. So mapping from embedding space to vocabulary space, we could actually share the same weights. So we are not doing it here for simplicity, but we could technically reuse the same weights in the output head here because they have the same dimensions. And it's actually a design decision. Some people do that. Some people don't. If you share the weights, yeah, you have a fewer parameters in the model because these are really large matrices. But there are architectures or many, many architectures that don't do that. So it really depends. So LAMA models by MetaAI, for example, LAMA 3 and 3.1, they don't do the weight sharing, but 3.2 is doing the weight sharing. It's really almost arbitrary. It depends if you want to save on parameters, you can share the weights. But otherwise, in my personal experience, it's not always leading to better results. So it's really like a trade off between parameter size and better training dynamics. But it really, really depends on the architecture. But yeah, what I wanted to tell you is that the number of parameters here, how it's usually computed for GPD2 models, because they usually originally use the weight sharing, is without counting it twice. So what I mean is here, we counted once and we counted twice here. So sorry, let's focus on those here and here. If those are shared, we are over counting. So what we could do is we could subtract, let's maybe do it here. We can subtract this one here. Let's see if this works. So you will get the 124 million that is usually quoted when people talk about GPD2 models. I also have the paper open here. So here in this paper, this is the original GPD2 paper. They should have a table somewhere, let me see, where they talk about the model sizes right here. So here they have four different model sizes. And this is the one we just implemented with the 12 layers and the 768 dimensional embeddings. You might notice this is not 124. And this is because they had a calculation error, I think. So I think they talked about it somewhere, I think maybe in the code repository. But they haven't updated the paper, which is why the number looks a bit off. But it should be 124 million. And they also have two different sizes here. And I'm maybe leaving this as an exercise for you, the viewer. So to implement those, actually, it should be relatively simple. Once you have the architecture coded, so you could copy and paste this here and then change some of the values here to make the architecture larger, to implement the other ones you are seeing here in this table. I should tell you the table doesn't tell you the whole story. It talks about the number of layers and the embedding dimensions. So here are the layers and the embedding dimension, where layers is maybe a bit unfortunate as the term, because this really refers to the number of times the transformer block is repeated. So it's used right here. So it's not necessarily other layers. It's really specific to the transformer block. So you could also maybe just call it number of transformer blocks to be more clear or n blocks or something like that. But yeah, I'm sticking here with n layers because that's what's used in this paper and what's a convention nowadays in LLM terminology. But there's one more thing missing. And also the number of heads is different. So they didn't describe that in the paper, but you can see that actually when you download the model weights and we will be doing that in chapter five, that they also changed the number of heads. And for your convenience, I will just copy and paste these numbers here. So here I have the four different model sizes, GPT is too small, which we just implemented 12 layers, 12 heads and 768 dimensions for the embeddings. And then medium, large and XL correspond to these three models. And yeah, as an exercise, I encourage you to try out these models. You only have to change this vocabulary, so the dictionary here, and then you can reinitialize different sized models. And you can also compute the number of parameters, the size of the parameters, sorry, the number of the parameters and the size of the model using this code. And yeah, just find out how big these models are as an optional exercise. And then in the next video, I will go over how we convert the output back into words. Let's now talk about how we can use the GPT model to generate actual text. So in the previous section, we implemented this GPT model architecture, and we have seen that it outputs this tensor. So it doesn't look like text at all yet. So there's something else we have to do now. So one thing I mentioned before is that if we have four input tokens, we get a tensor here consisting of four rows, but each row is 50,257 dimensional. So how do we get these 50,257 dimensions back into words? That's the focus of this section. And so one thing before we get there, I wanted to mention is LLMs generate one word at a time. So if we have an input, hello, I am, it might generate the next word, a, and then we concatenate the a back to the input, hello, I'm a, and then in the next round, it generates the next word and so forth. So it's actually an iterative process where we are always generating one word or one token, and then we append this token to the input and we put the input again through the LLM and so forth. And if you have used a web application like chat GPT, for example, if you type in a query and you pay close attention to the output, you might see there's one token or word appearing at a time when it generates the output. And that's because it's an iterative process. It's not generating the whole response all at once. It's doing it step by step. And so how does that work? Here is an overview. It's maybe very technical, but we will get through this, I promise you. So what you are seeing here is again, the input, hello, I am. And for now, please ignore anything below the dashed line. So the input might be hello, I am. And then similar to what we have done or learned in chapter two, first, we convert the tokens from a string representation into a token ID, into an integer representation. So we are converting text into token IDs. And then we put these token IDs into GPT, the GPT model we just coded in the previous section. And then like we've seen, it outputs this tensor here and it has four rows and each of the rows corresponds to one of the input words here. And if you recall a long time ago, but if you recall from chapter two, the way we set up our data loader was that the targets, the inputs shifted by one position. So the way we are going to train the LLM is that for each token, it will associate the next token because of the plus one shifting that we implemented in the targets. So what that ultimately means is at each position, this vector will contain information about the next position, about the plus one position. And so that means that the last here, the last vector, which corresponds to the last vector in the input will contain information about the next word we want to generate. So the last row contains the information we need for the next word, but we need to now convert this vector back into the, yeah, into the word that we want to generate. How does that work? One thing I wanted to mention is this last row in general, these last rows here, they are also called logits. It's not a LLM term. It's a general deep learning term. I don't want to go into too much detail why they are called logits. That would go into a statistics lecture here. But in general, it's a convention to call the output of the last linear layer. So the output of the last linear layer in a neural network is usually called the logits. So the values that come out of this linear layer here. So we call them logits. And so here we are looking at the logits of the last token. So we are just, you know, copy pasting. If you pay attention, these are the same values we just copy and pasting here in this figure, the last row, which we call logits. And then we apply the softmax function to normalize them. So they range from minus infinity to infinity. So it could be very negative small values to very large values. And we convert them into probabilities, which are all strictly positive and sum up to one. And by the way, this is an optional step. This is not necessary. You can actually skip this step because the smallest value always corresponds to the smallest probability and the largest value always corresponds to the largest probability. So there is no need for the softmax, but it makes it a bit easier to interpret. And so what we're looking at is the normalized logits as probabilities. And we are interested in the so maybe one step back. I should say again, this is 50,257 dimensional. So we have 50,257 values here and each value corresponds to a token ID in the vocabulary. And now our goal is to train the network such that the correct next token that we want to generate has the highest probability. We are not training anything here. This will be done in the next chapter. But then one, let's assume we have a well-trained GPT model. It will associate the token we want to generate with the highest probability. So let's just assume this is a trained model and it will have the highest probability in position 257. So this is the first position, position zero. This is the last position, position 50,257 and everything else is dotted out. So otherwise it won't fit. And this model happens to put the largest probability score here into this position, 257. And if we take our tokenizer from chapter two and we use tokenizer.decode, put in the token ID 257, it will generate the next token A. So this is the desired token we want to generate. Now how does this actually or maybe one more thing here is we then take this generated token and append it to the input for the next round. So this is only showing you one round. And like I mentioned before, we do this in an iterative fashion as long as we want to generate more text. So every time we generate the next token, it's added to the input. And then we put this whole input again through this pipeline and so forth. But yeah, to make it maybe more clear, how does that actually work out in code? So let me go to the notebook and let me code this. So we can define a function, call it generate, let's say text simple. Why simple? Because we will be implementing more sophisticated versions of that in later chapters that contain some additional sampling procedures. But the simplest version, which is the one I showed you in the figure, works as follows. So we have the model that we pass to our function. We have the text, let's call them IDX index tokens. And then we pass the number of max new tokens we want to generate. We have to define a number, otherwise it will be infinitely generating more tokens. And then also the supported context size that the LLM supports. And then we have a for loop. So the for loop is for how many tokens we want to generate. And then we keep generating. And so here the goal at the end, so let's dot this out for the moment. And then we will return the modified index. So here we pass in the index, then here we keep appending to the index, and then we return it so that is similar to what you're seeing here. In the beginning, in the first round, we have this input, and then we generate the next word A and we add it to the input and so forth in an iterative fashion. Maybe this is a bit abstract, so what is index? So index correspond to the token IDs or token index positions, because the token ID corresponds to the index position in this tensor. So index position means 0, 1, 2, 3, 4, 5, 6, 7, up to 50,257. And that's the same as the token ID. So the token ID corresponds to the index position. So if I say token ID or index here in this case means the same thing because we're talking about the index position of this probability tensor. Okay, so just to have something to work with, we have this start context, just the first text we start with. We encode this with our tokenizer into token IDs. So let me just show it here. So these are our token IDs, and you can also call them indexes. And then we also, because our model only works with pytorch tensors, we have to convert this list into a pytorch tensor. So can we just show it to you? So this is our pytorch tensor, and I added the batch dimension here. So with this unsqueeze, I just added an additional dimension. So you can see this is now a 1 by 4 dimensional one. So our model expects batches. So we only have a batch size of one, but it's just for compatibility reasons. And then we can use that model later for training. Okay, now let's fill in the interesting part here. So we are going to code now our token generation. So first we're going to truncate. So what we're doing here is we are truncating it up to a size that the model supports. In our case, if we have something like this, that's not an issue. It's just four tokens, and our model supports, what have we defined, up to 1,024 tokens. But let's say we copy and paste a large document, you know, 50,000 tokens, so that the model doesn't crash. We just truncate it and pass only the first 1,024 tokens. It's just like a sanity check. And then let me double check if I have one, two, three, four, oops, one, two, three, four, then I just have the right spacing because it looked a bit off. It's just a Python convention. And then we compute the logits of the model, of the sorry, of the inputs. So this is corresponding to this one here. In PyTorch, though, one more thing I should add is no grad context. So in PyTorch, by default, it is always building this computation graph of the model in the background that is used for the backpropagation algorithm, the training algorithm. And if we don't train the model, this is very inefficient. So here I'm suppressing the generation of this gradient computation, the computation graph, because we are not training here, we're just using the model. So it's just a good practice to save memory, so it's less compute intensive. And then what we are doing is we are grabbing the first, sorry, the last row. So here what I'm doing is I'm just getting the last row of the logits. This is what we're doing, we're extracting the last row. Okay, it's just like this, might not. Next, what we are going to do is now we are computing the probabilities. So we use torch.softmax, similar to what we have done with the multi-head attention. So we'll do that for the last dimension. And then we look up, so here maybe one more thing to explain, the arcmax. We are using the arcmax function in PyTorch. So arcmax is a, let me just move that down here. The arcmax function we are going to use next is a function in PyTorch that looks up the index position. So let me make an arbitrary example tensor. Let's do something like this. And so what arcmax is doing, it's finding the index position with the highest value. Here the highest value is the first value, so it should return a zero. Now if I put this 14, or let me just put a larger one here, it should return 012344. So it is returning the index position of the highest value in the probus here. That's what we are doing. And let's call that the next index of what we want to generate. So here we are going over the last dimension, because the first dimension is the batch dimension, so we don't want to mix up different batches, we want to have this for each. So here we are only looking at one batch, but we have maybe two rows if we have a batch size of two. So we don't want to mix between batches, we want to keep them independent. So we are looking at the last dimension. And let's also keep them true, so that the dimension matches the one that we have as the input, and then we can nicely, I hope, concatenate. Let's do index, index, next, and the dimensions is always something you have to keep in mind to make sure everything stays the same. And if I didn't make a mistake here, this should be adding the next generated token, which you see here, back to the input here. Basically, we are not converting back to text here. That is what we will do later with the tokenizer. We are here operating in this token ID dimension. Okay, so let's hope I didn't make a mistake, and let's try this out in practice. So for that, we are going to use this text, and then we will generate the output. We'll use our generate text simple, and let me just copy and paste here what we have. It's maybe easier this way. And then we assign the model here. The index is our encoded tensor. Max new tokens, how many do we want? You can experiment with this. Let's do six. And the context size, let's just reuse this here. This should be context length. And hopefully this works. If I haven't made a mistake, I did, of course. Oh, I think I forgot a colon, of course. So this would not work yet. So I need a colon here. Keep them without the underscore. There we go. Now we have something. And let's take a look at how that looks like. So we have now token IDs. Now the last thing, of course, is to use our decode method to convert token IDs back into words. You can see this does not work. The reason is this is a tensor, and our tokenizer works with Python lists. So we have to convert this back into Python lists. So first we can do maybe a squeeze to get rid of one dimension. So this got rid of one of the dimensions. And then we can do a to list. I always forget if we need an underscore or not. Let's just see. Yeah, this worked. So we have a list now, and we can see what happens here. So we can see it generated something, but this does not look like what we want to generate at all. You can see this, of course, is our input context. This looks correct. But this, for example, does not look like at all what we have seen here. It doesn't say, hello, I'm a model ready to help. It is more like gibberish. And the reason is we have initialized our model with random weights, and we have not trained the model yet. So the model training will be actually the topic of chapter five. And once we have completed chapter five, hopefully this will actually generate useful text. But again, we have this generate text method, and we can actually reuse that after the training to see that this actually will help us generate coherent text. But yeah, stay tuned for the next chapter on training the model.
